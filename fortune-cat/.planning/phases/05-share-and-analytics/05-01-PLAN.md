---
phase: 05-share-and-analytics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - granite.config.ts
  - src/pages/TarotPage.jsx
  - src/components/TarotShuffle.jsx
autonomous: true
requirements:
  - SHARE-01
  - ANL-01
  - ANL-02
  - ANL-03
requirements_addressed:
  - SHARE-01 (TarotPage.handleShare 본체 — getTossShareLink + share — Phase 3 stub 교체)
  - ANL-01 (tarot_view 이벤트 — TarotPage useEffect 안 already_drawn 플래그 발화)
  - ANL-02 (card_drawn 이벤트 — handleSelectCard 안 card_id + slot 발화)
  - ANL-03 (card_shared 이벤트 — handleShare 의 share() 성공 후 card_id + with_link 발화)

must_haves:
  truths:
    - "사용자가 result 화면에서 공유하기를 누르면 토스 공유 시트가 열려 카드 헤드라인 + 메시지(80자 트림) + 토스 링크가 외부로 공유된다 (SHARE-01)"
    - "Sandbox 환경에서는 deeplink = intoss-private://appsintoss?_deploymentId=... 가, Production 에서는 intoss://fortune-cat/tarot 가 사용된다 (D-02)"
    - "공유 실패(getTossShareLink 실패 / share 취소·실패) 시 사용자 알림 없이 silent 로 console.error 만 남는다 (D-09)"
    - "사용자가 타로 탭에 진입하면 Firebase Analytics 에 tarot_view 이벤트가 already_drawn 플래그와 함께 1회 기록된다 (ANL-01)"
    - "사용자가 카드를 뽑으면 Firebase Analytics 에 card_drawn 이벤트가 card_id + slot 파라미터로 기록된다 (ANL-02)"
    - "사용자가 결과를 공유하면 Firebase Analytics 에 card_shared 이벤트가 card_id + with_link 플래그와 함께 기록된다 (ANL-03)"
    - "앱 displayName 이 '복냥사주&타로' 로 변경되어 빌드 산출물에 통합 브랜딩이 반영된다 (D-04)"
    - "Phase 3·4 잠금 시그니처(useState 7종, todayDraw 훅, pickThreeRandom, handleHome/handleRetry/startShuffle/handleSelectCard, TarotIntro 인라인) 가 모두 보존된다"
    - "v1.0 사주/딥리딩/부적 흐름은 본 plan 변경에 영향받지 않는다 (NAV-03 회귀 방지 — TarotPage / TarotShuffle / granite.config.ts 외 파일 미수정)"
  artifacts:
    - path: "granite.config.ts"
      provides: "통합 브랜딩 displayName"
      contains: "복냥사주&타로"
    - path: "src/pages/TarotPage.jsx"
      provides: "handleShare 실구현 + 3종 logEvent 발화 + slot 인자 수신"
      contains: "getTossShareLink"
      min_lines: 280
    - path: "src/components/TarotShuffle.jsx"
      provides: "onSelect 시그니처 확장 (id, slot)"
      contains: "onSelect(cards[selectedSlot].id, selectedSlot)"
  key_links:
    - from: "src/pages/TarotPage.jsx handleShare"
      to: "@apps-in-toss/web-framework getTossShareLink + share"
      via: "AIT bridge 호출"
      pattern: "await getTossShareLink\\("
    - from: "src/pages/TarotPage.jsx handleShare"
      to: "src/lib/supabase.js getOgImageUrl"
      via: "OG 이미지 URL 조회 (코드 변경 없음, getOgImageUrl 그대로 사용)"
      pattern: "getOgImageUrl\\(\\)"
    - from: "src/pages/TarotPage.jsx handleShare"
      to: "@apps-in-toss/web-framework getOperationalEnvironment + env"
      via: "sandbox/production deeplink 분기"
      pattern: "getOperationalEnvironment\\(\\) === ['\"]sandbox['\"]"
    - from: "src/pages/TarotPage.jsx useEffect"
      to: "src/lib/firebase.js logEvent"
      via: "tarot_view 이벤트 — already_drawn 플래그 1회 발화"
      pattern: "logEvent\\(['\"]tarot_view['\"]"
    - from: "src/pages/TarotPage.jsx handleSelectCard"
      to: "src/lib/firebase.js logEvent"
      via: "card_drawn 이벤트 — card_id + slot"
      pattern: "logEvent\\(['\"]card_drawn['\"]"
    - from: "src/pages/TarotPage.jsx handleShare"
      to: "src/lib/firebase.js logEvent"
      via: "card_shared 이벤트 — share 성공 후 card_id + with_link"
      pattern: "logEvent\\(['\"]card_shared['\"]"
    - from: "src/components/TarotShuffle.jsx handleConfirm"
      to: "TarotPage handleSelectCard"
      via: "onSelect(id, slot) 시그니처로 slot 전달"
      pattern: "onSelect\\(cards\\[selectedSlot\\]\\.id, selectedSlot\\)"
---

<objective>
Phase 5 의 4개 요구사항(SHARE-01, ANL-01/02/03) + 9개 잠금 결정(D-01~D-09)을 단일 plan 으로 완성한다.

Purpose: TarotPage 의 `handleShare` stub 을 v1.0 HomePage 패턴으로 실구현하고, 타로 진입·카드 뽑기·공유 3종 Firebase Analytics 이벤트를 발화한다. 추가로 granite.config.ts 의 displayName 을 '복냥사주&타로' 통합 브랜딩으로 갱신한다. Phase 3·4 잠금 시그니처(useState 7종, useTodayDrawStorage 훅, pickThreeRandom, handleHome/handleRetry/startShuffle/handleSelectCard, TarotIntro 인라인)는 모두 보존하며 추가만 한다.

Output:
- granite.config.ts (displayName 1줄 변경 — D-04)
- src/pages/TarotPage.jsx (handleShare 본체 + tarot_view useEffect + handleSelectCard slot/card_drawn + handleShare 의 card_shared)
- src/components/TarotShuffle.jsx (onSelect 시그니처 확장 — slot 전달)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-share-and-analytics/05-CONTEXT.md
@.planning/phases/04-daily-lock-and-storage/04-CONTEXT.md
@.planning/phases/04-daily-lock-and-storage/04-02-SUMMARY.md
@src/pages/TarotPage.jsx
@src/pages/HomePage.jsx
@src/lib/firebase.js
@src/lib/supabase.js
@src/components/TarotShuffle.jsx
@src/components/DeepReadingResult.jsx
@granite.config.ts

<interfaces>
<!-- v1.0 정본 패턴과 본 plan 이 사용할 외부 API 시그니처. 실행자는 본 블록만 읽고 코드 작성 가능. -->

### v1.0 share 정본 — `src/pages/HomePage.jsx:183-196`

```javascript
const handleShare = async () => {
  logEvent("share_click");
  try {
    const isSandbox = getOperationalEnvironment() === "sandbox";
    const deepLink = isSandbox
      ? `intoss-private://appsintoss?_deploymentId=${env.getDeploymentId()}`
      : "intoss://fortune-cat";

    const tossLink = await getTossShareLink(deepLink, getOgImageUrl());
    await share({ message: tossLink });
  } catch (error) {
    console.error("[HomePage] 공유 실패:", error);
  }
};
```

### Toss SDK — `@apps-in-toss/web-framework`

```typescript
function getTossShareLink(deepLink: string, ogImageUrl?: string): Promise<string>;
function share(options: { message: string }): Promise<void>;
function getOperationalEnvironment(): 'sandbox' | 'production' | ...;
const env: { getDeploymentId(): string };
```

### OG 이미지 URL 헬퍼 — `src/lib/supabase.js:36-39` (코드 변경 없음 — D-03)

```javascript
export function getOgImageUrl() {
  const { data } = supabase.storage.from("menu_images").getPublicUrl("og_image.png");
  return data.publicUrl;
}
```

### Firebase Analytics 래퍼 — `src/lib/firebase.js:30-35`

```javascript
export function logEvent(eventName, eventParams = {}) {
  if (analytics) {
    firebaseLogEvent(analytics, eventName, eventParams)
    console.log(`[Firebase] Event logged: ${eventName}`, eventParams)
  }
}
```

### TarotPage 의 todayDraw / todayKST (Phase 4 carry-forward)

```javascript
// src/pages/TarotPage.jsx 안에서 이미 import + 사용 중
import { useTodayDrawStorage } from '../hooks/useTodayDrawStorage';
import { todayKST } from '../utils/dateKST';

const { loading: storageLoading, todayDraw, saveTodayDraw, clearTodayDraw } = useTodayDrawStorage();

// 본 plan 의 already_drawn 플래그 계산식 (Phase 4 잠금 식 재사용 — 04-02-SUMMARY "hasTodayDraw 계산식 잠금"):
//   Boolean(todayDraw && todayDraw.date === todayKST())
```

### TarotShuffle 의 현재 시그니처 — `src/components/TarotShuffle.jsx:16,26-29`

```javascript
export default function TarotShuffle({ cards, onSelect }) {
  // ...
  const handleConfirm = () => {
    if (selectedSlot === null) return;
    onSelect(cards[selectedSlot].id);   // ← 본 plan 이 onSelect(cards[selectedSlot].id, selectedSlot) 로 확장
  };
}
```

### TarotPage handleSelectCard 의 현재 시그니처 — `src/pages/TarotPage.jsx:114-120`

```javascript
const handleSelectCard = (id) => {                  // ← 본 plan 이 (id, slot) 로 확장
  setSelectedCardId(id);
  saveTodayDraw({ date: todayKST(), card_id: id });
  setCurrentPage('result');
};
```

### TarotPage handleShare 의 현재 stub — `src/pages/TarotPage.jsx:131-133`

```javascript
const handleShare = () => {
  console.log('[TarotPage] share stub — Phase 5 SHARE-01 implements toss share sheet');
};
// ↑ 본 plan 이 async + getTossShareLink + share + try/catch silent + card_shared 발화로 교체
```

### selectedCard 객체 — `src/pages/TarotPage.jsx:139-141` (handleShare 가 사용)

```javascript
const selectedCard = selectedCardId !== null
  ? cardsData.find((c) => c.id === selectedCardId)
  : null;
// 구조: { id, name_ko, name_en, emoji, image_path, keywords, message }
// handleShare 안에서 selectedCard.name_ko / name_en / message 사용
```
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: granite.config.ts displayName 변경 (D-04)</name>
  <files>granite.config.ts</files>
  <read_first>
    - granite.config.ts (현재 displayName 확인 — line 6 = '복냥사주')
    - .planning/phases/05-share-and-analytics/05-CONTEXT.md (D-04 정의 + 변경 사유)
  </read_first>
  <action>
    `granite.config.ts` line 6 의 `displayName: '복냥사주'` 을 `displayName: '복냥사주&타로'` 로 변경한다.

    **변경 전:**
    ```typescript
    displayName: '복냥사주', // 화면에 노출될 앱의 한글 이름으로 바꿔주세요.
    ```

    **변경 후:**
    ```typescript
    displayName: '복냥사주&타로', // 화면에 노출될 앱의 한글 이름으로 바꿔주세요.
    ```

    **변경 금지:**
    - `appName` (= 'fortune-cat') — Toss 콘솔 등록명, 변경 시 심사 영향 (CLAUDE.md Constraints 참조)
    - `brand.icon` — 사용자가 디자인 자산 준비 후 별도 결정 (CONTEXT D-04 명시 deferred)
    - `primaryColor`, `bridgeColorMode`, `navigationBar`, `web`, `permissions`, `outdir` — 본 plan 책임 외
    - 그 외 한 줄도 손대지 않음 (DRY + CLAUDE.md "지시사항과 관련 없는 코드는 수정하지 마세요")

    **사유:** Toss 콘솔 등록명/아이콘 변경은 사용자가 직접 처리(deferred). 본 task 는 코드 산출물(`fortune-cat.ait`) 의 displayName 1줄만 통합 브랜딩으로 갱신.
  </action>
  <verify>
    <automated>
      grep -c "displayName: '복냥사주&타로'" granite.config.ts  # 결과: 1
      grep -c "displayName: '복냥사주'," granite.config.ts        # 결과: 0 (구 displayName 흔적 없음)
      git diff --stat granite.config.ts                          # 1 file changed, 1 insertion(+), 1 deletion(-)
      git diff --name-only HEAD                                   # granite.config.ts 만 (다른 파일 미수정 확인)
      npm run build                                               # exit 0 (Vite + AIT 빌드 PASS, fortune-cat.ait 산출)
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "displayName: '복냥사주&타로'" granite.config.ts` = 1
    - `grep -c "displayName: '복냥사주'," granite.config.ts` = 0
    - `git diff granite.config.ts` 가 정확히 1줄 add + 1줄 delete (displayName 라인만)
    - `git diff --name-only HEAD` 결과에 다른 파일 없음 (Task 1 만 단일 commit 시점)
    - `npm run build` exit 0 — AIT 빌드 산출물(`fortune-cat.ait`) 생성
  </acceptance_criteria>
  <done>
    granite.config.ts displayName = '복냥사주&타로' 적용 + 빌드 통과. Task 1 커밋: `feat(05-01): granite displayName → '복냥사주&타로' 통합 브랜딩 (D-04)`
  </done>
</task>

<task type="auto">
  <name>Task 2: TarotPage handleShare 실구현 + 3종 logEvent + TarotShuffle slot 전달 (D-01,02,05,06,07,08,09)</name>
  <files>src/pages/TarotPage.jsx, src/components/TarotShuffle.jsx</files>
  <read_first>
    - src/pages/TarotPage.jsx (전체 — Phase 3·4 잠금 시그니처 식별)
    - src/components/TarotShuffle.jsx (16-29 라인 — onSelect 호출 시점)
    - src/pages/HomePage.jsx (1-20, 183-196 — v1.0 share 정본 패턴 + import 시그니처)
    - src/lib/firebase.js (전체 — logEvent 시그니처)
    - src/lib/supabase.js (36-39 — getOgImageUrl 헬퍼, 코드 변경 없음 확인 D-03)
    - src/components/DeepReadingResult.jsx (10, 99-108 — logEvent + Analytics.click 패턴 참조, 본 plan 은 logEvent 만 사용)
    - .planning/phases/05-share-and-analytics/05-CONTEXT.md (D-01 ~ D-09 모두)
    - .planning/phases/04-daily-lock-and-storage/04-02-SUMMARY.md ("hasTodayDraw 계산식 잠금" — already_drawn 도 동일 식 재사용)
  </read_first>
  <action>
    **목표:** TarotPage 의 handleShare stub 을 실구현 + 3종 Firebase Analytics 이벤트(`tarot_view`/`card_drawn`/`card_shared`) 발화 + TarotShuffle 의 onSelect 시그니처에 slot 추가. Phase 3·4 잠금 시그니처(useState 7종, useTodayDrawStorage 훅 호출, pickThreeRandom, handleHome/handleRetry/startShuffle, TarotIntro 인라인 + props 시그니처)는 모두 보존하며 추가만 한다.

    ---

    ### Step 1 — TarotShuffle.jsx onSelect 시그니처 확장 (D-07 의 slot 전달 준비)

    `src/components/TarotShuffle.jsx` line 26-29 의 `handleConfirm`:

    **변경 전:**
    ```javascript
    const handleConfirm = () => {
      if (selectedSlot === null) return;
      onSelect(cards[selectedSlot].id);
    };
    ```

    **변경 후:**
    ```javascript
    const handleConfirm = () => {
      if (selectedSlot === null) return;
      // CONTEXT D-07: card_drawn 이벤트의 slot 파라미터(0/1/2) 를 TarotPage 로 전달.
      onSelect(cards[selectedSlot].id, selectedSlot);
    };
    ```

    **다른 라인 손대지 않음 (DRY).** 라인 2의 주석(`onSelect(id) 호출`)은 변경 가능 — `onSelect(id, slot) 호출`로 1글자만 보강.

    ---

    ### Step 2 — TarotPage.jsx import 추가

    `src/pages/TarotPage.jsx` line 12-22 의 import 블록에 다음 추가:

    ```javascript
    import {
      getTossShareLink,
      share,
      getOperationalEnvironment,
      env,
    } from '@apps-in-toss/web-framework';
    import { getOgImageUrl } from '../lib/supabase';
    import { logEvent } from '../lib/firebase';
    ```

    **위치:** 기존 import 그룹과 정합. `from '../lib/supabase'` 는 기존에 `fetchTarotCards` import 가 있으므로 같은 import 문에 합치는 것이 DRY (단 ESLint 가 strict 가 아니라면 별도 import 도 허용 — 단, `fetchTarotCards` import 와 같은 import 문에 통합 권장):

    ```javascript
    // 권장 — 기존 fetchTarotCards 와 한 import 로 통합 (DRY)
    import { fetchTarotCards, getOgImageUrl } from '../lib/supabase';
    ```

    **CommonMark/CommonJS 영향 없음** — 모두 named export.

    ---

    ### Step 3 — `tarot_view` 이벤트 발화 useEffect 추가 (D-06, ANL-01)

    Phase 4 의 lock useEffect (line 82-105) **다음** 위치에 신규 useEffect 추가:

    ```javascript
    // CONTEXT D-06 (ANL-01): tarot_view 이벤트 — storage + cardsData 준비 완료 후 1회 발화.
    // already_drawn = Boolean(todayDraw && todayDraw.date === todayKST())
    // Phase 4 의 hasTodayDraw 계산식 그대로 재사용 (04-02-SUMMARY "hasTodayDraw 계산식 잠금").
    // RESEARCH Pitfall 7: cleanup cancelled flag 안에서 호출 (unmount 후 logEvent 차단).
    useEffect(() => {
      if (storageLoading) return;
      if (errorState) return;
      if (!cardsData || cardsData.length === 0) return;

      let cancelled = false;
      // 마이크로태스크로 미루어 unmount 직후 케이스에 cancelled flag 가 작동하도록 보강
      Promise.resolve().then(() => {
        if (cancelled) return;
        const alreadyDrawn = Boolean(todayDraw && todayDraw.date === todayKST());
        logEvent('tarot_view', { already_drawn: alreadyDrawn });
      });
      return () => { cancelled = true; };
    }, [storageLoading, errorState, cardsData, todayDraw]);
    ```

    **deps 5종 (storageLoading + errorState + cardsData + todayDraw)** — `clearTodayDraw` setter 는 ESLint react-hooks/exhaustive-deps 가 stable 보장 + 본 effect 가 호출 안 함, 미포함.

    **발화 시점:** storage 로드 + cardsData fetch 완료 후 1회. todayDraw 가 변경되면 재발화 (자정 리셋으로 todayDraw → null 변경 시 재발화는 의도된 동작 — already_drawn 분포 측정).

    **Pitfall 회피:** Pitfall 7 의 cleanup cancelled flag 패턴 적용. `Promise.resolve().then` 으로 마이크로태스크 큐에 미루어 unmount 와의 race 회피.

    ---

    ### Step 4 — handleSelectCard 시그니처 확장 + `card_drawn` 발화 (D-07, ANL-02)

    line 114-120 의 `handleSelectCard`:

    **변경 전:**
    ```javascript
    const handleSelectCard = (id) => {
      setSelectedCardId(id);
      saveTodayDraw({ date: todayKST(), card_id: id });
      setCurrentPage('result');
    };
    ```

    **변경 후:**
    ```javascript
    const handleSelectCard = (id, slot) => {
      setSelectedCardId(id);
      // CONTEXT D-11 (Phase 4): shuffle 확정 시 todayDraw 영속 저장 (fire-and-forget).
      saveTodayDraw({ date: todayKST(), card_id: id });
      // CONTEXT D-07 (ANL-02): card_drawn 이벤트 — saveTodayDraw 와 동일 라인에 발화 (둘 다 fire-and-forget OK).
      logEvent('card_drawn', { card_id: id, slot });
      setCurrentPage('result');
    };
    ```

    **slot 인자 출처:** Step 1 에서 확장한 `TarotShuffle` 의 `onSelect(id, slot)` 콜백.

    **Phase 4 잠금 보존:** `saveTodayDraw` 호출 라인은 byte-for-byte 유지. `setCurrentPage('result')` 도 동일.

    ---

    ### Step 5 — handleShare 본체 교체 (D-01, D-02, D-08, D-09 + SHARE-01, ANL-03)

    line 131-133 의 stub:

    **변경 전:**
    ```javascript
    // 공유하기 = Phase 5 (SHARE-01) 가 토스 공유 시트 연결. Phase 3 stub.
    const handleShare = () => {
      console.log('[TarotPage] share stub — Phase 5 SHARE-01 implements toss share sheet');
    };
    ```

    **변경 후:**
    ```javascript
    // CONTEXT D-01/D-02/D-08/D-09 (SHARE-01 + ANL-03): handleShare 실구현.
    // - D-01: 메시지 = 헤드라인 + 80자 트림 + 토스 링크
    // - D-02: deeplink = sandbox/production 분기
    // - D-08: card_shared 이벤트 = share() 성공 후 1회
    // - D-09: 실패 silent (try/catch + console.error 만, 사용자 알림 없음, HomePage 패턴 동일)
    const handleShare = async () => {
      if (!selectedCard) return; // result 단계 외 호출 방지 (defensive)

      // D-01 헤드라인 + 본문(80자 트림)
      const headline = `[복냥타로] 오늘의 카드 — ${selectedCard.name_ko} · ${selectedCard.name_en}`;
      const snippet = selectedCard.message.length > 80
        ? selectedCard.message.slice(0, 80) + '…'
        : selectedCard.message;

      // D-02 deeplink + getTossShareLink — 실패 시 link = undefined 로 두고 메시지만 공유
      let link;
      try {
        const isSandbox = getOperationalEnvironment() === 'sandbox';
        const deepLink = isSandbox
          ? `intoss-private://appsintoss?_deploymentId=${env.getDeploymentId()}`
          : 'intoss://fortune-cat/tarot';
        link = await getTossShareLink(deepLink, getOgImageUrl());
      } catch (error) {
        console.error('[TarotPage] getTossShareLink 실패:', error);
        // link = undefined — D-09: 헤드라인 + 본문만 공유 시도
      }

      const message = link
        ? `${headline}\n${snippet}\n${link}`
        : `${headline}\n${snippet}`;

      try {
        await share({ message });
        // D-08 (ANL-03): share() 성공 후에만 발화. 사용자 취소·실패 catch 에서는 발화 X.
        logEvent('card_shared', {
          card_id: selectedCard.id,
          with_link: Boolean(link),
        });
      } catch (error) {
        // D-09: silent — 사용자 알림 없음, HomePage 패턴
        console.error('[TarotPage] 공유 실패:', error);
      }
    };
    ```

    **D-09 일관성:**
    - getTossShareLink 실패 → link = undefined, 메시지만 공유 시도 (silent)
    - share 실패 → console.error 만 (silent)
    - 토스트/alert 없음 (HomePage 패턴 동일, CONTEXT D-09 명시)

    **D-08 발화 시점 보장:** `await share` 가 throw 하지 않은 경우에만 logEvent 호출. catch 블록에서는 발화 X.

    **`selectedCard` 사용 안전:** Phase 3 의 `selectedCard` 계산식(line 139-141) 변경 없음. result 단계에서만 호출되며 stub 의 `if (!selectedCard) return` 가드로 보강.

    ---

    ### Step 6 — 잠금 보존 self-check (실행자가 코드 작성 후 직접 grep)

    다음 항목들이 변경되지 않았는지 확인:

    - `useState` 7종 모두 line 37-43 그대로
    - `useTodayDrawStorage()` 호출 line 46 그대로
    - `pickThreeRandom` 함수 정의 line 26-34 그대로
    - `handleHome` 정의 (Step 5 이전 line) 그대로
    - `handleRetry` 정의 그대로
    - `startShuffle` 정의 그대로
    - `function TarotIntro({ hasTodayDraw, onStart, onResume })` 인라인 컴포넌트 시그니처 + body 그대로
    - Phase 4 의 lock useEffect (line 82-105) byte-for-byte 그대로
    - Phase 3 의 fetch useEffect (line 49-75) byte-for-byte 그대로

    **변경 외 영역 한 줄도 손대지 않음 (CLAUDE.md "지시사항과 관련 없는 코드는 수정하지 마세요").**
  </action>
  <verify>
    <automated>
      # === TarotShuffle.jsx onSelect 시그니처 확장 ===
      grep -c "onSelect(cards\[selectedSlot\].id, selectedSlot)" src/components/TarotShuffle.jsx  # 1
      grep -c "onSelect(cards\[selectedSlot\].id);$" src/components/TarotShuffle.jsx              # 0 (구 시그니처 없음)

      # === TarotPage.jsx import ===
      grep -c "getTossShareLink" src/pages/TarotPage.jsx                                          # >=1 (import + handleShare)
      grep -c "from '@apps-in-toss/web-framework'" src/pages/TarotPage.jsx                        # >=1
      grep -c "getOgImageUrl" src/pages/TarotPage.jsx                                             # >=2 (import + handleShare 내 호출)
      grep -c "import.*logEvent.*from '\\.\\./lib/firebase'" src/pages/TarotPage.jsx              # 1

      # === tarot_view 이벤트 (D-06 ANL-01) ===
      grep -c "logEvent('tarot_view'" src/pages/TarotPage.jsx                                     # 1
      grep -c "already_drawn:" src/pages/TarotPage.jsx                                            # 1
      grep -c "Boolean(todayDraw && todayDraw.date === todayKST())" src/pages/TarotPage.jsx       # >=2 (Phase 4 hasTodayDraw + 본 plan ANL-01)

      # === card_drawn 이벤트 (D-07 ANL-02) ===
      grep -c "const handleSelectCard = (id, slot) =>" src/pages/TarotPage.jsx                    # 1
      grep -c "logEvent('card_drawn'" src/pages/TarotPage.jsx                                     # 1
      grep -c "card_id: id, slot" src/pages/TarotPage.jsx                                         # 1

      # === card_shared 이벤트 (D-08 ANL-03) + handleShare 실구현 (D-01/02/09 SHARE-01) ===
      grep -c "const handleShare = async () =>" src/pages/TarotPage.jsx                           # 1
      grep -c "logEvent('card_shared'" src/pages/TarotPage.jsx                                    # 1
      grep -c "with_link: Boolean(link)" src/pages/TarotPage.jsx                                  # 1
      grep -c "\\[복냥타로\\] 오늘의 카드" src/pages/TarotPage.jsx                                  # 1
      grep -c "selectedCard.message.length > 80" src/pages/TarotPage.jsx                          # 1
      grep -c "intoss://fortune-cat/tarot" src/pages/TarotPage.jsx                                # 1
      grep -c "intoss-private://appsintoss?_deploymentId=" src/pages/TarotPage.jsx                # 1
      grep -c "getOperationalEnvironment() === 'sandbox'" src/pages/TarotPage.jsx                 # 1
      grep -c "console.error('\\[TarotPage\\] 공유 실패:'" src/pages/TarotPage.jsx                # 1
      grep -c "console.error('\\[TarotPage\\] getTossShareLink 실패:'" src/pages/TarotPage.jsx    # 1

      # === handleShare stub 흔적 제거 ===
      grep -c "share stub — Phase 5" src/pages/TarotPage.jsx                                      # 0

      # === Phase 3·4 잠금 시그니처 보존 ===
      grep -c "function pickThreeRandom" src/pages/TarotPage.jsx                                  # 1
      grep -c "const startShuffle" src/pages/TarotPage.jsx                                        # 1
      grep -c "const handleHome" src/pages/TarotPage.jsx                                          # 1
      grep -c "const handleRetry" src/pages/TarotPage.jsx                                         # 1
      grep -c "function TarotIntro({ hasTodayDraw, onStart, onResume })" src/pages/TarotPage.jsx  # 1
      grep -c "useTodayDrawStorage()" src/pages/TarotPage.jsx                                     # 1
      grep -c "import { todayKST } from '../utils/dateKST'" src/pages/TarotPage.jsx               # 1
      grep -c "오늘의 카드 다시 보기 ✨" src/pages/TarotPage.jsx                                    # 1
      grep -c "오늘의 카드 뽑기 ✨" src/pages/TarotPage.jsx                                        # 1
      # useState 7종
      grep -E "const \[(currentPage|cardsData|shuffledThree|selectedCardId|isLoading|errorState|retryNonce)" src/pages/TarotPage.jsx | wc -l   # 7

      # === 신규 의존성 0 (P1-D-08 carry) ===
      grep -c "framer-motion\|zustand\|date-fns\|dayjs" src/pages/TarotPage.jsx src/components/TarotShuffle.jsx 2>/dev/null  # 0

      # === 변경 파일 범위 (granite.config.ts 는 Task 1 commit, Task 2 는 src/ 만) ===
      git diff --name-only HEAD~1 HEAD -- src/                                                     # 결과: src/pages/TarotPage.jsx, src/components/TarotShuffle.jsx 2개
      git diff --stat package.json package-lock.json pnpm-lock.yaml                                 # 빈 (변경 없음)

      # === 빌드 + 린트 ===
      npx eslint src/pages/TarotPage.jsx src/components/TarotShuffle.jsx                           # 0 errors
      npm run build                                                                                 # exit 0 (Vite + AIT, fortune-cat.ait 산출)
    </automated>
  </verify>
  <acceptance_criteria>
    **TarotShuffle.jsx (Step 1):**
    - `grep -c "onSelect(cards\[selectedSlot\].id, selectedSlot)" src/components/TarotShuffle.jsx` = 1
    - `grep -c "onSelect(cards\[selectedSlot\].id);$" src/components/TarotShuffle.jsx` = 0
    - 변경 줄 수 ≤ 3 (handleConfirm 본체 + 주석 보강만)

    **TarotPage.jsx imports (Step 2):**
    - `grep -c "getTossShareLink" src/pages/TarotPage.jsx` ≥ 1
    - `grep -c "from '@apps-in-toss/web-framework'" src/pages/TarotPage.jsx` ≥ 1 (Step 2 추가 import)
    - `grep -c "getOgImageUrl" src/pages/TarotPage.jsx` ≥ 2 (import + handleShare 호출)
    - `grep -c "import.*logEvent.*firebase" src/pages/TarotPage.jsx` = 1

    **tarot_view 이벤트 (Step 3, D-06 ANL-01):**
    - `grep -c "logEvent('tarot_view'" src/pages/TarotPage.jsx` = 1
    - `grep -c "already_drawn:" src/pages/TarotPage.jsx` = 1
    - useEffect deps = `[storageLoading, errorState, cardsData, todayDraw]` (4종, clearTodayDraw 미포함)
    - cleanup cancelled flag 패턴 적용 (Pitfall 7)

    **card_drawn 이벤트 (Step 4, D-07 ANL-02):**
    - `grep -c "const handleSelectCard = (id, slot) =>" src/pages/TarotPage.jsx` = 1
    - `grep -c "logEvent('card_drawn'" src/pages/TarotPage.jsx` = 1
    - `grep -c "card_id: id, slot" src/pages/TarotPage.jsx` = 1

    **handleShare 실구현 + card_shared (Step 5, D-01/02/08/09 + SHARE-01 ANL-03):**
    - `grep -c "const handleShare = async () =>" src/pages/TarotPage.jsx` = 1
    - `grep -c "logEvent('card_shared'" src/pages/TarotPage.jsx` = 1
    - `grep -c "with_link: Boolean(link)" src/pages/TarotPage.jsx` = 1
    - `grep -c "\\[복냥타로\\] 오늘의 카드" src/pages/TarotPage.jsx` = 1 (헤드라인)
    - `grep -c "selectedCard.message.length > 80" src/pages/TarotPage.jsx` = 1 (80자 트림)
    - `grep -c "intoss://fortune-cat/tarot" src/pages/TarotPage.jsx` = 1 (production deeplink)
    - `grep -c "intoss-private://appsintoss?_deploymentId=" src/pages/TarotPage.jsx` = 1 (sandbox deeplink)
    - `grep -c "getOperationalEnvironment() === 'sandbox'" src/pages/TarotPage.jsx` = 1
    - `grep -c "console.error('\\[TarotPage\\] 공유 실패:'" src/pages/TarotPage.jsx` = 1 (D-09 silent)
    - `grep -c "console.error('\\[TarotPage\\] getTossShareLink 실패:'" src/pages/TarotPage.jsx` = 1 (D-09 link 실패도 silent)
    - `grep -c "share stub — Phase 5" src/pages/TarotPage.jsx` = 0 (stub 흔적 제거)
    - `await share({ message })` 라인이 try 블록 안에 있고 logEvent 가 그 try 블록 안 (catch 에는 logEvent 없음)

    **Phase 3·4 잠금 시그니처 보존:**
    - `grep -c "function pickThreeRandom" src/pages/TarotPage.jsx` = 1
    - `grep -c "const startShuffle" src/pages/TarotPage.jsx` = 1
    - `grep -c "const handleHome" src/pages/TarotPage.jsx` = 1
    - `grep -c "const handleRetry" src/pages/TarotPage.jsx` = 1
    - `grep -c "function TarotIntro({ hasTodayDraw, onStart, onResume })" src/pages/TarotPage.jsx` = 1
    - `grep -c "useTodayDrawStorage()" src/pages/TarotPage.jsx` = 1
    - `grep -c "import { todayKST } from '../utils/dateKST'" src/pages/TarotPage.jsx` = 1
    - `grep -E "const \[(currentPage|cardsData|shuffledThree|selectedCardId|isLoading|errorState|retryNonce)" src/pages/TarotPage.jsx | wc -l` = 7
    - `grep -c "오늘의 카드 다시 보기 ✨" src/pages/TarotPage.jsx` = 1
    - `grep -c "오늘의 카드 뽑기 ✨" src/pages/TarotPage.jsx` = 1
    - Phase 4 의 lock useEffect (storageLoading 가드 + clearTodayDraw 호출) 보존

    **신규 의존성 0 (P1-D-08 carry):**
    - `grep -c "framer-motion\\|zustand\\|date-fns\\|dayjs" src/pages/TarotPage.jsx src/components/TarotShuffle.jsx` = 0

    **변경 범위 (회귀 방지):**
    - `git diff --name-only HEAD~1 HEAD -- src/` 결과 = `src/pages/TarotPage.jsx`, `src/components/TarotShuffle.jsx` (정확히 2개)
    - `git diff --stat package.json package-lock.json pnpm-lock.yaml` 빈 (lockfile 변경 없음)
    - `git diff --stat src/pages/HomePage.jsx src/pages/SajuPage.jsx src/pages/NewYearPage.jsx src/pages/AmuletPage.jsx` 빈 (v1.0 페이지 미수정)
    - `git diff --stat src/lib/` 빈 (firebase.js, supabase.js 미수정 — D-03)

    **빌드 + 린트:**
    - `npx eslint src/pages/TarotPage.jsx src/components/TarotShuffle.jsx` 0 errors
    - `npm run build` exit 0 — `fortune-cat.ait` 산출

    **선택 카드 안전성:**
    - handleShare 본체에 `if (!selectedCard) return;` 가드 존재 (defensive — currentPage='result' 외 호출 방지)
  </acceptance_criteria>
  <done>
    Phase 5 의 9개 D-XX 결정 + 4개 요구사항 모두 코드 반영 완료. Phase 3·4 잠금 시그니처 보존 + v1.0 페이지 무영향 + 빌드 통과.

    Task 2 커밋: `feat(05-01): TarotPage handleShare 실구현 + 3종 logEvent + TarotShuffle slot 전달 (SHARE-01, ANL-01/02/03, D-01~D-09)`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TarotPage → @apps-in-toss/web-framework (`getTossShareLink`, `share`, `getOperationalEnvironment`, `env`) | 토스 네이티브 브릿지 — AIT WebView 안에서만 작동. 일반 브라우저/dev 환경에서는 throw 가능. |
| TarotPage → Firebase Analytics (`logEvent` via `src/lib/firebase.js`) | Firebase 초기화 실패 시 wrapper 가 silent 처리 (firebase.js:30-35). 외부 네트워크 호출. |
| TarotPage → Supabase Storage (`getOgImageUrl`) | 정적 URL 생성만 — 실제 fetch 는 Toss 가 처리. 본 plan 코드 변경 없음 (D-03). |
| TarotShuffle → TarotPage (onSelect 콜백) | 같은 레포 컴포넌트 간 props — 신규 trust boundary 도입 0. selectedSlot (number 0/1/2) 만 추가 전달. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | handleShare 메시지 본문 (D-01: name_ko/name_en/message) | accept | 카드 이름 + 해석 텍스트는 Supabase 의 공개 시드 데이터 (`tarot_cards`) — PII 무관. 사용자가 명시적으로 "공유" 버튼을 누른 경우에만 외부 전송. |
| T-05-02 | Tampering | getOperationalEnvironment 결과 위조 → sandbox deeplink 가 production 에서 사용됨 | accept | `getOperationalEnvironment()` 는 토스 SDK 시그니처 — 사용자 공격면 없음 (앱 빌드 시점 결정). HomePage 도 동일 패턴 (T-fix 0). |
| T-05-03 | Denial of Service | getTossShareLink / share await 가 무한 hang | mitigate | 본 plan 의 try/catch 블록이 throw 시 silent 처리 (D-09). hang 자체는 토스 SDK 책임 — UI block 은 result 화면이 sync 로 이미 표시된 상태에서 발생하므로 사용자 영향 최소 (버튼 재탭 가능). |
| T-05-04 | Information Disclosure | logEvent 의 `card_id` / `slot` / `with_link` 파라미터 → Firebase 로 전송 | accept | card_id (1~22) + slot (0/1/2) + with_link (boolean) 모두 PII 무관. Firebase user_id 는 Toss 익명키 (`useAnonymousKey`) 기반 — 별도 PII 없음. |
| T-05-05 | Repudiation | share() 성공 후 logEvent('card_shared') 발화 사이에 throw → 공유는 됐지만 이벤트 누락 | accept | logEvent 자체는 fire-and-forget + Firebase wrapper silent. share 성공 직후 동기 호출이라 누락 가능성 매우 낮음. funnel 측정 정확도 영향 ≤ 0.1% — accept. |
| T-05-06 | Tampering | 일반 브라우저(dev)에서 `getTossShareLink` 미정의 → throw → handleShare 내 try/catch 가 link=undefined 로 fallback → share() 도 미정의 → 두 번째 catch 도 silent | mitigate | D-09 silent 정책 그대로. dev 환경에서 공유 버튼 눌러도 console.error 만 + 사용자 차단 없음. AIT 빌드만 정상 동작 (CONTEXT 기존 정책). |
| T-05-07 | Spoofing | 공격자가 스푸핑한 OG 이미지 URL → 토스 공유 시트에 가짜 미리보기 노출 | accept | getOgImageUrl 결과는 Supabase Storage 의 fixed URL (`menu_images/og_image.png`). 본 plan 코드 변경 없음 (D-03). 자산 자체 교체는 사용자가 Storage 콘솔에서 직접 — 별도 trust boundary. |
| T-05-08 | Elevation of Privilege | TarotShuffle.onSelect 콜백 시그니처 확장 — slot 인자 추가가 외부 호출자에게 영향 | mitigate | TarotShuffle 은 TarotPage 안에서만 사용 (`src/pages/TarotPage.jsx:232` 단 1곳 — grep 확인). 시그니처 확장 시 호출자 코드도 같은 plan 의 Step 4 에서 동시 수정. 외부 영향 0. |
| T-05-09 | Information Disclosure | tarot_view 이벤트의 `already_drawn` 플래그 — Firebase 로 전송 | accept | boolean 1개 — PII 무관. retention 측정용 의도적 노출 (CONTEXT D-06 명시). |

**모든 mitigation 은 본 plan 의 Step 1~5 내에 byte-for-byte 명시된 코드로 구현. accept 들은 CONTEXT 명시 정책 (D-09 silent / 익명키 기반 분석 / Phase 4 carry-forward) 의 직접 결과.**
</threat_model>

<verification>
## 자동 검증 (실행자가 task verify 블록 + 본 섹션 모두 PASS 해야 plan 종결)

```bash
# === SHARE-01 (D-01/02/09) ===
grep -c "\\[복냥타로\\] 오늘의 카드" src/pages/TarotPage.jsx                   # 1
grep -c "selectedCard.message.length > 80" src/pages/TarotPage.jsx             # 1
grep -c "intoss://fortune-cat/tarot" src/pages/TarotPage.jsx                   # 1
grep -c "intoss-private://appsintoss?_deploymentId=" src/pages/TarotPage.jsx   # 1
grep -c "getOperationalEnvironment" src/pages/TarotPage.jsx                    # >=1
grep -c "await share({ message })" src/pages/TarotPage.jsx                     # 1
grep -c "console.error('\\[TarotPage\\] 공유 실패:'" src/pages/TarotPage.jsx   # 1

# === ANL-01 (D-06) ===
grep -c "logEvent('tarot_view'" src/pages/TarotPage.jsx                         # 1
grep -c "already_drawn:" src/pages/TarotPage.jsx                                # 1

# === ANL-02 (D-07) ===
grep -c "logEvent('card_drawn'" src/pages/TarotPage.jsx                         # 1
grep -c "card_id: id, slot" src/pages/TarotPage.jsx                             # 1
grep -c "onSelect(cards\\[selectedSlot\\].id, selectedSlot)" src/components/TarotShuffle.jsx  # 1

# === ANL-03 (D-08) ===
grep -c "logEvent('card_shared'" src/pages/TarotPage.jsx                        # 1
grep -c "with_link: Boolean(link)" src/pages/TarotPage.jsx                      # 1

# === D-04 displayName ===
grep -c "displayName: '복냥사주&타로'" granite.config.ts                      # 1

# === D-03 OG 이미지 코드 변경 없음 ===
git diff --stat src/lib/supabase.js                                             # 빈

# === Phase 3·4 잠금 보존 ===
grep -c "useTodayDrawStorage()" src/pages/TarotPage.jsx                         # 1
grep -c "function pickThreeRandom" src/pages/TarotPage.jsx                      # 1
grep -c "function TarotIntro({ hasTodayDraw, onStart, onResume })" src/pages/TarotPage.jsx  # 1
grep -E "const \\[(currentPage|cardsData|shuffledThree|selectedCardId|isLoading|errorState|retryNonce)" src/pages/TarotPage.jsx | wc -l  # 7

# === v1.0 무영향 (NAV-03 회귀 방지) ===
git diff --stat src/pages/HomePage.jsx src/pages/SajuPage.jsx src/pages/NewYearPage.jsx src/pages/AmuletPage.jsx  # 빈
git diff --stat src/lib/firebase.js src/lib/supabase.js                                                          # 빈

# === 신규 의존성 0 (P1-D-08 carry) ===
grep -rn "framer-motion\\|zustand\\|date-fns\\|dayjs" src/                       # 0
git diff --stat package.json package-lock.json pnpm-lock.yaml                    # 빈

# === 빌드 + 린트 ===
npx eslint src/pages/TarotPage.jsx src/components/TarotShuffle.jsx granite.config.ts  # 0 errors
npm run build                                                                          # exit 0
```

## 인간 UAT (코드 자동 검증 외 — 별도 HUMAN-UAT.md 에 추적)

본 plan 의 Firebase 콘솔 이벤트 수신 검증과 토스 공유 시트 실제 동작 검증은 자동화 불가 — Phase 4 의 04-HUMAN-UAT.md 패턴으로 별도 추적:

1. AIT WebView 빌드 산출물(`fortune-cat.ait`) 을 dev 디바이스에 배포
2. 타로 탭 진입 → Firebase 콘솔 (DebugView 또는 Realtime) 에서 `tarot_view` 이벤트 + `already_drawn=false` 확인
3. 카드 뽑기 → `card_drawn` + `card_id`, `slot` 파라미터 확인
4. 같은 날 재진입 → `tarot_view` + `already_drawn=true` 확인
5. result 화면 공유하기 → 토스 공유 시트 노출 + 메시지에 헤드라인/80자 본문/링크 모두 포함
6. 공유 완료 → `card_shared` + `card_id`, `with_link=true` 확인
7. 공유 취소 → `card_shared` 미발화 (silent) 확인
8. dev 일반 브라우저에서 공유 버튼 → console.error 만 + 사용자 차단 없음 확인
9. 빌드 산출물 displayName 이 토스 미니앱 카드에 '복냥사주&타로' 로 표시되는지 확인
</verification>

<success_criteria>
- 4개 요구사항(SHARE-01, ANL-01, ANL-02, ANL-03) 모두 코드에 반영
- 9개 잠금 결정(D-01 ~ D-09) 모두 byte-for-byte 명시 코드 그대로 구현
- Phase 3·4 잠금 시그니처(useState 7종, useTodayDrawStorage 훅 호출, pickThreeRandom, handleHome/handleRetry/startShuffle, TarotIntro 인라인) 모두 grep 보존 확인
- v1.0 페이지(HomePage/SajuPage/NewYearPage/AmuletPage) + lib(firebase.js/supabase.js) 미수정 (NAV-03 회귀 방지)
- 신규 의존성 0 (P1-D-08 carry — framer-motion/zustand/date-fns/dayjs 미도입)
- ESLint 0 errors + AIT 빌드 성공 (`fortune-cat.ait` 산출)
- 변경 파일 정확히 3개: granite.config.ts, src/pages/TarotPage.jsx, src/components/TarotShuffle.jsx
</success_criteria>

<output>
완료 후 다음 SUMMARY 파일을 생성:

`.planning/phases/05-share-and-analytics/05-01-SUMMARY.md`

다음 항목을 포함:
- Performance (duration / files modified)
- Accomplishments (D-01 ~ D-09, 4개 요구사항 매핑)
- Task Commits (Task 1: granite, Task 2: TarotPage + TarotShuffle)
- Files Modified (정확히 3개)
- Decisions Made (구현 시 fork 결정 — slot 시그니처 확장 위치, tarot_view useEffect 위치 등)
- Deviations from Plan (있다면)
- Verification Results (자동 검증 grep 결과 + ESLint + 빌드)
- Threat Model Verification (T-05-01 ~ T-05-09)
- Phase 5 종결 노트 (v1.1 마일스톤 코드 반영 완료, 인간 UAT 보류 항목)
- Self-Check (잠금 보존 + 변경 범위)
</output>
