---
phase: 04-daily-lock-and-storage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useTodayDrawStorage.js
  - src/utils/dateKST.js
autonomous: true
requirements:
  - DAILY-01
  - DAILY-02
  - DAILY-03
requirements_addressed:
  - DAILY-03 (Toss Storage 영속성 — 앱 재시작·새로고침에도 todayDraw 유지)
  - DAILY-02 (KST 자정 비교 헬퍼 — date 불일치 → clear 동작 기반)
  - DAILY-01 (todayDraw 로드/저장 시그니처 — Plan 02 가 useEffect 분기에서 사용)

must_haves:
  truths:
    - "useTodayDrawStorage 훅이 { loading, todayDraw, saveTodayDraw, clearTodayDraw } 시그니처를 노출한다"
    - "todayKST() 헬퍼가 KST(Asia/Seoul) 기준 'YYYY-MM-DD' 문자열을 반환한다"
    - "Storage 미지원 환경에서도 훅이 throw 하지 않고 todayDraw=null + loading=false 로 graceful degradation 한다"
    - "손상된 JSON / date 누락 / card_id 누락 / date 형식 오류 데이터는 자동으로 removeItem 처리되고 null 반환한다"
    - "saveTodayDraw 호출 시 production 에서 실패 시 Sentry.captureException 이 호출되고 UI 는 차단되지 않는다"
  artifacts:
    - path: "src/hooks/useTodayDrawStorage.js"
      provides: "todayDraw 영속 저장 훅 (Toss Storage wrapper)"
      exports: ["useTodayDrawStorage"]
      min_lines: 80
    - path: "src/utils/dateKST.js"
      provides: "KST 기준 날짜 헬퍼"
      exports: ["todayKST"]
      min_lines: 8
  key_links:
    - from: "src/hooks/useTodayDrawStorage.js"
      to: "@apps-in-toss/web-framework Storage"
      via: "Storage.getItem/setItem/removeItem"
      pattern: "Storage\\.(get|set|remove)Item\\("
    - from: "src/hooks/useTodayDrawStorage.js"
      to: "@sentry/react"
      via: "Sentry.captureException"
      pattern: "Sentry\\.captureException"
    - from: "src/hooks/useTodayDrawStorage.js"
      to: "src/utils/dateKST.js"
      via: "todayKST import (검증 시 사용)"
      pattern: "from '../utils/dateKST'"
---

<objective>
Phase 4 의 데이터 레이어 — `useTodayDrawStorage` 훅 + `todayKST()` 날짜 헬퍼 — 를 인터페이스 잠금 형태로 만든다. 이 훅은 Plan 02 의 TarotPage 통합이 사용할 정확한 시그니처를 제공한다.

Purpose: v1.0 `useUserInfoStorage` 패턴을 그대로 미러링해 todayDraw `{ date, card_id }` 를 Toss Storage 에 영속 저장한다. Plan 02 가 import 만 추가하면 즉시 사용 가능한 형태로 잠금.

Output:
- `src/hooks/useTodayDrawStorage.js` (신규 훅)
- `src/utils/dateKST.js` (신규 KST 날짜 헬퍼)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-daily-lock-and-storage/04-CONTEXT.md
@src/hooks/useUserInfoStorage.js
@src/hooks/useAnonymousKey.jsx

<interfaces>
<!-- Plan 02 (TarotPage 통합) 가 사용할 잠금 시그니처. 본 Plan 산출물이 정확히 이 형태여야 한다. -->

From src/hooks/useTodayDrawStorage.js (신규, 본 plan 산출):
```javascript
export function useTodayDrawStorage(): {
  loading: boolean,                                    // 초기 load 진행 중
  todayDraw: { date: string, card_id: number } | null, // 마운트 시 storage 로드 결과 (graceful degradation 시 null)
  saveTodayDraw: (draw: { date: string, card_id: number }) => Promise<boolean>,  // 저장 + state 갱신, 실패 시 false
  clearTodayDraw: () => Promise<boolean>,              // removeItem + state null, 실패 시 false
}
```

From src/utils/dateKST.js (신규, 본 plan 산출):
```javascript
export function todayKST(): string  // 'YYYY-MM-DD' (KST 기준), 예: '2026-05-03'
```

From @apps-in-toss/web-framework (외부 SDK, AIT WebView 내에서만 동작):
```javascript
Storage.getItem(key: string): Promise<string | null>
Storage.setItem(key: string, value: string): Promise<void>
Storage.removeItem(key: string): Promise<void>
```

From v1.0 useUserInfoStorage (정본 패턴 — 시그니처 미러링 대상):
- `loading` boolean (초기 load 동안 true)
- `loadXxx` async fn (mount 시 자동 호출 useEffect, 외부 노출은 본 plan 에선 안 함 — Plan 02 가 자동 로드만 사용)
- `saveXxx` async fn (validate → JSON.stringify → setItem → setState, 실패 시 false 반환)
- `clearXxx` async fn (removeItem → setState null, 실패 시 false 반환)
- `isValidXxx` 내부 헬퍼 (typeof 객체 검증 + 필수 필드 검증, 실패 시 removeItem)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: src/utils/dateKST.js — KST 'YYYY-MM-DD' 헬퍼 작성</name>
  <files>src/utils/dateKST.js</files>
  <read_first>
    - .planning/phases/04-daily-lock-and-storage/04-CONTEXT.md (D-06, D-07, D-08 자정 / KST / setInterval 미사용 결정 확인)
    - src/utils/dataTransform.js (camelCase named export + JSDoc 주석 패턴 확인)
  </read_first>
  <behavior>
    - todayKST() → 'YYYY-MM-DD' 문자열 (예: '2026-05-03')
    - 디바이스 timezone 무관, 항상 Asia/Seoul 기준
    - 호출 시점이 한국 시각 2026-05-03 23:59:59 → '2026-05-03'
    - 호출 시점이 한국 시각 2026-05-04 00:00:01 → '2026-05-04'
    - en-CA locale 강제로 ISO 형식 ('YYYY-MM-DD') 보장 (CONTEXT D-07)
  </behavior>
  <action>
신규 파일 src/utils/dateKST.js 를 작성한다. 정확히 다음 내용으로 시작한다 (JSDoc 포함):

```javascript
/**
 * KST(Asia/Seoul) 기준 오늘 날짜를 'YYYY-MM-DD' 문자열로 반환한다.
 *
 * Phase 4 (CONTEXT D-06, D-07): 모든 사용자 자정 리셋을 한국 자정으로 통일한다.
 * 디바이스 timezone 과 무관. en-CA locale 이 ISO 8601 형식을 보장한다.
 *
 * @returns {string} 'YYYY-MM-DD' (예: '2026-05-03')
 */
export function todayKST() {
  return new Date().toLocaleDateString('en-CA', { timeZone: 'Asia/Seoul' });
}
```

다른 export 추가 금지 (DRY — 본 페이즈에서 필요한 함수 1개만). date-fns/dayjs 같은 외부 의존성 도입 금지 (CONTEXT P1-D-08 carry).
  </action>
  <verify>
    <automated>node -e "const { todayKST } = require('./src/utils/dateKST.js'); const r = todayKST(); if (!/^\d{4}-\d{2}-\d{2}$/.test(r)) { console.error('FAIL: format', r); process.exit(1); } console.log('PASS:', r);" 2>&1 || npx vite-node -e "import { todayKST } from './src/utils/dateKST.js'; const r = todayKST(); if (!/^\d{4}-\d{2}-\d{2}$/.test(r)) { throw new Error('FAIL: ' + r); } console.log('PASS:', r);"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/utils/dateKST.js` exit 0 (파일 존재)
    - `grep -c "export function todayKST" src/utils/dateKST.js` 결과 = 1
    - `grep -c "Asia/Seoul" src/utils/dateKST.js` 결과 = 1
    - `grep -c "en-CA" src/utils/dateKST.js` 결과 = 1
    - `grep -c "toLocaleDateString" src/utils/dateKST.js` 결과 = 1
    - `grep -cE "from\\s+['\"]date-fns['\"]|from\\s+['\"]dayjs['\"]" src/utils/dateKST.js` 결과 = 0 (의존성 0)
    - `grep -cE "^export " src/utils/dateKST.js` 결과 = 1 (todayKST 외 export 없음)
    - `npx eslint src/utils/dateKST.js` 0 errors
    - `wc -l src/utils/dateKST.js` 결과 ≤ 30 줄 (단순 헬퍼)
  </acceptance_criteria>
  <done>todayKST() 가 'YYYY-MM-DD' 문자열을 반환하고 ESLint 통과. 외부 의존성 0.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: src/hooks/useTodayDrawStorage.js — todayDraw 영속 저장 훅</name>
  <files>src/hooks/useTodayDrawStorage.js</files>
  <read_first>
    - src/hooks/useUserInfoStorage.js (정본 패턴 — 시그니처와 try/catch/console.log prefix 컨벤션 미러링 대상)
    - src/hooks/useAnonymousKey.jsx (Storage import + cancelled flag + Sentry 패턴 참고)
    - .planning/phases/04-daily-lock-and-storage/04-CONTEXT.md (D-01~D-05, D-15~D-17 결정 확인)
    - src/utils/dateKST.js (Task 1 산출물 — 검증 함수 isValidTodayDraw 안에서 date 형식 직접 검사 — todayKST() 호출은 본 plan 에서 안 함, Plan 02 가 사용)
  </read_first>
  <behavior>
    - useTodayDrawStorage() 훅 호출 시 mount 시점에 자동으로 Storage.getItem 호출 (loading=true → 결과 → loading=false)
    - 저장된 데이터가 있고 isValidTodayDraw 통과 → todayDraw state 에 객체 세팅 (loading=false)
    - 저장된 데이터가 없거나(null) 손상되었으면 → todayDraw=null, removeItem (손상된 데이터 정리 D-05)
    - Storage.getItem 자체가 throw 하면 (Storage 미지원 환경 D-15) → todayDraw=null, loading=false, throw 안 함
    - saveTodayDraw({ date, card_id }) 호출 시 → JSON.stringify → setItem → setState, 성공 시 true, 실패 시 false 반환 + Sentry 보고 (production)
    - clearTodayDraw() 호출 시 → removeItem → setState null, 성공 시 true, 실패 시 false
    - 모든 console.log/error 는 [TarotStorage] prefix 사용 (v1.0 [Storage] 컨벤션 일관)
  </behavior>
  <action>
신규 파일 src/hooks/useTodayDrawStorage.js 를 작성한다. v1.0 useUserInfoStorage.js 패턴(정본)을 정확히 미러링하되 데이터 구조와 prefix 만 바꾼다. 정확히 다음 내용을 작성한다:

```javascript
import { Storage } from '@apps-in-toss/web-framework';
import { useState, useCallback, useEffect } from 'react';
import * as Sentry from '@sentry/react';

const TODAY_DRAW_STORAGE_KEY = 'FORTUNE_CAT_TAROT_TODAY_DRAW';

// CONTEXT D-05: 손상된 데이터 정리 — JSON 파싱 실패 / date 누락 / card_id 누락 / date 형식 오류 → null 반환 + removeItem.
function isValidTodayDraw(draw) {
  if (!draw || typeof draw !== 'object') return false;
  const { date, card_id } = draw;
  if (typeof date !== 'string') return false;
  if (!/^\d{4}-\d{2}-\d{2}$/.test(date)) return false;
  if (typeof card_id !== 'number') return false;
  if (!Number.isInteger(card_id)) return false;
  if (card_id < 0 || card_id > 21) return false; // 메이저 아르카나 22장 (Phase 3 D-08)
  return true;
}

/**
 * Phase 4 (CONTEXT D-04): todayDraw `{ date, card_id }` 를 Toss Storage 에 영속 저장한다.
 *
 * v1.0 useUserInfoStorage 패턴 미러:
 *   - mount 시 자동 load (loading=true → 결과 → false)
 *   - 손상 데이터는 removeItem 후 null 반환 (D-05)
 *   - Storage 미지원 환경(일반 브라우저, dev 미러)에서는 graceful degradation — todayDraw=null + 차단 없음 (D-15)
 *   - 저장 실패 시 Sentry 보고 + UI 차단 안 함 (D-16, D-17)
 *
 * @returns {{ loading: boolean, todayDraw: object|null, saveTodayDraw: function, clearTodayDraw: function }}
 */
export function useTodayDrawStorage() {
  const [todayDraw, setTodayDraw] = useState(null);
  const [loading, setLoading] = useState(true);

  const loadTodayDraw = useCallback(async () => {
    setLoading(true);
    try {
      const jsonString = await Storage.getItem(TODAY_DRAW_STORAGE_KEY);
      console.log('[TarotStorage] 불러온 데이터:', jsonString);

      if (!jsonString) {
        setTodayDraw(null);
        return null;
      }

      let parsed;
      try {
        parsed = JSON.parse(jsonString);
      } catch (parseErr) {
        console.warn('[TarotStorage] JSON 파싱 실패, 손상 데이터 정리:', parseErr);
        try { await Storage.removeItem(TODAY_DRAW_STORAGE_KEY); } catch (e) { console.error('[TarotStorage] 손상 데이터 삭제 실패:', e); }
        setTodayDraw(null);
        return null;
      }

      if (!isValidTodayDraw(parsed)) {
        console.warn('[TarotStorage] 저장된 데이터가 유효하지 않습니다:', parsed);
        try { await Storage.removeItem(TODAY_DRAW_STORAGE_KEY); } catch (e) { console.error('[TarotStorage] 손상 데이터 삭제 실패:', e); }
        setTodayDraw(null);
        return null;
      }

      console.log('[TarotStorage] 유효한 데이터 로드 성공:', parsed);
      setTodayDraw(parsed);
      return parsed;
    } catch (error) {
      // D-15: Storage 미지원 환경 — graceful degradation. throw 하지 않고 null 반환.
      console.warn('[TarotStorage] todayDraw 로드 실패 (Storage 미지원 가능):', error);
      setTodayDraw(null);
      return null;
    } finally {
      setLoading(false);
    }
  }, []);

  const saveTodayDraw = useCallback(async (draw) => {
    try {
      console.log('[TarotStorage] 저장 시도:', draw);

      if (!isValidTodayDraw(draw)) {
        console.error('[TarotStorage] 유효하지 않은 데이터:', draw);
        throw new Error('유효하지 않은 todayDraw 입니다.');
      }

      const jsonString = JSON.stringify(draw);
      await Storage.setItem(TODAY_DRAW_STORAGE_KEY, jsonString);
      console.log('[TarotStorage] 저장 완료');
      setTodayDraw(draw);
      return true;
    } catch (error) {
      console.error('[TarotStorage] todayDraw 저장 실패:', error);
      // D-16/D-17: production 에서 Sentry 보고. UI 는 차단하지 않음 — 호출자가 false 반환을 무시할 수 있음.
      Sentry.captureException(error, { extra: { phase: 'tarot_storage_save' } });
      return false;
    }
  }, []);

  const clearTodayDraw = useCallback(async () => {
    try {
      await Storage.removeItem(TODAY_DRAW_STORAGE_KEY);
      setTodayDraw(null);
      return true;
    } catch (error) {
      console.error('[TarotStorage] todayDraw 삭제 실패:', error);
      Sentry.captureException(error, { extra: { phase: 'tarot_storage_clear' } });
      return false;
    }
  }, []);

  useEffect(() => {
    loadTodayDraw();
  }, [loadTodayDraw]);

  return {
    loading,
    todayDraw,
    saveTodayDraw,
    clearTodayDraw,
  };
}
```

추가 export 금지. 외부 SDK / 라이브러리 신규 도입 금지 (CONTEXT P1-D-08).
  </action>
  <verify>
    <automated>npx eslint src/hooks/useTodayDrawStorage.js</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/hooks/useTodayDrawStorage.js` exit 0
    - `grep -c "export function useTodayDrawStorage" src/hooks/useTodayDrawStorage.js` = 1
    - `grep -c "FORTUNE_CAT_TAROT_TODAY_DRAW" src/hooks/useTodayDrawStorage.js` = 1 (D-02 키 컨벤션)
    - `grep -c "from '@apps-in-toss/web-framework'" src/hooks/useTodayDrawStorage.js` = 1
    - `grep -c "import \* as Sentry from '@sentry/react'" src/hooks/useTodayDrawStorage.js` = 1 (D-17 Sentry import)
    - `grep -c "Sentry.captureException" src/hooks/useTodayDrawStorage.js` ≥ 1 (saveTodayDraw 실패 시 보고)
    - `grep -c "phase: 'tarot_storage_save'" src/hooks/useTodayDrawStorage.js` = 1 (D-17 extra 메타)
    - `grep -c "function isValidTodayDraw" src/hooks/useTodayDrawStorage.js` = 1 (D-05 검증 함수)
    - `grep -c "Storage.getItem" src/hooks/useTodayDrawStorage.js` = 1
    - `grep -c "Storage.setItem" src/hooks/useTodayDrawStorage.js` = 1
    - `grep -c "Storage.removeItem" src/hooks/useTodayDrawStorage.js` ≥ 2 (load 손상 정리 + clear)
    - `grep -cE "JSON\\.(parse|stringify)" src/hooks/useTodayDrawStorage.js` = 2 (parse 1 + stringify 1)
    - `grep -c "saveTodayDraw" src/hooks/useTodayDrawStorage.js` ≥ 2 (정의 + return)
    - `grep -c "clearTodayDraw" src/hooks/useTodayDrawStorage.js` ≥ 2 (정의 + return)
    - `grep -c "loading" src/hooks/useTodayDrawStorage.js` ≥ 3 (state + setter + return)
    - `grep -c "todayDraw" src/hooks/useTodayDrawStorage.js` ≥ 4 (state + setter + return + 키)
    - `grep -c "useEffect" src/hooks/useTodayDrawStorage.js` ≥ 2 (import + 호출)
    - `grep -c "useCallback" src/hooks/useTodayDrawStorage.js` ≥ 2 (import + 호출 ≥3)
    - `grep -c "\\[TarotStorage\\]" src/hooks/useTodayDrawStorage.js` ≥ 5 (console prefix 컨벤션)
    - `grep -cE "from '(date-fns|dayjs|moment)'" src/hooks/useTodayDrawStorage.js` = 0 (의존성 0)
    - `npx eslint src/hooks/useTodayDrawStorage.js` 0 errors
    - `git diff --name-only` 결과에 `src/hooks/useTodayDrawStorage.js`, `src/utils/dateKST.js` 외 다른 파일 없음 (본 plan 스코프 외 파일 미수정)
  </acceptance_criteria>
  <done>useTodayDrawStorage 훅이 { loading, todayDraw, saveTodayDraw, clearTodayDraw } 시그니처로 export 되고 ESLint 통과. v1.0 useUserInfoStorage 패턴 미러링 완료. Plan 02 가 import 즉시 사용 가능한 상태.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Toss WebView ↔ Storage | AIT WebView 안에서만 Storage API 동작. 일반 브라우저는 throw 또는 undefined 반환 |
| 저장 데이터 ↔ 클라이언트 코드 | localStorage / Toss Storage 는 사용자가 DevTools 로 변조 가능 |
| 본 페이즈 코드 ↔ Sentry | production 에서 에러 메타데이터가 Sentry 로 송신 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Storage 의 todayDraw JSON | mitigate | isValidTodayDraw 가 date 형식(YYYY-MM-DD)·card_id 타입(integer 0~21) 검증, 실패 시 removeItem (D-05) |
| T-04-02 | Information Disclosure | Sentry extra 메타 | mitigate | extra 객체에 phase 키 1개만 포함 (사용자 입력 / PII 무관, P3-T-03-11 패턴 carry) |
| T-04-03 | Denial of Service | Storage 미지원 환경에서 훅이 throw | mitigate | catch 블록이 todayDraw=null + loading=false 로 종결, throw 안 함 (D-15) |
| T-04-04 | Repudiation | 디바이스 시각 변조로 자정 우회 | accept | CONTEXT D-09 — 본 마일스톤은 무시. 서버 시각 검증은 별도 마일스톤 후보 |
| T-04-05 | Elevation of Privilege | 외부 라이브러리 도입으로 의존성 표면 확장 | mitigate | date-fns/dayjs 도입 금지 (P1-D-08 carry), 네이티브 Date + en-CA toLocaleDateString 만 사용. acceptance criteria grep 으로 강제 |
</threat_model>

<verification>
## 통합 검증

```bash
# 두 파일 ESLint 통과
npx eslint src/utils/dateKST.js src/hooks/useTodayDrawStorage.js

# 새 파일만 변경되었는지 확인
git diff --name-only | sort -u | tee /tmp/changed.txt
test "$(cat /tmp/changed.txt)" = "src/hooks/useTodayDrawStorage.js
src/utils/dateKST.js"

# 신규 의존성 도입 없음
git diff --stat package.json package-lock.json pnpm-lock.yaml | grep -E "^\\s*$" || (echo "FAIL: 의존성 변경됨" && exit 1)

# Vite + AIT 빌드 통과 (Plan 02 가 import 하기 전 단독 빌드)
npm run build
```
</verification>

<success_criteria>
- [ ] src/utils/dateKST.js 가 todayKST() 만 export 한다
- [ ] todayKST() 가 KST 'YYYY-MM-DD' 형식을 반환한다
- [ ] src/hooks/useTodayDrawStorage.js 가 { loading, todayDraw, saveTodayDraw, clearTodayDraw } 4종을 반환한다
- [ ] mount 시 자동 load (Storage.getItem) 동작
- [ ] 손상 데이터는 removeItem 후 null 반환
- [ ] Storage 미지원 환경에서 throw 하지 않음 (catch 블록 → todayDraw=null)
- [ ] saveTodayDraw 실패 시 Sentry.captureException 호출 + false 반환 (UI 차단 안 함)
- [ ] 신규 의존성 0 (package.json/lock 변경 없음)
- [ ] ESLint 통과
- [ ] Vite + AIT 빌드 통과
</success_criteria>

<output>
완료 후 `.planning/phases/04-daily-lock-and-storage/04-01-SUMMARY.md` 작성. 잠금 인터페이스 시그니처를 명시해 Plan 02 가 그대로 사용할 수 있도록 한다.
</output>
