---
phase: 04-daily-lock-and-storage
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/pages/TarotPage.jsx
autonomous: true
requirements:
  - DAILY-01
  - DAILY-02
  - DAILY-03
requirements_addressed:
  - DAILY-01 (같은 날 재진입 시 result 직진 — useEffect lock 분기)
  - DAILY-02 (KST 자정 이후 진입 시 intro 재표시 — date 불일치 시 clearTodayDraw)
  - DAILY-03 (앱 재시작·새로고침에도 todayDraw 유지 — Plan 01 storage 훅 통합)

must_haves:
  truths:
    - "사용자가 카드를 뽑은 후 같은 날(KST) 다시 진입하면 intro/shuffle 단계 건너뛰고 즉시 result 화면이 표시된다"
    - "KST 자정(00:00) 이후 진입하면 새 카드를 뽑을 수 있는 intro 단계가 다시 표시된다"
    - "앱을 재시작하거나 페이지를 새로고침해도 그날 뽑은 카드가 유지된다"
    - "저장 데이터 손상 / 미존재 / Storage 미지원 케이스에서도 graceful degradation 으로 intro 화면이 표시된다 (앱 차단 없음)"
    - "intro 화면 CTA 가 todayDraw 존재 여부에 따라 '오늘의 카드 다시 보기 ✨' / '오늘의 카드 뽑기 ✨' 로 분기된다"
    - "intro CTA 가 todayDraw 존재 시 즉시 result 진입, 없을 때 startShuffle 호출로 분기된다"
    - "shuffle 확정 시 saveTodayDraw 가 호출되어 다음 진입에 lock 이 발동된다"
    - "v1.0 사주/딥리딩/부적 흐름은 본 plan 변경에 영향받지 않는다 (NAV-03 회귀 방지)"
  artifacts:
    - path: "src/pages/TarotPage.jsx"
      provides: "intro/shuffle/result 3단계 + daily lock 통합"
      contains: "useTodayDrawStorage"
      min_lines: 250
  key_links:
    - from: "src/pages/TarotPage.jsx"
      to: "src/hooks/useTodayDrawStorage.js"
      via: "import + 훅 호출"
      pattern: "import \\{ useTodayDrawStorage \\} from '../hooks/useTodayDrawStorage'"
    - from: "src/pages/TarotPage.jsx"
      to: "src/utils/dateKST.js"
      via: "import + todayKST() 호출"
      pattern: "import \\{ todayKST \\} from '../utils/dateKST'"
    - from: "TarotPage useEffect"
      to: "loadTodayDraw + 자정 비교 + 분기"
      via: "fetch + storage 통합 effect"
      pattern: "todayDraw\\.date === today"
    - from: "handleSelectCard"
      to: "saveTodayDraw"
      via: "shuffle 확정 시 fire-and-forget 저장"
      pattern: "saveTodayDraw\\("
---

<objective>
Plan 01 의 `useTodayDrawStorage` 훅 + `todayKST()` 헬퍼를 TarotPage 에 통합해 daily lock 동작을 완성한다.

Purpose: Phase 3 의 `intro → shuffle → result` 3단계 상태 머신에 daily lock + 자정 리셋 + 영속 저장을 얹는다. CONTEXT D-10 ~ D-17 잠금 결정을 모두 반영. Phase 3 산출물(상태 7종, handleHome, handleShare, fetch effect) 시그니처는 변경하지 않고 추가만 한다.

Output:
- src/pages/TarotPage.jsx (Phase 3 산출물 확장 — useTodayDrawStorage 통합 + intro CTA 분기 + handleSelectCard 안 saveTodayDraw 호출)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-daily-lock-and-storage/04-CONTEXT.md
@.planning/phases/04-daily-lock-and-storage/04-01-PLAN.md
@.planning/phases/03-daily-one-card-core/03-03-SUMMARY.md
@src/pages/TarotPage.jsx
@src/hooks/useTodayDrawStorage.js
@src/utils/dateKST.js

<interfaces>
<!-- Plan 01 산출물 (Wave 1) — 본 plan 이 import 해서 사용. -->

From src/hooks/useTodayDrawStorage.js (Plan 01):
```javascript
export function useTodayDrawStorage(): {
  loading: boolean,
  todayDraw: { date: string, card_id: number } | null,
  saveTodayDraw: (draw: { date: string, card_id: number }) => Promise<boolean>,
  clearTodayDraw: () => Promise<boolean>,
}
```

From src/utils/dateKST.js (Plan 01):
```javascript
export function todayKST(): string  // 'YYYY-MM-DD' (KST)
```

<!-- Phase 3 산출물 (Wave 0 carry-forward) — 본 plan 이 변경하지 않고 그대로 유지. -->

From src/pages/TarotPage.jsx (Phase 3 03-03-SUMMARY 잠금):
- useState 7종: currentPage, cardsData, shuffledThree, selectedCardId, isLoading, errorState, retryNonce
- pickThreeRandom(cards) — Fisher-Yates + slice(0,3)
- 함수 시그니처 잠금:
  - startShuffle() — pickThreeRandom 호출 + setCurrentPage('shuffle')
  - handleSelectCard(id) — setSelectedCardId(id) + setCurrentPage('result')  [본 plan 이 saveTodayDraw 호출 추가]
  - handleHome() — setSelectedCardId(null) + setShuffledThree([]) + setCurrentPage('intro')
  - handleShare() — Phase 5 stub
  - handleRetry() — setRetryNonce(n => n + 1)
- TarotIntro 컴포넌트 (동일 파일 내 inline) — 현재 props { onStart } 만 받음. 본 plan 이 { hasTodayDraw, onStart, onResume } 로 확장.
- intro fetch useEffect — deps [retryNonce] + cancelled flag + Sentry.captureException
- 로딩 화면 (Loader + "카드를 준비하고 있어요")
- 에러 화면 ("카드 데이터를 불러오지 못했어요" + 재시도 버튼)

<!-- Phase 3 잠금 (변경 금지) — 본 plan 이 손대면 안 되는 항목. -->
- TarotShuffle / TarotResult / TarotCardArt / Boknyang 컴포넌트 — 본 plan 에서 미수정
- v1.0 페이지 (HomePage, SajuPage, AmuletPage, NewYearPage) — 본 plan 에서 미수정
- granite.config.ts / package.json / lockfile — 본 plan 에서 미수정
- v1.0 컴포넌트 (TabBar, Loading, BirthdateInput 등) — 본 plan 에서 미수정
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: TarotPage.jsx — useTodayDrawStorage 통합 + 자정 비교 + 분기</name>
  <files>src/pages/TarotPage.jsx</files>
  <read_first>
    - src/pages/TarotPage.jsx (Phase 3 산출물 현재 상태 — useState 7종, useEffect, startShuffle, handleSelectCard, handleHome, handleShare, handleRetry, TarotIntro 함수 본체 라인 192~274 모두)
    - src/hooks/useTodayDrawStorage.js (Plan 01 산출물 — 시그니처 정확히 확인)
    - src/utils/dateKST.js (Plan 01 산출물 — todayKST 시그니처 확인)
    - .planning/phases/04-daily-lock-and-storage/04-CONTEXT.md (D-10 ~ D-17 결정 — 진입 흐름 / shuffle 저장 / handleHome / intro CTA / graceful degradation 모두 정확 명세)
    - .planning/phases/03-daily-one-card-core/03-03-SUMMARY.md (Phase 3 잠금 시그니처 — handleHome / handleShare / handleRedraw 호출 패턴)
  </read_first>
  <behavior>
    - mount 시 fetch + storage load 동시 진행 (Phase 3 의 retryNonce useEffect 안에서 storage 로드도 처리)
    - storage load 완료 후, todayDraw 존재하고 date === todayKST() → setSelectedCardId(todayDraw.card_id) + setCurrentPage('result') (intro 건너뜀)
    - todayDraw 존재하고 date !== todayKST() → clearTodayDraw() 호출 (자정 지남), setCurrentPage('intro') 유지
    - todayDraw null → setCurrentPage('intro') 유지 (기본)
    - storage 훅의 loading 이 true 인 동안에도 cardsData fetch 는 병렬 진행 (Promise.all 가능)
    - intro CTA 라벨: hasTodayDraw → '오늘의 카드 다시 보기 ✨', 없을 때 '오늘의 카드 뽑기 ✨'
    - intro CTA onClick: hasTodayDraw → setCurrentPage('result') (selectedCardId 이미 세팅됨), 없을 때 startShuffle()
    - handleSelectCard(id): setSelectedCardId(id) → saveTodayDraw({ date: todayKST(), card_id: id }) (fire-and-forget, await 하지 않음 — D-11) → setCurrentPage('result')
    - handleHome 동작 변경 없음 — 같은 세션 내에서 다시 intro 로 돌아갈 수 있되, 새로고침 시 lock 발동 (D-12 트레이드오프)
    - Storage 미지원 환경 → todayDraw 항상 null → 기본 intro 흐름 (Phase 3 동작 그대로 유지) (D-15)
    - 저장 실패 (saveTodayDraw 가 false 반환) → result 화면은 그대로 표시 (메모리 fallback). 로그만 남기고 UI 차단 안 함 (D-16)
  </behavior>
  <action>
src/pages/TarotPage.jsx 를 Phase 3 산출물에서 다음 변경만 추가한다 (변경 외 기존 코드 라인 절대 수정 금지 — Phase 3 잠금):

**1. import 추가 (기존 import 블록 하단에 추가, 라인 12~20 사이):**

```javascript
import { useTodayDrawStorage } from '../hooks/useTodayDrawStorage';
import { todayKST } from '../utils/dateKST';
```

**2. TarotPage 컴포넌트 본체 — 7종 useState 직후 (라인 41 다음) 에 훅 호출 추가:**

```javascript
  const { loading: storageLoading, todayDraw, saveTodayDraw, clearTodayDraw } = useTodayDrawStorage();
```

**3. 기존 fetch useEffect (라인 44~70) 의 setCardsData 직후, prefetchAllCardImages 호출 직전에 daily lock 분기 추가.**

기존:
```javascript
        setCardsData(data);
        // Pitfall 2 회피: 22장 webp prefetch (intro 단계 사용자 헤드라인 읽는 시간 동안 다운로드)
        prefetchAllCardImages();
```

변경 후:
```javascript
        setCardsData(data);
        // Pitfall 2 회피: 22장 webp prefetch (intro 단계 사용자 헤드라인 읽는 시간 동안 다운로드)
        prefetchAllCardImages();
```

(이 부분은 변경 없음 — fetch effect 안에서 todayDraw 분기를 하지 않는 이유: useTodayDrawStorage 가 자체 useEffect 로 비동기 로드 → 별도 useEffect 가 todayDraw 변화를 listen 해야 race condition 회피)

**4. fetch useEffect 직후, daily lock 분기 useEffect 신설 (라인 70 다음):**

```javascript
  // Phase 4 (CONTEXT D-10): storage 로드 완료 + cardsData 준비 → todayDraw 와 KST 자정 비교 후 분기.
  // - todayDraw 존재 + date 일치 → result 직진 (intro/shuffle 건너뜀)
  // - todayDraw 존재 + date 불일치 (자정 지남) → clearTodayDraw + intro 유지
  // - todayDraw 없음 → intro 유지 (기본)
  // 의존: storageLoading 종료 + cardsData 준비. cardsData 길이 검증은 fetch effect 가 errorState 로 처리.
  useEffect(() => {
    if (storageLoading) return;          // storage 로드 진행 중
    if (errorState) return;              // fetch 에러 화면 표시 중
    if (!cardsData || cardsData.length === 0) return; // fetch 미완료
    if (!todayDraw) return;              // 저장된 카드 없음 → 기본 intro

    const today = todayKST();
    if (todayDraw.date === today) {
      // D-10 케이스 1: 같은 날 재진입 → result 직진
      const card = cardsData.find((c) => c.id === todayDraw.card_id);
      if (card) {
        setSelectedCardId(todayDraw.card_id);
        setCurrentPage('result');
      } else {
        // 저장된 card_id 가 22장 안에 없음 (시드 변경 또는 손상) → 정리 후 intro
        console.warn('[TarotPage] todayDraw.card_id 가 cardsData 에 없음, 정리:', todayDraw.card_id);
        clearTodayDraw();
      }
    } else {
      // D-10 케이스 2: 자정 지남 → clear 후 intro
      console.log('[TarotPage] KST 자정 지남, todayDraw 정리:', todayDraw.date, '->', today);
      clearTodayDraw();
    }
  }, [storageLoading, errorState, cardsData, todayDraw, clearTodayDraw]);
```

**5. handleSelectCard 함수 본체에 saveTodayDraw 호출 추가 (CONTEXT D-11):**

기존 (라인 79~82):
```javascript
  const handleSelectCard = (id) => {
    setSelectedCardId(id);
    setCurrentPage('result');
  };
```

변경 후:
```javascript
  const handleSelectCard = (id) => {
    setSelectedCardId(id);
    // CONTEXT D-11: shuffle 확정 시 todayDraw 영속 저장 (fire-and-forget — await 하지 않음).
    // 저장 실패해도 result 화면은 메모리 기반으로 표시됨 (D-16 graceful).
    saveTodayDraw({ date: todayKST(), card_id: id });
    setCurrentPage('result');
  };
```

**6. 로딩 화면 게이팅에 storageLoading 추가 (라인 106):**

기존:
```javascript
  if (isLoading) {
```

변경 후:
```javascript
  // storage 로드 + cardsData fetch 둘 다 완료해야 분기 가능 — 둘 중 하나라도 진행 중이면 Loader 노출.
  if (isLoading || storageLoading) {
```

**7. TarotIntro 호출에 hasTodayDraw + onResume props 추가 (라인 178~180):**

기존:
```javascript
      {currentPage === 'intro' && (
        <TarotIntro onStart={startShuffle} />
      )}
```

변경 후:
```javascript
      {currentPage === 'intro' && (
        <TarotIntro
          hasTodayDraw={Boolean(todayDraw && todayDraw.date === todayKST())}
          onStart={startShuffle}
          onResume={() => {
            // CONTEXT D-14: todayDraw 존재 시 result 진입. selectedCardId 는 lock useEffect 가 이미 세팅했지만
            // 사용자가 result 에서 처음으로(handleHome) 누른 뒤 다시 intro 진입한 케이스를 위해 명시적으로 보강.
            if (todayDraw) {
              setSelectedCardId(todayDraw.card_id);
              setCurrentPage('result');
            }
          }}
        />
      )}
```

**8. TarotIntro 컴포넌트 시그니처 + CTA 분기 변경 (라인 193~274):**

기존:
```javascript
function TarotIntro({ onStart }) {
```

변경 후:
```javascript
function TarotIntro({ hasTodayDraw, onStart, onResume }) {
```

기존 CTA 버튼 (라인 244~267):
```javascript
        <button
          type="button"
          onClick={onStart}
          className="tap-card"
          style={{
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
            gap: 8,
            width: '100%',
            minHeight: 56,
            padding: '16px 24px',
            fontSize: 16,
            fontWeight: 700,
            color: '#FFFFFF',
            backgroundImage: 'linear-gradient(135deg, #A78BFA 0%, #7C3AED 100%)',
            border: 0,
            borderRadius: 24,
            boxShadow: '0 6px 18px rgba(124, 58, 237, 0.45), inset 0 1px 0 rgba(255, 255, 255, 0.4)',
            cursor: 'pointer',
          }}
        >
          오늘의 카드 뽑기 ✨
        </button>
```

변경 후 (CONTEXT D-13/D-14: hasTodayDraw 분기로 라벨 + onClick 분기):
```javascript
        <button
          type="button"
          onClick={hasTodayDraw ? onResume : onStart}
          className="tap-card"
          style={{
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
            gap: 8,
            width: '100%',
            minHeight: 56,
            padding: '16px 24px',
            fontSize: 16,
            fontWeight: 700,
            color: '#FFFFFF',
            backgroundImage: 'linear-gradient(135deg, #A78BFA 0%, #7C3AED 100%)',
            border: 0,
            borderRadius: 24,
            boxShadow: '0 6px 18px rgba(124, 58, 237, 0.45), inset 0 1px 0 rgba(255, 255, 255, 0.4)',
            cursor: 'pointer',
          }}
        >
          {hasTodayDraw ? '오늘의 카드 다시 보기 ✨' : '오늘의 카드 뽑기 ✨'}
        </button>
```

**9. 본 plan 이 변경하지 않는 항목 (Phase 3 잠금):**
- pickThreeRandom 함수
- useState 7종 선언 라인
- startShuffle / handleHome / handleShare / handleRetry 함수
- 로딩/에러 화면의 JSX (storageLoading 조건만 추가하고 그 외는 변경 없음)
- TarotShuffle / TarotResult / Boknyang 호출 부분
- TarotIntro 의 레이아웃, 마스코트, 헤드라인 텍스트, 자정 안내 카피 — CTA 버튼만 변경
- 다른 파일은 절대 수정하지 않음

ESLint 위반이 발생하면 `// eslint-disable-next-line` 한 줄로 회피하지 말고 코드를 정리한다 (필요 시 useEffect deps 에서 setSelectedCardId/setCurrentPage 같은 setter 는 React 가 stable identity 보장하므로 deps 추가 불필요).
  </action>
  <verify>
    <automated>npx eslint src/pages/TarotPage.jsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import { useTodayDrawStorage } from '../hooks/useTodayDrawStorage'" src/pages/TarotPage.jsx` = 1 (Plan 01 훅 import)
    - `grep -c "import { todayKST } from '../utils/dateKST'" src/pages/TarotPage.jsx` = 1 (Plan 01 헬퍼 import)
    - `grep -c "useTodayDrawStorage()" src/pages/TarotPage.jsx` = 1 (훅 호출)
    - `grep -c "loading: storageLoading" src/pages/TarotPage.jsx` = 1 (rename to avoid 충돌)
    - `grep -c "todayDraw," src/pages/TarotPage.jsx` ≥ 1 (구조 분해)
    - `grep -c "saveTodayDraw" src/pages/TarotPage.jsx` ≥ 2 (구조 분해 + 호출)
    - `grep -c "clearTodayDraw" src/pages/TarotPage.jsx` ≥ 3 (구조 분해 + lock effect 2회 호출)
    - `grep -cE "todayKST\\(\\)" src/pages/TarotPage.jsx` ≥ 3 (lock effect 비교 + handleSelectCard 저장 + TarotIntro hasTodayDraw)
    - `grep -c "todayDraw.date === today" src/pages/TarotPage.jsx` = 1 (D-10 케이스 1 lock 비교)
    - `grep -c "saveTodayDraw({ date: todayKST(), card_id: id })" src/pages/TarotPage.jsx` = 1 (D-11 shuffle 확정 시 저장)
    - `grep -c "isLoading || storageLoading" src/pages/TarotPage.jsx` = 1 (D-15 로딩 게이팅)
    - `grep -c "hasTodayDraw" src/pages/TarotPage.jsx` ≥ 4 (TarotPage props 전달 + TarotIntro 시그니처 + CTA onClick 분기 + CTA 라벨 분기)
    - `grep -c "onResume" src/pages/TarotPage.jsx` ≥ 3 (TarotPage 콜백 정의 + TarotIntro 시그니처 + CTA onClick)
    - `grep -c "오늘의 카드 다시 보기 ✨" src/pages/TarotPage.jsx` = 1 (D-13 todayDraw 존재 라벨)
    - `grep -c "오늘의 카드 뽑기 ✨" src/pages/TarotPage.jsx` = 1 (D-13 기본 라벨, 기존 1개 유지)
    - `grep -c "function TarotIntro" src/pages/TarotPage.jsx` = 1 (Phase 3 인라인 컴포넌트 유지)
    - `grep -c "function TarotIntro({ hasTodayDraw, onStart, onResume })" src/pages/TarotPage.jsx` = 1 (시그니처 변경)
    - `grep -c "useEffect" src/pages/TarotPage.jsx` ≥ 2 (Phase 3 fetch effect 유지 + 본 plan lock effect 신설)
    - `grep -c "function pickThreeRandom" src/pages/TarotPage.jsx` = 1 (Phase 3 함수 유지)
    - `grep -c "const handleSelectCard" src/pages/TarotPage.jsx` = 1
    - `grep -c "const handleHome" src/pages/TarotPage.jsx` = 1 (Phase 3 시그니처 잠금 유지)
    - `grep -c "const handleShare" src/pages/TarotPage.jsx` = 1 (Phase 5 stub 유지)
    - `grep -c "const handleRetry" src/pages/TarotPage.jsx` = 1
    - `grep -c "const startShuffle" src/pages/TarotPage.jsx` = 1
    - `grep -c "const \\[currentPage" src/pages/TarotPage.jsx` = 1
    - `grep -c "const \\[cardsData" src/pages/TarotPage.jsx` = 1
    - `grep -c "const \\[shuffledThree" src/pages/TarotPage.jsx` = 1
    - `grep -c "const \\[selectedCardId" src/pages/TarotPage.jsx` = 1
    - `grep -c "const \\[isLoading" src/pages/TarotPage.jsx` = 1
    - `grep -c "const \\[errorState" src/pages/TarotPage.jsx` = 1
    - `grep -c "const \\[retryNonce" src/pages/TarotPage.jsx` = 1
    - `grep -cE "fontWeight: (500\|600\|800\|900)" src/pages/TarotPage.jsx` = 0 (UI-SPEC 4 — 400/700 만)
    - `grep -cE "(framer-motion\|zustand)" src/pages/TarotPage.jsx` = 0 (의존성 0 P1-D-08)
    - `git diff --name-only` 결과 = `src/pages/TarotPage.jsx` 1개 (다른 파일 미수정)
    - `git diff --stat granite.config.ts package.json package-lock.json pnpm-lock.yaml` 빈 출력 (인프라 미변경)
    - `npx eslint src/pages/TarotPage.jsx` 0 errors
    - `npm run build` exit 0 (Vite + AIT 빌드 통과, fortune-cat.ait 생성)
  </acceptance_criteria>
  <done>
    TarotPage 가 useTodayDrawStorage + todayKST 를 통합한다. mount 시 todayDraw 로드 + 자정 비교 + 분기. handleSelectCard 안에서 saveTodayDraw 호출. intro CTA 가 hasTodayDraw 에 따라 라벨 + 동작 분기. Phase 3 잠금 시그니처(handleHome, handleShare, useState 7종, pickThreeRandom, TarotIntro 인라인) 모두 유지. ESLint + build 통과.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| storage 데이터 ↔ TarotPage 분기 로직 | useTodayDrawStorage 가 isValidTodayDraw 통과한 데이터만 노출. 본 plan 의 추가 검증은 card_id 가 cardsData 에 존재하는지 |
| handleSelectCard ↔ saveTodayDraw | 사용자 입력(카드 탭) → todayKST() + 카드 id → JSON 저장. PII 무관 |
| intro CTA 분기 ↔ hasTodayDraw 계산 | 매 렌더 시 todayDraw && date === todayKST() 재계산. memoization 미사용 (값 1개 비교 — 비용 무시 가능) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Tampering | storage 의 card_id 가 22장 범위 밖 | mitigate | lock useEffect 의 `cardsData.find((c) => c.id === todayDraw.card_id)` 가 undefined 반환 시 clearTodayDraw 호출 (시드 변경 / 손상 케이스) |
| T-04-07 | Race Condition | storage load 와 cardsData fetch 가 비동기 + 별도 useEffect | mitigate | lock useEffect 의 의존 가드 (storageLoading + errorState + cardsData.length 검증) → 양쪽 모두 준비된 후에만 분기 실행 |
| T-04-08 | Information Disclosure | console.log 의 todayDraw 객체 노출 | accept | { date, card_id } 둘 다 PII 무관, 디버그 로그 패턴 v1.0 일관 (production minify 가 console 제거) |
| T-04-09 | Denial of Service | saveTodayDraw 가 무한 await 으로 result 진입 차단 | mitigate | fire-and-forget 패턴 (await 없이 호출) — saveTodayDraw 가 throw 해도 setCurrentPage('result') 가 동기 실행 |
| T-04-10 | Repudiation | 사용자가 같은 세션 내 handleHome → CTA 다시 누르면 새 카드 뽑힐지 / 같은 카드 보일지 모호 | accept | CONTEXT D-12 명시 트레이드오프. 새로고침 시 lock 발동(todayDraw 존재 + date 일치 → result 직진). v1.2 에서 일관성 강화 후보 |
| T-04-11 | Tampering | 디바이스 시각 변조로 자정 우회 | accept | CONTEXT D-09 — 본 마일스톤 무시. 서버 시각 검증 별도 마일스톤 |
| T-04-12 | Elevation of Privilege | TarotIntro 의 onResume 콜백이 외부에서 잘못 호출되어 lock state 우회 | mitigate | TarotIntro 는 동일 파일 인라인 컴포넌트 — 외부 노출 없음. onResume 내부에서 todayDraw null 체크 |
</threat_model>

<verification>
## 통합 검증

```bash
# ESLint 통과
npx eslint src/pages/TarotPage.jsx

# Vite + AIT 빌드 통과
npm run build

# 본 plan 외 파일 미수정 확인
git diff --name-only
# 출력은 src/pages/TarotPage.jsx 1개여야 함

# Phase 3 잠금 시그니처 grep 일괄 확인
grep -c "function pickThreeRandom\|const handleHome\|const handleShare\|const handleRetry\|const startShuffle\|const handleSelectCard" src/pages/TarotPage.jsx
# 6개 모두 1회씩 매치 = 6
```

## 수동 검증 시나리오 (Plan 02 완료 후 사용자가 디바이스에서 확인)

본 plan 의 acceptance criteria 는 grep + ESLint + build 까지 자동 검증한다. 실제 동작 검증은 사용자가 다음 시나리오를 수행한다:

1. **DAILY-01 같은 날 재진입:** 카드 뽑은 후 앱 종료 → 다시 진입 → result 직진 (intro 보이면 안 됨)
2. **DAILY-03 새로고침 영속:** 카드 뽑은 후 새로고침 → result 직진
3. **DAILY-02 자정 리셋:** 디바이스 날짜를 다음날로 변경 → 다시 진입 → intro 표시 + CTA "오늘의 카드 뽑기 ✨"
4. **D-13 intro CTA 분기:** result 에서 처음으로 → intro CTA 라벨이 "오늘의 카드 다시 보기 ✨" 로 표시
5. **D-14 intro Resume 동작:** 위 상태에서 CTA 탭 → result 직진 (shuffle 재진입 안 됨)
6. **D-15 graceful degradation:** 일반 브라우저 (npm run dev) 에서 진입 → throw 없이 intro 정상 노출, 카드 뽑기 → result 정상 (lock 안 됨, 메모리 기반)
7. **NAV-03 v1.0 회귀 방지:** 사주/딥리딩/부적/신년 흐름 정상 동작 확인
</verification>

<success_criteria>
- [ ] 같은 날 재진입 시 result 직진 (DAILY-01)
- [ ] KST 자정 후 진입 시 intro 재표시 (DAILY-02)
- [ ] 새로고침 후에도 todayDraw 유지 (DAILY-03)
- [ ] Storage 미지원 환경 graceful degradation (intro 정상 노출, 카드 뽑기 가능)
- [ ] intro CTA 라벨 hasTodayDraw 분기 정확
- [ ] handleSelectCard 안에 saveTodayDraw 호출 추가됨
- [ ] Phase 3 잠금 시그니처 모두 유지 (pickThreeRandom, handleHome, handleShare, handleRetry, startShuffle, useState 7종, TarotIntro 인라인)
- [ ] 본 plan 외 파일 미수정 (TarotShuffle/TarotResult/TarotCardArt/Boknyang/v1.0 페이지/granite/lockfile 모두 0 변경)
- [ ] ESLint 0 errors
- [ ] Vite + AIT 빌드 통과 (fortune-cat.ait 생성)
- [ ] 의존성 0 (date-fns/dayjs/framer-motion/zustand 미도입)
</success_criteria>

<output>
완료 후 `.planning/phases/04-daily-lock-and-storage/04-02-SUMMARY.md` 작성. Phase 5 (SHARE-01, ANL-01) 가 사용할 lock state (todayDraw, hasTodayDraw, already_drawn 플래그 후보) 잠금 명시. Phase 3 / Phase 4 carry-forward 정리.
</output>
