---
phase: 01-tarot-prototype-evaluation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-tarot-prototype-evaluation/01-INVENTORY.md
  - .planning/phases/01-tarot-prototype-evaluation/01-GAPS.md
autonomous: true
requirements: []
tags: [tarot, evaluation, porting, inventory]

must_haves:
  truths:
    - "사용자(개발자)가 boknyang-tarot 프로토타입의 핵심 자산(카드 이미지·해석 데이터·인터랙션 코드) 위치를 INVENTORY.md에서 표 형태로 확인할 수 있다 (Success Criteria 1)"
    - "사용자가 v1.1 포팅 대상(데일리 원카드: index/shuffle/result + TarotCardArt + cards.ts + 22장 이미지)과 제외 대상(archive/RatingWidget/Particles/Boknyang/주제별·스프레드·유료)을 INVENTORY.md에서 In-scope·Reviewed but excluded 두 영역으로 명확히 구분해 검토할 수 있다 (Success Criteria 2, D-03·D-04)"
    - "사용자가 fortune-cat의 디자인 시스템·의존성·라우팅과 충돌하는 항목을 GAPS.md에서 5종(Tailwind→Emotion, zustand→Context, Radix→TDS, TS→JS, TanStack→React Router)+framer-motion 미결정으로 강도 표시(🔴/🟡/🟢)와 함께 확인할 수 있다 (Success Criteria 3, D-08·D-09)"
    - "사용자가 카드 이미지 보관 위치(`src/assets/images/cards/` 정적 임포트)와 해석 텍스트 보관 위치(Supabase `tarot_cards` 테이블) 결정이 INVENTORY.md '대응 fortune-cat 위치' 컬럼에 반영된 상태로 확인할 수 있다 (Success Criteria 4, D-06·D-07)"
  artifacts:
    - path: ".planning/phases/01-tarot-prototype-evaluation/01-INVENTORY.md"
      provides: "boknyang-tarot 자산 인벤토리 — 22장 카드 이미지·라우트·컴포넌트·데이터·라이브러리 별 In-scope/Out-of-scope 표 + 대응 fortune-cat 위치"
      contains: "## 자산 인벤토리"
      min_lines: 80
    - path: ".planning/phases/01-tarot-prototype-evaluation/01-GAPS.md"
      provides: "포팅 갭 매트릭스 — 5종 의존성 충돌 + framer-motion 미결정 + 라우팅·언어 충돌의 강도·해결 전략"
      contains: "## 갭 매트릭스"
      min_lines: 40
  key_links:
    - from: "01-INVENTORY.md '대응 fortune-cat 위치' 컬럼"
      to: "src/assets/images/cards/, src/pages/TarotPage.jsx, src/lib/supabase.js (tarot_cards 조회)"
      via: "표 행별 명시"
      pattern: "src/assets/images/cards|src/pages/TarotPage|tarot_cards"
    - from: "01-GAPS.md 행"
      to: "CONTEXT.md D-08·D-09·D-10"
      via: "관련 D-XX ID 컬럼"
      pattern: "D-08|D-09|D-10"
    - from: "01-INVENTORY.md Reviewed but excluded 섹션"
      to: "CONTEXT.md D-04 명시 항목 (archive/RatingWidget/Particles/Boknyang)"
      via: "직접 항목명 매칭"
      pattern: "archive|RatingWidget|Particles|Boknyang"
---

<objective>
Phase 1의 두 산출물 — **자산 인벤토리(`01-INVENTORY.md`)** 와 **갭 매트릭스(`01-GAPS.md`)** — 를 작성한다. 산출물은 Phase 2~5 플래너/실행자가 외부 프로토타입(`boknyang-tarot`)을 fortune-cat으로 포팅할 때 참조하는 단일 진실 소스가 된다.

Purpose: Phase 1의 4개 Success Criteria를 모두 충족시키고, 후속 페이즈가 자산 위치·포팅 여부·재구현 전략을 grep/참조 가능한 markdown 표로 즉시 활용할 수 있게 한다.

Output:
1. `.planning/phases/01-tarot-prototype-evaluation/01-INVENTORY.md` — 22장 카드 + 라우트/컴포넌트/데이터/라이브러리 In-scope·Out-of-scope 표 + Reviewed but excluded 섹션
2. `.planning/phases/01-tarot-prototype-evaluation/01-GAPS.md` — 의존성/스택/라우팅/언어 충돌의 강도·해결 전략 매트릭스
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-tarot-prototype-evaluation/01-CONTEXT.md

<!-- 이 플랜은 코드 변경이 없는 평가/문서화 페이즈입니다. -->
<!-- 위 4개 문서는 결정의 근거(D-01~D-10)와 Success Criteria 출처입니다 — task 실행 전 반드시 읽으세요. -->

<!-- 외부 프로토타입 실 파일 위치 (Read 도구로 직접 접근) -->
- 프로토타입 루트: `/Users/trinity/Projects/toss-app-generator/workspace/boknyang-tarot/boknyang-tarot/`
- `package.json` — 의존성 목록 (갭 매트릭스 입력)
- `src/data/cards.ts` — 메이저 아르카나 22장 데이터 (이름/이모지/키워드/메시지)
- `src/routes/{__root,index,shuffle,result,archive}.tsx` — 라우트 5개 (archive는 D-03·D-04에 의해 Reviewed but excluded)
- `src/components/{TarotCardArt,BottomNav,Boknyang,RatingWidget,Particles}.tsx` — 5개 컴포넌트 (in/out 분류 D-03·D-04)
- `src/store/tarot.ts` — zustand 스토어 (D-08에 의해 Context+useState로 재구현 대상)
- `src/lib/{utils,ratings,session,track}.ts` — 유틸/추적 (in/out 분류 D-03·D-04)
- `public/cards/00.webp ~ 21.webp` — 카드 이미지 22장 (실제 형식: **WebP**)
- `granite.config.ts` — AIT 빌드 설정 비교용

<!-- fortune-cat 측 통합 지점 (포팅 시 대응 위치 — INVENTORY 작성 시 참조) -->
- `src/App.jsx` — 라우터 + Provider 래핑 (탭바 통합은 Phase 2에서 결정)
- `src/pages/SajuPage.jsx`, `src/pages/AmuletPage.jsx` — `currentPage` 상태 머신 패턴 (D-10 참조)
- `src/lib/supabase.js` — Supabase 클라이언트 + 메뉴 테이블 조회 (`tarot_cards` 동일 패턴, D-07)
- `src/assets/images/` — 정적 일러스트 임포트 패턴 (`src/assets/images/cards/` 신설, D-06)
- `package.json` — fortune-cat 측 의존성 (갭 매트릭스 비교)
</context>

<tasks>

<task type="auto">
  <name>Task 1: boknyang-tarot 자산 인벤토리 작성 (01-INVENTORY.md)</name>
  <files>.planning/phases/01-tarot-prototype-evaluation/01-INVENTORY.md</files>

  <read_first>
    - `.planning/phases/01-tarot-prototype-evaluation/01-CONTEXT.md` (D-01~D-10 잠금 결정, 특히 D-03/D-04/D-06/D-07/D-10)
    - `/Users/trinity/Projects/toss-app-generator/workspace/boknyang-tarot/boknyang-tarot/src/data/cards.ts` (22장 카드 데이터 구조 확인)
    - `/Users/trinity/Projects/toss-app-generator/workspace/boknyang-tarot/boknyang-tarot/src/routes/index.tsx`
    - `/Users/trinity/Projects/toss-app-generator/workspace/boknyang-tarot/boknyang-tarot/src/routes/shuffle.tsx`
    - `/Users/trinity/Projects/toss-app-generator/workspace/boknyang-tarot/boknyang-tarot/src/routes/result.tsx`
    - `/Users/trinity/Projects/toss-app-generator/workspace/boknyang-tarot/boknyang-tarot/src/routes/archive.tsx` (Out-of-scope 사유 기록용)
    - `/Users/trinity/Projects/toss-app-generator/workspace/boknyang-tarot/boknyang-tarot/src/components/TarotCardArt.tsx`
    - `/Users/trinity/Projects/toss-app-generator/workspace/boknyang-tarot/boknyang-tarot/src/components/BottomNav.tsx`
    - `/Users/trinity/Projects/toss-app-generator/workspace/boknyang-tarot/boknyang-tarot/src/components/{Boknyang,RatingWidget,Particles}.tsx` (Out-of-scope 사유 기록용)
    - `/Users/trinity/Projects/toss-app-generator/workspace/boknyang-tarot/boknyang-tarot/src/store/tarot.ts`
    - `/Users/trinity/Projects/toss-app-generator/workspace/boknyang-tarot/boknyang-tarot/src/lib/utils.ts`
    - `/Users/trinity/Projects/toss-app-generator/workspace/boknyang-tarot/boknyang-tarot/src/lib/{ratings,session,track}.ts` (Out-of-scope 사유 기록용)
    - `ls /Users/trinity/Projects/toss-app-generator/workspace/boknyang-tarot/boknyang-tarot/public/cards/` (22장 파일명 정확 확인 — **실제 형식이 .webp임을 확인하고 인벤토리에 반영**)
    - `/Users/trinity/Projects/app-in-toss/fortune-cat/src/pages/SajuPage.jsx` (currentPage 상태 머신 — 대응 위치 컬럼 작성용 참조)
    - `/Users/trinity/Projects/app-in-toss/fortune-cat/src/lib/supabase.js` (메뉴 테이블 조회 패턴 — `tarot_cards` 대응 위치 작성용)
  </read_first>

  <action>
한국어 markdown 파일 `.planning/phases/01-tarot-prototype-evaluation/01-INVENTORY.md` 를 작성한다. 다음 구조와 컬럼·헤딩 텍스트를 **그대로** 사용한다.

### 파일 헤더 (frontmatter 없이 markdown 본문만)

```
# Phase 1: boknyang-tarot 자산 인벤토리

**Created:** 2026-04-30
**Source prototype:** `/Users/trinity/Projects/toss-app-generator/workspace/boknyang-tarot/boknyang-tarot/`
**Decisions referenced:** D-01, D-03, D-04, D-06, D-07, D-10 (see `01-CONTEXT.md`)
**Downstream consumers:** Phase 2 (탭바), Phase 3 (데일리 원카드 코어), Phase 5 (Analytics·공유)
```

### Section 1 — `## 자산 인벤토리` (필수 H2)

다음 5개 H3 서브섹션을 이 순서로 작성:

1. `### 라우트 (Routes)` — 표 컬럼: `파일 경로` / `역할` / `In/Out-of-scope` / `대응 fortune-cat 위치` / `관련 D-XX` / `비고`
   - 행 5개 (`__root.tsx`, `index.tsx`, `shuffle.tsx`, `result.tsx`, `archive.tsx`)
   - In-scope 라우트(`index`, `shuffle`, `result`)의 "대응 fortune-cat 위치" = `src/pages/TarotPage.jsx` 내부 `currentPage` 상태(`intro` → `shuffle` → `result`) — **D-10 명시**
   - `archive.tsx` = Out-of-scope, "사유: 데일리 원카드 1종 한정 정책 (D-04)"
   - `__root.tsx` = TanStack Router 전용, fortune-cat은 React Router DOM이므로 직접 포팅 안 함, "대응 위치: `src/App.jsx` 라우터 구조 + Phase 2 탭바 셸"

2. `### 컴포넌트 (Components)` — 동일 컬럼
   - 행 5개 (`TarotCardArt.tsx`, `BottomNav.tsx`, `Boknyang.tsx`, `RatingWidget.tsx`, `Particles.tsx`)
   - `TarotCardArt.tsx` = In-scope (핵심), 대응 위치 = `src/components/TarotCardArt.jsx` (TS→JS, D-08)
   - `BottomNav.tsx` = In-scope (참고용), 대응 위치 = "Phase 2 탭바 셸에서 TDS Mobile로 재구현 — 직접 포팅 아님"
   - `Boknyang.tsx`, `RatingWidget.tsx`, `Particles.tsx` = Out-of-scope, 사유는 D-04에서 인용 (Boknyang=캐릭터 IP 검토 필요, RatingWidget=v1.2 PMF 도구 후보, Particles=framer-motion 도입과 함께 검토)

3. `### 데이터 (Data)` — 동일 컬럼
   - 행 1개 (`src/data/cards.ts`)
   - In-scope, **22장 메이저 아르카나** (id, name, name_en, emoji, keywords, message)
   - 대응 위치 = "**Supabase `tarot_cards` 테이블** (D-07) — 클라이언트 정적 데이터로 두지 않음. 정확 스키마는 Phase 3 마이그레이션에서 확정. 메시지 톤(`~다냥`)·길이 유지 (CONTEXT specifics)."

4. `### 라이브러리/스토어 (Libraries & Store)` — 동일 컬럼
   - 행 5개 (`store/tarot.ts`, `lib/utils.ts`, `lib/ratings.ts`, `lib/session.ts`, `lib/track.ts`)
   - `store/tarot.ts` = In-scope, "**zustand → React Context API + useState** 재구현 대상 (D-08). 대응 위치: `src/contexts/TarotContext.jsx` 또는 `TarotPage` 내부 useState (Phase 3 결정)"
   - `lib/utils.ts` = In-scope (필요 부분만), "데일리 원카드 흐름에서 호출되는 함수만 포팅 — 정확 범위는 Phase 3에서 grep 후 확정"
   - `lib/ratings.ts`, `lib/session.ts`, `lib/track.ts` = Out-of-scope, 사유 = "RatingWidget 의존(out)·자체 세션(fortune-cat은 `useSession` 사용)·자체 트래킹(fortune-cat은 Firebase `logEvent` 사용)"

5. `### 카드 이미지 (Card Images)` — 표 컬럼: `파일명` / `메이저 아르카나 ID` / `실제 형식` / `대응 fortune-cat 위치` / `비고`
   - **22행 (00 ~ 21)**: 모든 `00.webp ~ 21.webp` 명시 (`ls /Users/trinity/Projects/toss-app-generator/workspace/boknyang-tarot/boknyang-tarot/public/cards/` 결과 그대로)
   - "대응 fortune-cat 위치" 컬럼 모든 행: `src/assets/images/cards/{id}.webp` (정적 임포트, D-06)
   - 표 위 또는 아래에 명시 박스: "**중요:** CONTEXT.md D-06은 `.png`로 가정했으나 실제 프로토타입은 `.webp` 형식이다. Phase 3 자산 복사 시 PNG 변환 여부는 별도 결정 (CONTEXT deferred '카드 이미지 파일 포맷' 참조)."

### Section 2 — `## In-scope vs Out-of-scope 요약` (필수 H2)

두 H3 서브섹션:
- `### In-scope (포팅 평가 대상)` — bullet list로 D-03 In-scope 항목 인용 (라우트 3개·컴포넌트 2개·데이터 1·라이브러리 부분)
- `### Reviewed but excluded` — bullet list로 D-04 명시 항목 4개(`archive`, `RatingWidget`, `Particles`, `Boknyang`) + 추가 라이브러리 3개(`lib/ratings.ts`, `lib/session.ts`, `lib/track.ts`) — 각 항목에 사유와 향후 활용 가능성 한 줄

### Section 3 — `## 대응 fortune-cat 통합 지점 요약` (필수 H2)

- `src/assets/images/cards/` (신설) — 카드 이미지 22장 정적 임포트 위치 (D-06)
- Supabase `tarot_cards` 테이블 (신설, Phase 3) — 카드 해석 텍스트 (D-07)
- `src/pages/TarotPage.jsx` (신설, Phase 3) — `currentPage` 상태 머신 (`intro` → `shuffle` → `result`, D-10)
- `src/components/TarotCardArt.jsx` (신설, Phase 3) — `TarotCardArt.tsx`의 TS→JS 포팅
- `src/App.jsx` + 신규 탭바 컴포넌트 (Phase 2) — `BottomNav.tsx` 참고하되 TDS Mobile로 재구현
- `src/lib/supabase.js` (확장, Phase 3) — `tarot_cards` 조회 함수 추가

### 작성 규칙

- **모든 표는 markdown table 형식** (`| col | col |` + 구분선)
- **D-XX 결정 ID는 대괄호 없이 그대로 본문에 인용** (예: "D-04에 의해 제외")
- **22장 카드 행을 빠짐없이 기록** — 22개의 행이 정확히 있어야 함
- **한국어**로 작성 (CLAUDE.md 규칙)
- 외부 프로토타입 파일 경로는 절대 경로(`/Users/trinity/...`)로, fortune-cat 대응 위치는 상대 경로(`src/...`)로 작성
  </action>

  <acceptance_criteria>
    - 파일 `.planning/phases/01-tarot-prototype-evaluation/01-INVENTORY.md` 가 존재한다 (`test -f` 통과)
    - 파일에 H2 헤딩 `## 자산 인벤토리` 가 포함된다 (`grep -c "^## 자산 인벤토리" 01-INVENTORY.md` >= 1)
    - 5개 H3 서브섹션이 모두 존재: `### 라우트`, `### 컴포넌트`, `### 데이터`, `### 라이브러리/스토어`, `### 카드 이미지` (각 grep 결과 >= 1)
    - 22장 카드 이미지 파일명이 모두 등장: `00.webp` ~ `21.webp` 가 각각 최소 1회 언급 (`grep -c "0[0-9]\.webp\|1[0-9]\.webp\|2[01]\.webp" 01-INVENTORY.md` 결과 >= 22 — 또는 `for n in $(seq -w 0 21); do grep -q "${n}\.webp" 01-INVENTORY.md || echo MISSING $n; done` 출력 없음)
    - `## In-scope vs Out-of-scope 요약` H2 + `### Reviewed but excluded` H3 가 존재
    - Reviewed but excluded 섹션에 4개 항목 모두 등장: `archive`, `RatingWidget`, `Particles`, `Boknyang` (각 grep 결과 >= 1)
    - D-XX 결정 ID 인용: `D-03`, `D-04`, `D-06`, `D-07`, `D-10` 가 본문에 각각 최소 1회 등장
    - fortune-cat 대응 위치 키워드 등장: `src/assets/images/cards`, `tarot_cards`, `TarotPage`, `currentPage` 가 각각 최소 1회 등장
    - 파일 라인 수 >= 80 (`wc -l 01-INVENTORY.md` >= 80)
  </acceptance_criteria>

  <verify>
    <automated>
INV=.planning/phases/01-tarot-prototype-evaluation/01-INVENTORY.md
test -f "$INV" \
  && grep -q "^## 자산 인벤토리" "$INV" \
  && grep -q "^### 라우트" "$INV" \
  && grep -q "^### 컴포넌트" "$INV" \
  && grep -q "^### 데이터" "$INV" \
  && grep -q "^### 라이브러리/스토어" "$INV" \
  && grep -q "^### 카드 이미지" "$INV" \
  && grep -q "^## In-scope vs Out-of-scope 요약" "$INV" \
  && grep -q "^### Reviewed but excluded" "$INV" \
  && for n in $(seq -w 0 21); do grep -q "${n}\.webp" "$INV" || { echo "MISSING ${n}.webp"; exit 1; }; done \
  && for d in D-03 D-04 D-06 D-07 D-10; do grep -q "$d" "$INV" || { echo "MISSING $d"; exit 1; }; done \
  && for kw in archive RatingWidget Particles Boknyang; do grep -q "$kw" "$INV" || { echo "MISSING $kw"; exit 1; }; done \
  && for kw in "src/assets/images/cards" "tarot_cards" "TarotPage" "currentPage"; do grep -q "$kw" "$INV" || { echo "MISSING $kw"; exit 1; }; done \
  && [ "$(wc -l < "$INV")" -ge 80 ] \
  && echo "INVENTORY OK"
    </automated>
  </verify>

  <done>
    `01-INVENTORY.md` 가 존재하고 위 acceptance_criteria 모든 항목을 통과한다. 후속 페이즈가 grep으로 자산 위치·포팅 여부·대응 fortune-cat 위치를 즉시 찾을 수 있다.
  </done>
</task>

<task type="auto">
  <name>Task 2: 포팅 갭 매트릭스 작성 (01-GAPS.md)</name>
  <files>.planning/phases/01-tarot-prototype-evaluation/01-GAPS.md</files>

  <read_first>
    - `.planning/phases/01-tarot-prototype-evaluation/01-CONTEXT.md` (D-08, D-09, D-10 + `<specifics>` 색상 분류 규칙: 🔴 라우팅, 🟡 스타일/언어/상태/UI, 🟢 모션/아이콘)
    - `/Users/trinity/Projects/toss-app-generator/workspace/boknyang-tarot/boknyang-tarot/package.json` (의존성 목록)
    - `/Users/trinity/Projects/app-in-toss/fortune-cat/package.json` (대응 의존성 비교)
    - `/Users/trinity/Projects/toss-app-generator/workspace/boknyang-tarot/boknyang-tarot/granite.config.ts` (AIT 빌드 설정 비교)
    - `/Users/trinity/Projects/app-in-toss/fortune-cat/granite.config.ts` (대응 비교)
    - `.planning/codebase/STACK.md` (fortune-cat 의존성·환경변수 — 추가 충돌 항목 점검용)
    - 작성 직전 Task 1의 `01-INVENTORY.md` (자산 인벤토리에서 발견된 추가 충돌 항목 반영)
  </read_first>

  <action>
한국어 markdown 파일 `.planning/phases/01-tarot-prototype-evaluation/01-GAPS.md` 를 작성한다. 다음 구조와 컬럼·헤딩 텍스트를 **그대로** 사용한다.

### 파일 헤더

```
# Phase 1: 포팅 갭 매트릭스 (Gap Matrix)

**Created:** 2026-04-30
**Source prototype:** `/Users/trinity/Projects/toss-app-generator/workspace/boknyang-tarot/boknyang-tarot/`
**Target codebase:** `/Users/trinity/Projects/app-in-toss/fortune-cat/`
**Decisions referenced:** D-08, D-09, D-10 (see `01-CONTEXT.md`)
**색상 분류 규칙 (CONTEXT specifics):** 🔴 라우팅 / 🟡 스타일·언어·상태·UI / 🟢 모션·아이콘
```

### Section 1 — `## 갭 매트릭스` (필수 H2)

표 컬럼 (이 순서로):
`충돌 영역` | `프로토타입 측` | `fortune-cat 측` | `충돌 강도` | `해결 전략` | `관련 D-XX`

**필수 행 (정확히 이 순서, 최소 6행):**

1. **라우팅** | TanStack Router (`@tanstack/react-router` ^1.168.0) + `routeTree.gen.ts` 자동 생성 + 4 라우트 파일 | React Router DOM 7.9.5 (이미 설치) + `currentPage` 상태 머신 패턴 (`SajuPage`, `AmuletPage`) | 🔴 | 라우트 파일을 직접 포팅하지 않고 `TarotPage.jsx` 단일 라우트 + 내부 `currentPage` 상태(`intro` → `shuffle` → `result`)로 재구성. `__root.tsx`는 `App.jsx` Provider 구조로 흡수. 신규 `/tarot` 라우트 추가 금지(PROJECT.md). | D-10
2. **스타일링** | Tailwind v4 (`tailwindcss` ^4.2.4, `@tailwindcss/vite`, `tailwind-merge`, `clsx`, `class-variance-authority`) | Emotion (`@emotion/react` 11.14.0) + TDS Mobile 토큰 | 🟡 | Tailwind 클래스를 Emotion `css` prop / styled 컴포넌트로 재작성. `clsx`/`tailwind-merge`/`cva`는 도입 안 함 (DRY + AIT 심사 영향 최소화). 색상·간격은 TDS 토큰 우선, 부족 시 Emotion inline 정의. | D-08
3. **상태 관리** | zustand ^5.0.12 (`src/store/tarot.ts`) | React Context API + `useState` (현재 `SessionContext`/`ToastContext`/`AnonymousKeyContext` 패턴) | 🟡 | `tarot.ts` 스토어 슬라이스를 `TarotPage` 내부 `useState` 또는 신규 `TarotContext.jsx`(전역 필요 시)로 재구현. 신규 의존성 추가 없음. 정확한 위치(local state vs Context)는 Phase 3에서 결정. | D-08
4. **UI 프리미티브** | Radix UI (`@radix-ui/react-{dialog,label,separator,slot,toggle,tooltip}`) + `lucide-react` 아이콘 | TDS Mobile (`@toss/tds-mobile`, `@toss/tds-mobile-ait`) — 이미 사용 중 | 🟡 | Radix 컴포넌트는 데일리 원카드 흐름에서 사용된 것만 TDS Mobile 카탈로그로 1:1 매칭 후 재작성. 매칭 어려운 컴포넌트는 Emotion + 기본 HTML로 폴백. `lucide-react` 아이콘은 도입하지 않고 TDS 아이콘 또는 카드 이미지/이모지로 대체. | D-08
5. **언어** | TypeScript (`.ts`/`.tsx`, `tsconfig.{app,node}.json`) | JavaScript (`.js`/`.jsx`, ESLint만) | 🟡 | TS 코드를 JSX로 변환 (타입 어노테이션 제거, 인터페이스 → JSDoc 주석). `cards.ts`는 Supabase 데이터로 이전(D-07)되므로 변환 대상 아님. | D-08
6. **모션** | `framer-motion` ^12.38.0 (카드 뒤집기·셔플·`Particles`) | 미도입 — TDS 기본 트랜지션만 사용 | 🟢 | **Phase 1에서 결정하지 않는다 (D-09)**. Phase 2(탭바) 또는 Phase 3(데일리 원카드)에서 "TDS 기본 트랜지션 + CSS keyframes로 카드 뒤집기 가능 여부" 우선 검토. 불가피한 경우에만 도입. `Particles`는 Out-of-scope이므로 모션 의존도 낮음. | D-09

**선택 추가 행 (Task 1 인벤토리 작성 중 발견된 경우만):**
- `cards.ts` 데이터 위치 (정적 import → Supabase) — 강도 🟡, 해결 전략은 D-07 인용
- 카드 이미지 형식 (.webp) — 강도 🟢, fortune-cat의 기존 정적 이미지(`src/assets/images/`)와 형식 차이 점검 후 Phase 3에서 변환 여부 결정 (CONTEXT deferred 항목)
- AIT 빌드 설정 (`granite.config.ts` 차이) — 강도 🟢 또는 🟡, 권한 변경 영향 범위 명시

추가 행은 발견 시에만 추가. 발견 못 했으면 6행으로 완성.

### Section 2 — `## 충돌 강도 요약` (필수 H2)

세 H3 (각 강도별):
- `### 🔴 차단성 충돌 (Critical)` — 라우팅 1건. "Phase 2/3 시작 전 반드시 단일 라우트 + currentPage 패턴으로 합의 완료"
- `### 🟡 변환 필요 충돌 (Significant)` — 스타일링·상태·UI 프리미티브·언어 (4건). "각 의존성을 fortune-cat 패턴으로 재구현 — 신규 의존성 추가 없음 (D-08)"
- `### 🟢 결정 보류 / 영향 미미 (Low)` — 모션 (1건, framer-motion 도입 보류). "Phase 2/3에서 TDS 우선 검토 후 결정 (D-09)"

### Section 3 — `## 신규 의존성 정책` (필수 H2)

bullet list:
- TDS Mobile/AIT 디자인 일관성 + AIT 심사 영향 최소화를 위해 **신규 의존성 추가 없음** 원칙 (D-08)
- 예외: `framer-motion` 만 Phase 2/3에서 별도 검토 (D-09)
- 결과: 갭 매트릭스의 모든 🟡 항목은 fortune-cat 기존 의존성으로 재구현

### Section 4 — `## 후속 페이즈 영향` (필수 H2)

bullet list (각 페이즈 → 어떤 갭 항목이 영향을 주는지):
- **Phase 2 (탭바 셸):** 라우팅(🔴) + UI 프리미티브(🟡, BottomNav → TDS) + 모션(🟢, 탭 전환 트랜지션)
- **Phase 3 (데일리 원카드 코어):** 스타일링(🟡) + 상태 관리(🟡) + 언어(🟡) + 모션(🟢, 카드 뒤집기) + 데이터 위치(D-07)
- **Phase 4 (광고 게이팅):** 갭 항목 직접 영향 적음 — fortune-cat의 `Loading.jsx` 패턴 재사용
- **Phase 5 (Analytics·공유):** 갭 항목 직접 영향 적음 — `track.ts` Out-of-scope, fortune-cat `logEvent` 사용

### 작성 규칙

- **표는 markdown table 형식**
- **충돌 강도 컬럼에 이모지 정확히 사용**: 🔴, 🟡, 🟢
- **D-XX 결정 ID는 본문에 그대로 인용**
- **한국어**로 작성
- 외부 프로토타입 의존성 버전은 `package.json`에서 그대로 인용 (예: `^1.168.0`, `^4.2.4`)
  </action>

  <acceptance_criteria>
    - 파일 `.planning/phases/01-tarot-prototype-evaluation/01-GAPS.md` 가 존재한다
    - 4개 H2 헤딩 모두 존재: `## 갭 매트릭스`, `## 충돌 강도 요약`, `## 신규 의존성 정책`, `## 후속 페이즈 영향`
    - 표에 충돌 강도 마커가 모두 등장: `🔴`, `🟡`, `🟢` 가 각각 최소 1회
    - 6개 필수 충돌 영역 키워드가 모두 등장: `라우팅`, `스타일링`, `상태 관리`, `UI 프리미티브`, `언어`, `모션`
    - 5종 의존성 충돌이 명시: `Tailwind` (또는 `tailwindcss`), `zustand`, `Radix`, `TypeScript` (또는 `TS`), `TanStack` 키워드 각각 최소 1회
    - 대응 fortune-cat 패턴 명시: `Emotion`, `Context`, `TDS Mobile`, `JavaScript` (또는 `JSX`), `React Router` 키워드 각각 최소 1회
    - `framer-motion` 키워드 등장 + `D-09` 인용
    - D-XX 결정 ID 인용: `D-08`, `D-09`, `D-10` 가 본문에 각각 최소 1회 등장
    - 파일 라인 수 >= 40 (`wc -l 01-GAPS.md` >= 40)
  </acceptance_criteria>

  <verify>
    <automated>
GAP=.planning/phases/01-tarot-prototype-evaluation/01-GAPS.md
test -f "$GAP" \
  && grep -q "^## 갭 매트릭스" "$GAP" \
  && grep -q "^## 충돌 강도 요약" "$GAP" \
  && grep -q "^## 신규 의존성 정책" "$GAP" \
  && grep -q "^## 후속 페이즈 영향" "$GAP" \
  && grep -q "🔴" "$GAP" && grep -q "🟡" "$GAP" && grep -q "🟢" "$GAP" \
  && for kw in 라우팅 스타일링 "상태 관리" "UI 프리미티브" 언어 모션; do grep -q "$kw" "$GAP" || { echo "MISSING $kw"; exit 1; }; done \
  && for kw in Tailwind zustand Radix TanStack framer-motion; do grep -q "$kw" "$GAP" || { echo "MISSING $kw"; exit 1; }; done \
  && for kw in Emotion Context "TDS Mobile" "React Router"; do grep -q "$kw" "$GAP" || { echo "MISSING $kw"; exit 1; }; done \
  && for d in D-08 D-09 D-10; do grep -q "$d" "$GAP" || { echo "MISSING $d"; exit 1; }; done \
  && [ "$(wc -l < "$GAP")" -ge 40 ] \
  && echo "GAPS OK"
    </automated>
  </verify>

  <done>
    `01-GAPS.md` 가 존재하고 위 acceptance_criteria 모든 항목을 통과한다. 후속 페이즈 플래너가 의존성 재구현 전략·강도·관련 결정을 표 한 장으로 즉시 참조할 수 있다.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 외부 프로토타입 디렉토리 → fortune-cat 평가 산출물 | 사용자 본인 워크스페이스 내부 파일 읽기. 임의 코드 실행 없음 (Read 도구만 사용). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | 인벤토리/갭 문서가 외부 레포 내부 구조를 포함 | accept | `boknyang-tarot`은 사용자 본인 소유 워크스페이스로 외부 노출 위험 없음. 산출물은 `.planning/phases/` 안에만 존재하며 git에서 관리됨. |
| T-01-02 | Tampering (트레이서빌리티 누락) | D-XX 결정과 산출물 간 연결 | mitigate | 모든 표 행에 "관련 D-XX" 컬럼 강제. acceptance_criteria가 D-03·D-04·D-06·D-07·D-08·D-09·D-10 인용을 grep으로 검증. |
| T-01-03 | Repudiation (산출물 변경 추적 불가) | 산출물 markdown | mitigate | 파일은 git에 커밋되어 변경 이력 추적 가능. `Created:` 헤더로 작성 시점 명시. |
</threat_model>

<verification>
- 두 산출물 모두 phase dir(`.planning/phases/01-tarot-prototype-evaluation/`)에 생성되어 있다
- 두 task의 `<verify><automated>` 명령이 성공 종료 코드로 통과한다
- 각 산출물이 후속 페이즈(2~5) 플래너가 grep으로 검색 가능한 헤딩·키워드 구조를 갖는다
- D-04 "Reviewed but excluded" 4개 항목(archive/RatingWidget/Particles/Boknyang)이 누락 없이 인벤토리에 기록되어 있다
- 22장 카드 이미지 파일(00.webp ~ 21.webp)이 인벤토리에 모두 등장한다
- 갭 매트릭스에 5종 의존성 충돌 + framer-motion 미결정 항목이 강도 표시(🔴/🟡/🟢)와 함께 명시되어 있다
- 모든 표 행이 관련 D-XX 결정 ID를 참조한다
</verification>

<success_criteria>
1. ✅ Success Criteria 1: 사용자가 `01-INVENTORY.md`에서 프로토타입 레포 경로(D-01)와 핵심 자산(카드 이미지 22장 + 라우트·컴포넌트·데이터 표)을 표 형태로 확인 가능
2. ✅ Success Criteria 2: `01-INVENTORY.md`의 `## In-scope vs Out-of-scope 요약` 섹션과 각 표 행의 `In/Out-of-scope` 컬럼이 포팅 대상과 제외 항목을 명시적으로 구분 (D-03·D-04)
3. ✅ Success Criteria 3: `01-GAPS.md`가 5종 의존성 충돌 + framer-motion 미결정을 강도 표시·해결 전략과 함께 매트릭스로 제공 (D-08·D-09)
4. ✅ Success Criteria 4: `01-INVENTORY.md`의 `대응 fortune-cat 위치` 컬럼이 카드 이미지(`src/assets/images/cards/` 정적 임포트)와 해석 텍스트(Supabase `tarot_cards` 테이블) 결정을 모든 행에 반영 (D-06·D-07)
</success_criteria>

<output>
실행 완료 후 `.planning/phases/01-tarot-prototype-evaluation/01-01-SUMMARY.md` 를 작성한다.

SUMMARY는 다음을 포함:
- 두 산출물 파일 경로
- 인벤토리 행 수 / 갭 매트릭스 행 수
- 발견된 추가 충돌 항목(있다면)
- D-06 가정(`.png`)과 실제 형식(`.webp`)의 차이 — Phase 3 자산 복사 시점에 변환 여부 결정 필요
- 후속 페이즈(2~5) 플래너에게 전달할 핵심 메시지 3줄 이내
</output>
