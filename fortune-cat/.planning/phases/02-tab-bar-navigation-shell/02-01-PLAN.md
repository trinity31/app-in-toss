---
phase: 02-tab-bar-navigation-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/TabBar.jsx
  - src/pages/TarotPage.jsx
  - src/App.jsx
autonomous: true
requirements: [NAV-01, NAV-02, NAV-03]
must_haves:
  truths:
    - "사용자가 `/` 또는 `/tarot`에서 하단 탭바의 '사주'/'타로' 버튼을 한 번 탭하면 즉시 해당 라우트로 전환된다 (NAV-01, SC1)"
    - "사용자가 active 탭(컬러 + 아이콘 fill)과 inactive 탭(grey + 아이콘 stroke)을 시각적으로 명확히 구분할 수 있다 (NAV-02, SC2, D-09)"
    - "사용자가 `/saju`, `/newyear`, `/amulet` 라우트의 v1.0 흐름(진입 경로·결과 화면)을 v1.0과 동일하게 이용할 수 있다 — 회귀 없음 (NAV-03, SC3)"
    - "사용자가 `/saju`, `/newyear`, `/amulet`에서는 탭바가 보이지 않고, `/`와 `/tarot`에서만 탭바를 본다 (SC4, D-05)"
    - "사용자가 `/tarot`에 진입하면 빈 컨테이너(Phase 3 placeholder)를 볼 수 있다 (D-01)"
  artifacts:
    - path: "src/components/TabBar.jsx"
      provides: "라우트별 표시 판정 + 사주/타로 두 탭 + active 시각 강조 컴포넌트"
      exports: ["default (TabBar)"]
    - path: "src/pages/TarotPage.jsx"
      provides: "/tarot 라우트의 빈 컨테이너 (Phase 3에서 currentPage 상태 머신으로 확장)"
      exports: ["default (TarotPage)"]
    - path: "src/App.jsx"
      provides: "/tarot 라우트 추가 + <Routes> 바깥에 TabBar 형제로 통합"
      contains: "<Route path=\"/tarot\""
  key_links:
    - from: "src/App.jsx"
      to: "src/components/TabBar.jsx"
      via: "<Routes> 바깥의 sibling JSX (TabBar는 Routes와 형제)"
      pattern: "<TabBar"
    - from: "src/App.jsx"
      to: "src/pages/TarotPage.jsx"
      via: "<Route path=\"/tarot\" element={<TarotPage />} />"
      pattern: "path=\"/tarot\""
    - from: "src/components/TabBar.jsx"
      to: "react-router-dom"
      via: "useLocation() + useNavigate() — 라우트 판정과 전환"
      pattern: "useLocation|useNavigate"
---

<objective>
Phase 2 단일 플랜: 사주/타로 두 탭을 한 번에 전환할 수 있는 **하단 탭바 셸**과 `/tarot` 라우트의 빈 컨테이너(`TarotPage.jsx`)를 도입한다. 기존 사주 흐름(`/`, `/saju`, `/newyear`, `/amulet`)은 v1.0과 동일하게 유지하며, 탭바는 `/saju`·`/newyear`·`/amulet` 풀스크린 흐름을 가리지 않도록 라우트별 숨김 정책을 따른다.

Purpose: NAV-01/02/03 충족 — 4050 여성 사용자가 단일 진입점에서 사주↔타로를 직관적으로 전환할 수 있도록 셸을 마련한다. 데일리 원카드 코어(`intro` → `shuffle` → `result`)는 Phase 3 책임이므로 본 플랜은 라우팅·탭바 셸·빈 컨테이너까지만.

Output: `src/components/TabBar.jsx` (신규), `src/pages/TarotPage.jsx` (신규 빈 컨테이너), `src/App.jsx` (수정 — `/tarot` 라우트 추가 + `<Routes>` 바깥에 `TabBar` 형제 배치).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tab-bar-navigation-shell/02-CONTEXT.md
@.planning/phases/01-tarot-prototype-evaluation/01-CONTEXT.md
@.planning/phases/01-tarot-prototype-evaluation/01-INVENTORY.md
@.planning/phases/01-tarot-prototype-evaluation/01-GAPS.md

@src/App.jsx
@src/main.jsx
@src/pages/HomePage.jsx
@src/pages/SajuPage.jsx
@CLAUDE.md

<interfaces>
<!-- 본 플랜이 의존·수정하는 핵심 인터페이스. 실행자는 추가 탐색 없이 이를 사용. -->

현재 src/App.jsx 라우트 정의 (수정 전):
```jsx
import { Navigate, Route, Routes } from 'react-router-dom'
import HomePage from './pages/HomePage'
import SajuPage from './pages/SajuPage'
import NewYearPage from './pages/NewYearPage'
import AmuletPage from './pages/AmuletPage'
import './App.css'

function App() {
  return (
    <Routes>
      <Route path="/" element={<HomePage />} />
      <Route path="/saju" element={<SajuPage />} />
      <Route path="/newyear" element={<NewYearPage />} />
      <Route path="/amulet" element={<AmuletPage />} />
      <Route path="*" element={<HomePage />} />
    </Routes>
  )
}
```
주의: ROADMAP.md/PROJECT.md 일부 문서는 `/new-year`로 표기하지만 **실제 코드는 `/newyear`**. 본 플랜은 실제 코드(`/newyear`) 기준을 따른다.

react-router-dom 7.9.5 (이미 설치):
- `useLocation(): { pathname: string, ... }` — 현재 경로 판정
- `useNavigate(): (to: string) => void` — 라우트 전환
- `<Routes>`, `<Route path element>` — 라우트 선언

@toss/tds-mobile 2.1.2 (이미 설치, 카탈로그 grep 결과):
- `Tab` + `Tab.Item` 컴포넌트 존재 (상단 세그먼트 탭 스타일이 가능성 높음 — 실행 시 d.ts/사용 예시로 하단 탭바 적합 여부 재확인)
- `BottomCTA`, `BottomInfo`, `BottomSheet` 등은 하단 탭바와 의미 다름
- `TabBar`/`BottomNav`/`TabNavigation`/`BottomTab` 명시적 export 없음 (현재 grep 기준)
- 따라서 **D-07에 따라 Emotion 폴백 자체 제작이 우선 시나리오** — 실행자가 d.ts 한 번 더 확인 후 분기

@toss/tds-colors 0.1.0 (이미 설치, HomePage에서 import 패턴 확인):
- `import { colors } from '@toss/tds-colors'` — `colors.blue500`, `colors.grey500` 등 토큰 접근

@emotion/react 11.14.0 (이미 설치):
- `import { css } from '@emotion/react'` — Emotion css prop

@apps-in-toss/web-framework — 본 플랜에서는 사용하지 않음 (탭바는 토스 네이티브 브리지 비의존).

boknyang-tarot 참고 원본 (직접 포팅 아님 — 구조·active 판정 로직만 참고):
경로: `/Users/trinity/Projects/toss-app-generator/workspace/boknyang-tarot/boknyang-tarot/src/components/BottomNav.tsx`
- TABS 배열 패턴: `[{ to, label, Icon }, ...]`
- active 판정: `to === '/' ? location.pathname === '/' || pathname === '/shuffle' || pathname === '/result' : location.pathname === to`
- fortune-cat에서는 `/`(HomePage)와 `/tarot`이 명확히 분리되므로 단순 매칭 사용
- TanStack `Link` → React Router DOM `useNavigate()`로 대체
- `lucide-react` 아이콘 도입 금지 (D-08)
- Tailwind 클래스 → Emotion css prop 또는 inline style
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TabBar.jsx 신규 — 사주/타로 두 탭 + 라우트별 표시 판정 + active 시각 강조</name>
  <read_first>
    - `src/App.jsx` (현재 라우트 정의 — 수정 전 상태 파악)
    - `src/main.jsx` (Provider 체인 — TabBar는 ThemeProvider 안쪽에 위치)
    - `src/pages/HomePage.jsx` 1-30행 (TDS Mobile + `@toss/tds-colors` import 패턴 참고)
    - `.planning/phases/02-tab-bar-navigation-shell/02-CONTEXT.md` (D-01~D-10 잠금 결정 — 특히 D-05 라우트별 숨김, D-07 TDS 우선 + Emotion 폴백, D-08 TDS 아이콘 + lucide-react 금지, D-09 active = 컬러+fill, D-10 즉시 전환)
    - `.planning/phases/01-tarot-prototype-evaluation/01-CONTEXT.md` (P1-D-08 신규 의존성 금지 carry-forward)
    - `.planning/phases/01-tarot-prototype-evaluation/01-GAPS.md` (UI 프리미티브 🟡, 모션 🟢 행)
    - `/Users/trinity/Projects/toss-app-generator/workspace/boknyang-tarot/boknyang-tarot/src/components/BottomNav.tsx` (구조·active 판정 로직 참고용 — 직접 포팅 아님)
    - `node_modules/@toss/tds-mobile/dist/esm/index.d.ts`에서 `Tab`/`Tab.Item` 시그니처 (Task 1 시작 시 분기 결정용)
  </read_first>

  <files>src/components/TabBar.jsx</files>

  <action>
**구현 절차:**

**Step 1 — TDS 카탈로그 분기 결정 (D-07):**
다음 명령으로 TDS 컴포넌트 가용성을 한 번 더 확정한다:
```bash
grep -E "^export declare (const|function) (Tab|BottomNav|TabBar|TabNavigation|BottomTab)" \
  node_modules/@toss/tds-mobile/dist/esm/index.d.ts
grep -E "^export declare (const|function) (Tab|BottomNav|TabBar|TabNavigation|BottomTab)" \
  node_modules/@toss/tds-mobile-ait/dist/esm/index.d.ts 2>/dev/null
```
- **케이스 A (TDS `Tab`/`Tab.Item`이 하단 탭바 용도로 적합):** `Tab.Item`의 props가 `{ icon?, label?, active?, onClick? }` 또는 유사 형태로 하단 탭에 자연스럽게 매핑되면 wrapper로 사용. 단, 현재 `Tab`은 상단 세그먼트 탭일 가능성이 높으므로 d.ts/JSDoc을 직접 확인하고 위치(top vs bottom)·시각이 D-09 (컬러+아이콘 fill)에 맞는지 판단.
- **케이스 B (TDS 컴포넌트가 하단 탭바 용도로 부적합 — 우선 시나리오):** Emotion + 기본 HTML(`<nav>` + `<ul>` + `<li>` + `<button>`)로 자체 제작. boknyang-tarot `BottomNav.tsx` 구조를 참고하되 신규 의존성 추가 금지.

**선택한 케이스를 파일 상단 주석으로 1줄 기록한다.** 예: `// TDS Tab은 상단 세그먼트용이라 D-07 폴백(Emotion + nav/ul/li/button)으로 구현 — 카탈로그 grep 2026-XX-XX 확인`.

**Step 2 — 컴포넌트 시그니처 (default export):**
```jsx
// src/components/TabBar.jsx
import { useLocation, useNavigate } from 'react-router-dom'
import { colors } from '@toss/tds-colors'
// Step 1 결과에 따라 TDS Tab 또는 Emotion css 추가 import

const TABS = [
  { to: '/', label: '사주' },
  { to: '/tarot', label: '타로' },
]

// 라우트별 표시 정책 (D-05): /, /tarot에서만 표시
const VISIBLE_PATHS = new Set(['/', '/tarot'])

export default function TabBar() {
  const location = useLocation()
  const navigate = useNavigate()

  // 라우트별 숨김 (D-05) — /saju, /newyear, /amulet에서는 null 반환
  if (!VISIBLE_PATHS.has(location.pathname)) {
    return null
  }

  // active 판정 (D-09): 정확 매칭. /(HomePage)와 /tarot이 분리되어 있어 단순 비교 충분.
  const isActive = (to) => location.pathname === to

  // 탭 클릭 (D-03): 각 탭의 처음 화면으로 reset (사주=/, 타로=/tarot)
  const handleTabClick = (to) => {
    navigate(to)
  }

  return (
    /* nav 형태로 렌더 */
  )
}
```

**Step 3 — 시각 (D-09 active = 컬러 + 아이콘 fill):**
- active 컬러: `colors.blue500` (또는 fortune-cat 브랜드 토큰 — TDS 토큰 우선, HomePage에서 사용된 토큰과 일관). 정확한 토큰 선택은 Claude's Discretion (CONTEXT D-09).
- inactive 컬러: `colors.grey500` 또는 `colors.grey600` (대비 충분).
- 레이블 폰트 굵기 동일 유지 (D-09 — 컬러로만 구분).
- 아이콘 (D-08): 사주 = 별/달/점성술 SVG path, 타로 = 카드/다이아몬드 SVG path. **TDS 아이콘이 적절한 것이 있으면 그것 사용, 없으면 SVG path 자체 정의.** active일 때 `fill={activeColor}`, inactive일 때 `fill="none"` + `stroke={inactiveColor}` (filled vs outlined variant). **`lucide-react` 도입 금지.**
- 정확한 SVG path는 Claude's Discretion. 단순한 유니코드 문자(예: `★`, `🃏`)는 D-08 "가능하면 TDS 세트 한정" 단서에 따라 폴백 — TDS 아이콘 또는 SVG path 우선.

**Step 4 — 즉시 전환 (D-10):**
- 어떤 CSS `transition` 속성도 추가하지 않는다. `transform`, `opacity`, `color` 모두 transition 없음.
- `framer-motion` import 금지.
- 페이지 전환 wrapper(예: `<AnimatePresence>`) 추가 금지.

**Step 5 — 높이/safe-area (D-06):**
- 자체 `height: Xpx`/`padding-bottom: env(safe-area-inset-bottom)` 등을 직접 지정하지 않는다.
- 케이스 B(Emotion 폴백)에서는 `padding-bottom: env(safe-area-inset-bottom)` **한 줄만** 적용 가능 (TDS Mobile-AIT의 `SafeAreaInsets`가 자동 처리하지 않는 경우 대비). hit area 충족용 `min-height: 56px` 정도는 허용 (4050 사용자 기준).
- 자체 폰트 크기·spacing 토큰 정의 금지 — `colors` 토큰 + 기본 px만 사용.

**Step 6 — 레이아웃:**
- `position: fixed; bottom: 0; left: 0; right: 0; z-index: 20` (다른 페이지 컨텐츠 위에 떠 있도록).
- `background: white` (또는 TDS surface 토큰).
- 상단 1px border 또는 subtle shadow (TDS 패턴 일관성).
- `display: flex; justify-content: space-around` 두 탭 균등 분배.
- 각 탭 버튼: flex column, 아이콘 위 + 레이블 아래, `align-items: center`, `gap: 2-4px`, 폰트 11-12px (boknyang-tarot 패턴 참고).
- 탭 버튼은 `<button type="button">`으로 onClick 처리 (`<a>` 사용 금지 — SPA navigate).

**검증 가능한 명세 요약:**
- TABS 배열에 정확히 2개 항목, 각각 `to`/`label` 키 포함.
- 두 항목의 `label`은 정확히 `'사주'`, `'타로'`.
- 두 항목의 `to`는 정확히 `'/'`, `'/tarot'`.
- `useLocation`, `useNavigate` 양쪽 모두 import.
- `VISIBLE_PATHS` 또는 동등한 표현으로 `/saju`, `/newyear`, `/amulet`에서 숨김 처리.
- `lucide-react` import 0회.
- `framer-motion` import 0회.
- `clsx`, `tailwind-merge`, `class-variance-authority` import 0회.
- CSS `transition` 속성 사용 0회 또는 최소 (D-10).

신규 npm 의존성을 절대 설치하지 않는다 (`package.json` `dependencies`/`devDependencies` 변경 금지).
  </action>

  <acceptance_criteria>
    - 파일 존재: `test -f src/components/TabBar.jsx` exits 0
    - default export: `grep -c "export default function TabBar" src/components/TabBar.jsx` returns 1 (또는 `export default TabBar` 형태로 1)
    - 두 탭 정의: `grep -c "'사주'" src/components/TabBar.jsx` returns 1 이상 AND `grep -c "'타로'" src/components/TabBar.jsx` returns 1 이상
    - 라우트 정의: `grep -c "'/tarot'" src/components/TabBar.jsx` returns 1 이상
    - react-router-dom 사용: `grep -E "useLocation|useNavigate" src/components/TabBar.jsx | wc -l` returns 2 이상
    - 라우트별 숨김 판정 존재: `grep -E "(/saju|/newyear|/amulet|VISIBLE_PATHS|return null)" src/components/TabBar.jsx | wc -l` returns 1 이상
    - **신규 의존성 금지 (P1-D-08, D-08):**
      - `grep -c "lucide-react" src/components/TabBar.jsx` returns 0
      - `grep -c "framer-motion" src/components/TabBar.jsx` returns 0
      - `grep -c "clsx" src/components/TabBar.jsx` returns 0
      - `grep -c "tailwind-merge" src/components/TabBar.jsx` returns 0
      - `grep -c "class-variance-authority" src/components/TabBar.jsx` returns 0
    - **D-10 즉시 전환:** `grep -cE "transition\\s*:" src/components/TabBar.jsx` returns 0 (CSS transition 속성 미사용; "transition" 단어가 주석/이름에 등장하면 허용)
    - **D-07 분기 결정 기록:** 파일 상단(첫 5줄 이내)에 TDS vs Emotion 폴백 선택 사유 주석 1줄 존재 (`grep -nE "^//.*(TDS|Emotion|D-07)" src/components/TabBar.jsx | head -5` returns at least 1 line)
    - **package.json 변경 없음:** `git diff --name-only -- package.json package-lock.json pnpm-lock.yaml | wc -l` returns 0
  </acceptance_criteria>

  <verify>
    <automated>test -f src/components/TabBar.jsx && grep -c "export default" src/components/TabBar.jsx && grep -c "'/tarot'" src/components/TabBar.jsx && [ "$(grep -c "lucide-react" src/components/TabBar.jsx)" = "0" ] && [ "$(grep -c "framer-motion" src/components/TabBar.jsx)" = "0" ]</automated>
  </verify>

  <done>
    `src/components/TabBar.jsx` 파일이 존재하며 default export로 TabBar 컴포넌트를 노출한다. 두 개의 탭(`사주` → `/`, `타로` → `/tarot`)이 정의되어 있고, `useLocation`/`useNavigate`로 라우트 판정·전환을 처리한다. `/`와 `/tarot`에서만 렌더되며 `/saju`, `/newyear`, `/amulet`에서는 `null`을 반환한다. active 탭은 컬러 + 아이콘 fill로 강조되고, inactive 탭은 grey + stroke. CSS transition·framer-motion·lucide-react·clsx·tailwind-merge·class-variance-authority 모두 미사용. 신규 npm 의존성 0개 추가됨.
  </done>
</task>

<task type="auto">
  <name>Task 2: TarotPage.jsx 신규 — /tarot 라우트의 빈 컨테이너 (Phase 3 확장 포인트)</name>
  <read_first>
    - `src/pages/SajuPage.jsx` 1-60행 (`currentPage` 상태 머신 패턴 — TarotPage는 동일 패턴이지만 본 페이즈는 placeholder만)
    - `src/pages/HomePage.jsx` 1-30행 (TDS Mobile import 패턴 참고)
    - `.planning/phases/02-tab-bar-navigation-shell/02-CONTEXT.md` (D-04 단일 라우트 + currentPage 패턴 유지, Claude's Discretion: placeholder 콘텐츠는 Phase 3에서 교체될 자리표시)
    - `.planning/phases/01-tarot-prototype-evaluation/01-INVENTORY.md` (Phase 3 확장 시 currentPage 단계 = `intro` → `shuffle` → `result`)
  </read_first>

  <files>src/pages/TarotPage.jsx</files>

  <action>
**구현 절차:**

**Step 1 — 컴포넌트 시그니처 (default export):**
```jsx
// src/pages/TarotPage.jsx
// Phase 2: /tarot 라우트의 빈 컨테이너. Phase 3에서 currentPage('intro' → 'shuffle' → 'result')
//          상태 머신으로 확장됨 (D-04). 본 페이즈는 placeholder만.
import { useState } from 'react'

export default function TarotPage() {
  // Phase 3 확장 포인트: currentPage 상태 머신 (intro/shuffle/result)
  // 본 페이즈에서는 'intro' 단계의 placeholder만 노출.
  const [currentPage] = useState('intro')

  return (
    <div
      style={{
        minHeight: '100vh',
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        justifyContent: 'center',
        padding: '24px',
        // 탭바(높이 ~56-64px + safe-area)가 화면 하단에 fixed로 떠 있으므로
        // 컨테이너 하단 padding으로 컨텐츠가 가려지지 않게 buffer 확보 (D-06: TDS 기본 패턴 + safe-area는 TabBar가 처리)
        paddingBottom: '96px',
      }}
    >
      <p style={{ fontSize: '16px', color: '#666' }}>타로 준비 중</p>
    </div>
  )
}
```

**Step 2 — 단일 라우트 + currentPage 패턴 유지 (D-04):**
- `useState('intro')` 형태로 상태 머신 진입점만 마련. Phase 3 실행자가 `setCurrentPage('shuffle')`/`('result')` 등을 추가할 위치를 명확히 한다.
- 현 페이즈는 분기 로직(`if (currentPage === ...)`) 작성 금지 — placeholder만.

**Step 3 — placeholder 콘텐츠 (Claude's Discretion):**
- 텍스트 한 줄("타로 준비 중" 또는 동등한 한국어 메시지)만 렌더.
- 일러스트·이미지·복잡한 레이아웃 추가 금지 (Phase 3 책임).
- TDS Mobile 컴포넌트(예: `Text` 등) import는 선택 — 단순 `<p>` + inline style로 충분.

**Step 4 — 신규 의존성 금지:**
- 본 컴포넌트에서 `framer-motion`, `lucide-react`, `clsx`, `tailwind-merge`, `class-variance-authority` 모두 import 금지.
- 신규 npm 패키지 설치 금지.

**Step 5 — Phase 3 확장 포인트 명시:**
- 파일 상단 주석에 "Phase 3에서 currentPage 상태 머신으로 확장" 명시 (실행자가 본 파일을 다시 열 때 즉시 의도를 파악할 수 있도록).

**검증 가능한 명세 요약:**
- 파일이 존재하고 default export로 `TarotPage` 컴포넌트를 노출.
- `currentPage` 상태 변수 선언 (Phase 3 확장 포인트).
- placeholder 텍스트 1개 렌더.
- 신규 의존성 import 0개.
  </action>

  <acceptance_criteria>
    - 파일 존재: `test -f src/pages/TarotPage.jsx` exits 0
    - default export: `grep -c "export default function TarotPage" src/pages/TarotPage.jsx` returns 1 (또는 `export default TarotPage` 1)
    - currentPage 상태 머신 진입점 존재: `grep -c "currentPage" src/pages/TarotPage.jsx` returns 1 이상
    - Phase 3 확장 포인트 주석: `grep -E "Phase 3|intro|shuffle|result" src/pages/TarotPage.jsx | wc -l` returns 1 이상
    - 신규 의존성 import 0개:
      - `grep -c "framer-motion" src/pages/TarotPage.jsx` returns 0
      - `grep -c "lucide-react" src/pages/TarotPage.jsx` returns 0
    - **package.json 변경 없음:** `git diff --name-only -- package.json package-lock.json pnpm-lock.yaml | wc -l` returns 0
  </acceptance_criteria>

  <verify>
    <automated>test -f src/pages/TarotPage.jsx && grep -c "export default" src/pages/TarotPage.jsx && grep -c "currentPage" src/pages/TarotPage.jsx && [ "$(grep -c "framer-motion" src/pages/TarotPage.jsx)" = "0" ]</automated>
  </verify>

  <done>
    `src/pages/TarotPage.jsx` 파일이 존재하며 default export로 빈 컨테이너 컴포넌트를 노출한다. `currentPage` 상태 변수가 선언되어 Phase 3에서 `intro`/`shuffle`/`result` 상태 머신으로 확장 가능한 구조. placeholder 텍스트 한 줄 + 화면 중앙 정렬 레이아웃. 신규 의존성 0개. Phase 3 확장 포인트가 파일 상단 주석에 명시됨.
  </done>
</task>

<task type="auto">
  <name>Task 3: App.jsx 수정 — /tarot 라우트 추가 + Routes 바깥에 TabBar 형제 통합 + 회귀 검증 빌드</name>
  <read_first>
    - `src/App.jsx` (수정 대상 — 현재 4개 라우트 정의 확인)
    - `src/main.jsx` (Provider 체인 — TabBar는 ThemeProvider 안쪽에 위치하므로 App.jsx 안에서 import 시 모든 Provider에 접근 가능)
    - `src/components/TabBar.jsx` (Task 1 산출물 — default import)
    - `src/pages/TarotPage.jsx` (Task 2 산출물 — default import)
    - `.planning/phases/02-tab-bar-navigation-shell/02-CONTEXT.md` (D-01 /tarot 라우트 신규, D-02 Routes 바깥 outer shell, D-05 라우트별 숨김은 TabBar 내부에서 처리)
  </read_first>

  <files>src/App.jsx</files>

  <action>
**구현 절차:**

**Step 1 — App.jsx 수정 (D-01, D-02):**
```jsx
// src/App.jsx (수정 후 전체)
import { Route, Routes } from 'react-router-dom'
import HomePage from './pages/HomePage'
import SajuPage from './pages/SajuPage'
import NewYearPage from './pages/NewYearPage'
import AmuletPage from './pages/AmuletPage'
import TarotPage from './pages/TarotPage'
import TabBar from './components/TabBar'
import './App.css'

function App() {
  return (
    <>
      <Routes>
        <Route path="/" element={<HomePage />} />
        <Route path="/saju" element={<SajuPage />} />
        <Route path="/newyear" element={<NewYearPage />} />
        <Route path="/amulet" element={<AmuletPage />} />
        <Route path="/tarot" element={<TarotPage />} />
        <Route path="*" element={<HomePage />} />
      </Routes>
      <TabBar />
    </>
  )
}

export default App
```

**핵심 변경 사항:**
1. `import TarotPage from './pages/TarotPage'` 추가.
2. `import TabBar from './components/TabBar'` 추가.
3. `<Route path="/tarot" element={<TarotPage />} />` 추가 — `*` catch-all 위에 배치 (D-01).
4. `<Routes>`를 React Fragment(`<>...</>`)로 감싸고 `<TabBar />`를 `<Routes>`의 **형제(sibling)** 로 배치 (D-02 — outer shell).
5. **사용하지 않는 `Navigate` import 제거** — 현재 import되어 있으나 사용처가 없으므로 정리 (DRY, ESLint 경고 방지).

**Step 2 — 기존 라우트 정의 보호 (NAV-03 회귀 방지):**
- `<Route path="/" element={<HomePage />} />`, `<Route path="/saju" element={<SajuPage />} />`, `<Route path="/newyear" element={<NewYearPage />} />`, `<Route path="/amulet" element={<AmuletPage />} />`, `<Route path="*" element={<HomePage />} />` **5개 행을 변경하지 않는다.**
- 페이지 컴포넌트(`HomePage`, `SajuPage`, `NewYearPage`, `AmuletPage`) 자체는 본 플랜에서 수정 금지 (회귀 방지).

**Step 3 — 빌드 검증 (NAV-03):**
```bash
npm run build
```
- exit code 0 필수. 에러 발생 시 import path·JSX 구문 재검토 후 재시도.
- 빌드 성공이 곧 (a) JSX 유효성 (b) import resolution (c) AIT 빌드 산출물 호환성을 보장한다.

**Step 4 — 라우트별 숨김 동작 확인 (D-05, SC4):**
- 본 플랜은 자동화 테스트 인프라(Vitest 등) 부재 (`.planning/codebase/TESTING.md` 참고). 따라서 라우트별 숨김은 (a) Task 1 acceptance에서 코드 단계로 (`/saju`/`/newyear`/`/amulet`에서 `null` 반환) 검증, (b) 빌드 성공으로 통합 동작 확인.
- 추가 수동 테스트(`npm run dev` 후 라우트 클릭)는 본 페이즈 산출물 검증 책임이 아니라 페이즈 transition 단계에서 확인.

**Step 5 — 회귀 신호 가드:**
- `git diff src/pages/HomePage.jsx src/pages/SajuPage.jsx src/pages/NewYearPage.jsx src/pages/AmuletPage.jsx`가 빈 출력이어야 한다 (해당 파일들 무수정).
- `git diff src/main.jsx`가 빈 출력이어야 한다 (Provider 체인 무수정 — TabBar는 App.jsx에서만 통합).
- `git diff package.json package-lock.json pnpm-lock.yaml`가 빈 출력이어야 한다 (신규 의존성 0).
- `git diff granite.config.ts`가 빈 출력이어야 한다 (AIT 권한·빌드 설정 무수정 — 토스 심사 영향 없음).

**Step 6 — 신규 의존성 금지 재확인:**
본 task에서도 어떤 npm 패키지 추가 금지.
  </action>

  <acceptance_criteria>
    - **/tarot 라우트 등록:** `grep -c 'path="/tarot"' src/App.jsx` returns 1
    - **TarotPage import:** `grep -c "from './pages/TarotPage'" src/App.jsx` returns 1
    - **TabBar import:** `grep -c "from './components/TabBar'" src/App.jsx` returns 1
    - **TabBar는 Routes 바깥(D-02):** `<TabBar />` JSX는 `<Routes>` element 외부에 위치한다. 검증: `<Routes>` 내부에 `<TabBar` 문자열이 있으면 안 됨.
      - `awk '/<Routes>/,/<\/Routes>/' src/App.jsx | grep -c "TabBar"` returns 0
      - `grep -c "<TabBar" src/App.jsx` returns 1 이상 (Routes 바깥에 1회)
    - **기존 4개 라우트 보존(NAV-03):**
      - `grep -c 'path="/" element={<HomePage' src/App.jsx` returns 1
      - `grep -c 'path="/saju"' src/App.jsx` returns 1
      - `grep -c 'path="/newyear"' src/App.jsx` returns 1
      - `grep -c 'path="/amulet"' src/App.jsx` returns 1
      - `grep -c 'path="\*"' src/App.jsx` returns 1
    - **페이지 컴포넌트 무수정:** `git diff --name-only src/pages/HomePage.jsx src/pages/SajuPage.jsx src/pages/NewYearPage.jsx src/pages/AmuletPage.jsx | wc -l` returns 0
    - **Provider 체인 무수정:** `git diff --name-only src/main.jsx | wc -l` returns 0
    - **AIT 설정 무수정:** `git diff --name-only granite.config.ts | wc -l` returns 0
    - **package.json/lockfile 무수정 (신규 의존성 0):** `git diff --name-only package.json package-lock.json pnpm-lock.yaml | wc -l` returns 0
    - **빌드 성공:** `npm run build` exits 0
    - **신규 의존성 import 금지:**
      - `grep -c "lucide-react" src/App.jsx` returns 0
      - `grep -c "framer-motion" src/App.jsx` returns 0
  </acceptance_criteria>

  <verify>
    <automated>grep -c 'path="/tarot"' src/App.jsx && grep -c "from './pages/TarotPage'" src/App.jsx && grep -c "from './components/TabBar'" src/App.jsx && [ "$(awk '/<Routes>/,/<\/Routes>/' src/App.jsx | grep -c TabBar)" = "0" ] && [ "$(git diff --name-only -- src/pages/HomePage.jsx src/pages/SajuPage.jsx src/pages/NewYearPage.jsx src/pages/AmuletPage.jsx src/main.jsx granite.config.ts package.json package-lock.json pnpm-lock.yaml | wc -l | tr -d ' ')" = "0" ] && npm run build</automated>
  </verify>

  <done>
    `src/App.jsx`가 (a) `/tarot` 라우트를 catch-all 위에 추가했고, (b) `<TabBar />`를 `<Routes>` 바깥의 형제로 통합했으며, (c) 기존 4개 라우트(`/`, `/saju`, `/newyear`, `/amulet`, `*`) 정의를 변경 없이 보존했다. 페이지 컴포넌트(HomePage, SajuPage, NewYearPage, AmuletPage)와 `src/main.jsx` Provider 체인, `granite.config.ts`, `package.json`/lockfile은 모두 무수정 — NAV-03 회귀 방지. `npm run build`가 exit 0으로 통과해 AIT 빌드 산출물 호환성이 확인된다. 신규 npm 의존성 0개.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client navigation → React Router DOM | TabBar 클릭이 `useNavigate()`를 통해 클라이언트 라우팅을 트리거 — 외부 입력 없음, 신뢰 경계 내부 |
| build pipeline → AIT 빌드 산출물 | `npm run build` → `dist/` → `fortune-cat.ait` 패키지 — 토스 심사 영향 가능 |

본 페이즈는 인증·민감 데이터·외부 API·결제와 무관한 순수 UI 셸 작업. ASVS L1 기준 위험 매우 낮음.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `src/App.jsx` 라우트 정의 변경 | mitigate | `<Route path="/" element={<HomePage />} />`, `/saju`, `/newyear`, `/amulet`, `*` 5개 라우트 정의 무수정 검증 acceptance(NAV-03 회귀 방지). 페이지 컴포넌트 4개도 `git diff --name-only` 0행 검증. |
| T-02-02 | Tampering | `package.json`/lockfile 신규 의존성 도입 | mitigate | 모든 task의 acceptance에서 `git diff package.json package-lock.json pnpm-lock.yaml` 0행 검증. 코드 단계 grep으로 `lucide-react`/`framer-motion`/`clsx`/`tailwind-merge`/`class-variance-authority` import 0회 검증. AIT 심사·번들 영향 차단 (P1-D-08, D-08). |
| T-02-03 | Denial of Service | `granite.config.ts` 권한·설정 변경으로 토스 심사 차단 | mitigate | Task 3 acceptance에서 `git diff granite.config.ts` 0행 검증. 본 페이즈는 라우트 추가만, 권한 변경 무관. |
| T-02-04 | Information Disclosure | safe-area 처리 누락으로 iPhone 홈 인디케이터가 탭바 가림 | accept | D-06에 따라 TDS 기본 패턴 채택. Emotion 폴백 시 `padding-bottom: env(safe-area-inset-bottom)` 한 줄로 처리. 4050 사용자 시각 인지에 영향 가능하지만 v1.1 마일스톤 출시 후 사용자 피드백 기반 보정. 정보 노출 위험은 없음 (시각 가림만). |

</threat_model>

<verification>

## Phase Verification

본 플랜이 Phase 2 success criteria 4개를 모두 충족함을 확인한다.

### SC1 — 한 번의 탭으로 사주↔타로 전환 (NAV-01)
- Task 1: TabBar에 정확히 2개 탭(`'사주'` → `/`, `'타로'` → `/tarot`) 정의 검증.
- Task 1: `useNavigate()` import 검증 → 단일 클릭으로 라우트 전환 가능.
- Task 3: `/tarot` 라우트가 `App.jsx`에 등록됨을 검증.

### SC2 — active 시각 강조 (NAV-02)
- Task 1: `colors` from `@toss/tds-colors` 사용 검증 (active = 브랜드 컬러).
- Task 1: 아이콘 fill vs stroke 분기 (D-09) — 코드 inspection에서 active state 분기 로직 존재.

### SC3 — 기존 흐름 회귀 없음 (NAV-03)
- Task 3: 기존 4개 라우트 정의 무수정 acceptance.
- Task 3: HomePage/SajuPage/NewYearPage/AmuletPage 컴포넌트 무수정 acceptance.
- Task 3: `npm run build` 성공 (빌드 단계 회귀 없음).
- Task 3: `granite.config.ts` 무수정 (AIT 빌드 호환).

### SC4 — 풀스크린 흐름 가리지 않기 (D-05)
- Task 1: `VISIBLE_PATHS` 또는 동등 표현으로 `/saju`/`/newyear`/`/amulet`에서 `null` 반환 검증.
- 라우트별 단순 매칭만 사용 (페이지 내부 currentPage별 추가 규칙 없음 — D-05 정신 유지).

### 통합 검증
- `npm run build` exit 0 → 모든 task가 합쳐졌을 때 빌드 가능.
- `grep -E "lucide-react|framer-motion|clsx|tailwind-merge|class-variance-authority" src/components/TabBar.jsx src/pages/TarotPage.jsx src/App.jsx | wc -l` returns 0 → 신규 의존성 0개.

</verification>

<success_criteria>

본 플랜이 완료되면 다음이 모두 참이어야 한다.

1. **NAV-01 충족 (SC1):** `src/components/TabBar.jsx`가 사주/타로 두 탭을 정의하고, 클릭 시 `useNavigate()`로 즉시 `/` 또는 `/tarot`으로 전환한다. `App.jsx`에 두 라우트 모두 등록됨.

2. **NAV-02 충족 (SC2):** TabBar는 active 탭을 컬러(TDS 토큰) + 아이콘 fill로, inactive 탭을 grey + 아이콘 stroke로 구분한다 (D-09).

3. **NAV-03 충족 (SC3):** 기존 4개 라우트(`/`, `/saju`, `/newyear`, `/amulet`, `*`) 정의와 페이지 컴포넌트 4개, `src/main.jsx` Provider 체인, `granite.config.ts`가 모두 무수정. `npm run build` exit 0.

4. **SC4 충족 (D-05):** TabBar는 `/`와 `/tarot`에서만 렌더되고, `/saju`/`/newyear`/`/amulet`에서는 `null` 반환.

5. **D-08 충족 (신규 의존성 0):** `lucide-react`, `framer-motion`, `clsx`, `tailwind-merge`, `class-variance-authority` import 0회. `package.json`/lockfile 무수정.

6. **D-10 충족 (즉시 전환):** TabBar에 CSS `transition` 속성 미사용. `framer-motion` import 0회.

7. **D-04 충족 (단일 라우트 + currentPage 패턴):** `TarotPage.jsx`가 `currentPage` 상태 변수를 선언해 Phase 3 확장 포인트를 마련.

</success_criteria>

<output>
완료 후 `.planning/phases/02-tab-bar-navigation-shell/02-01-SUMMARY.md` 파일을 생성해 다음 항목을 기록한다:
- 산출물 파일 3개 경로
- TDS Tab vs Emotion 폴백 분기 결정 (D-07) + 사유
- active/inactive 컬러 토큰 선택 (D-09 + Discretion)
- 사주/타로 아이콘 SVG/TDS 출처 (D-08 + Discretion)
- `npm run build` 결과 (exit code, 빌드 시간)
- 회귀 가드 검증 결과 (`git diff` 무변경 파일 목록)
- Phase 3 확장 포인트 (TarotPage `currentPage` 상태 머신 위치)
</output>
