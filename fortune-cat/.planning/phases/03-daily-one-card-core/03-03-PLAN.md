---
phase: 03-daily-one-card-core
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - src/pages/TarotPage.jsx
autonomous: false
requirements: [TAROT-01, TAROT-02, TAROT-03]
requirements_addressed: [TAROT-01, TAROT-02, TAROT-03]
must_haves:
  truths:
    - "사용자가 /tarot 진입 시 22장 fetch 진행 중 Loader가 표시된다 (D-09)"
    - "사용자가 /tarot 진입 시 fetch 실패하면 에러 헤드라인 + 다시 시도 버튼이 표시되고 Sentry.captureException이 호출된다 (D-10)"
    - "사용자가 fetch 성공 후 intro 단계에서 카드 뒷면 preview + '오늘의 한 장 뽑기' CTA를 볼 수 있다 (TAROT-01 시작점)"
    - "사용자가 intro CTA 탭 시 shuffle 단계로 전환되어 부채꼴 3장(매번 다른 22장 중 랜덤 3장 — 중복 없음)을 본다 (D-01, D-03)"
    - "사용자가 shuffle에서 1장 탭 시 그 자리에서 0.5s 뒤집기 애니메이션 후 result 단계로 진입한다 (TAROT-01)"
    - "사용자가 result에서 카드 이미지·한국어명/영문명·키워드 chip·메시지 본문을 한 화면에서 본다 (TAROT-02)"
    - "사용자가 result에서 fixed '다시 뽑기' 버튼을 탭하면 shuffle 단계로 돌아가 새 3장 부채꼴을 본다 (TAROT-03 + D-04 무제한)"
    - "사용자가 다시 뽑기 5회 반복 시 매번 다른 카드 조합이 노출된다 (D-04 + Pitfall 6 reshuffle 누락 회피)"
    - "TarotPage 마운트 직후 prefetchAllCardImages()가 1회 호출되어 첫 result 표시 시 깜빡임이 없다 (Pitfall 2)"
    - "useEffect cleanup이 cancelled 플래그로 unmount 후 setState warning을 회피한다 (Pitfall 7)"
  artifacts:
    - path: "src/pages/TarotPage.jsx"
      provides: "intro→shuffle→result 상태 머신 + 22장 fetch + Pitfall 2/6/7 회피"
      contains: "fetchTarotCards|TarotShuffle|TarotResult|prefetchAllCardImages"
      min_lines: 150
  key_links:
    - from: "src/pages/TarotPage.jsx useEffect"
      to: "src/lib/supabase.js fetchTarotCards"
      via: "intro 마운트 시 1회 호출 (Pitfall 7 cancelled flag)"
      pattern: "fetchTarotCards\\(\\)"
    - from: "src/pages/TarotPage.jsx"
      to: "src/components/TarotShuffle.jsx"
      via: "currentPage === 'shuffle' 분기 + cards/onSelect props"
      pattern: "<TarotShuffle"
    - from: "src/pages/TarotPage.jsx"
      to: "src/components/TarotResult.jsx"
      via: "currentPage === 'result' 분기 + card/onRedraw props"
      pattern: "<TarotResult"
    - from: "TarotPage handleRedraw"
      to: "startShuffle (새 3장 reshuffle)"
      via: "Pitfall 6 회피 — pickThreeRandom 매번 호출"
      pattern: "handleRedraw.*startShuffle|startShuffle.*pickThreeRandom"
    - from: "TarotPage useEffect mount"
      to: "src/assets/images/cards prefetchAllCardImages"
      via: "Pitfall 2 회피 — 22장 사전 다운로드"
      pattern: "prefetchAllCardImages\\(\\)"
---

<objective>
Phase 3 Wave 3 — 통합 + 검증. Phase 2가 만든 빈 `TarotPage.jsx`를 currentPage 상태 머신(intro → shuffle → result)으로 확장하고, Wave 1 데이터 레이어(fetchTarotCards, getCardImageUrl, prefetchAllCardImages)와 Wave 2 UI 컴포넌트(TarotCardArt, TarotShuffle, TarotResult)를 결합한다. 마지막으로 UI-SPEC Checker Sign-Off 6 dimensions + 수동 디바이스 검증으로 Phase 3 4개 Success Criteria가 실제로 동작함을 입증한다.

Purpose: Wave 1 데이터 + Wave 2 UI = TAROT-01/02/03 사용자 흐름 일순 가능. Phase 4(광고 게이팅)·Phase 5(Analytics·공유)가 본 Wave가 잠근 상태 머신·핸들러 위에 추가됨.

Output:
- `src/pages/TarotPage.jsx` — Phase 2 placeholder를 4 useState + 22장 fetch + 3단계 라우팅 + intro/Loading/Error inline + Pitfall 2/6/7 회피 코드로 확장
- UI-SPEC Checker Sign-Off 6 dimensions 통과 (03-UI-SPEC.md Sign-Off 체크리스트)
- 수동 디바이스 검증 1순회 (intro → shuffle → flip → result → 다시뽑기 5회 + 에러/로딩 시뮬레이션)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-daily-one-card-core/03-CONTEXT.md
@.planning/phases/03-daily-one-card-core/03-RESEARCH.md
@.planning/phases/03-daily-one-card-core/03-UI-SPEC.md
@.planning/phases/03-daily-one-card-core/03-VALIDATION.md
@.planning/phases/03-daily-one-card-core/03-01-SUMMARY.md
@.planning/phases/03-daily-one-card-core/03-02-SUMMARY.md

<interfaces>
<!-- Wave 1 인터페이스 (본 plan이 사용) -->

From src/lib/supabase.js (Wave 1):
```javascript
export async function fetchTarotCards(): Promise<Array<{
  id: number, name_ko: string, name_en: string, emoji: string,
  image_path: string, keywords: string[], message: string
}>>
// 실패 시 throw error → 호출 측에서 try/catch + Sentry.captureException
```

From src/assets/images/cards/index.js (Wave 1):
```javascript
export function getCardImageUrl(id: number): string
export function prefetchAllCardImages(): void
```

<!-- Wave 2 인터페이스 (본 plan이 사용) -->

From src/components/TarotShuffle.jsx (Wave 2):
- default export TarotShuffle({ cards, onSelect })
- cards: 정확히 3장 객체 배열
- onSelect: (id: number) => void  // 0.7s 후 호출

From src/components/TarotResult.jsx (Wave 2):
- default export TarotResult({ card, onRedraw })
- card: 7컬럼 객체
- onRedraw: () => void  // 즉시 호출 (Phase 3는 광고 없음)

From src/components/TarotCardArt.jsx (Wave 2):
- default export TarotCardArt({ emoji, nameEn, image, faceUp, size, framed })
- intro 단계에서 카드 뒷면 preview용으로 직접 사용 (size="lg")
</interfaces>

<reference_files>
- src/pages/TarotPage.jsx (Phase 2 placeholder — currentPage useState + 빈 컨테이너만 존재. 본 plan이 확장)
- src/pages/HomePage.jsx (라인 88-130, 307-352 — Promise.all + setHasError + Loader + 에러 + 재시도 버튼 패턴 정본)
- src/pages/SajuPage.jsx (currentPage 상태 머신 패턴 참고 — 다단계 화면 전환)
- src/pages/AmuletPage.jsx (currentPage 상태 머신 + 에러 처리 + Sentry 패턴)
- src/components/Loading.jsx (Sentry.captureException 사용 패턴 — `extra: { ... }` 메타 포함)
- src/index.css (CSS 변수 토큰 + prefers-reduced-motion 글로벌)
</reference_files>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 클라이언트 → Supabase fetch | Wave 1 fetchTarotCards가 throw하면 본 Wave가 try/catch + Sentry로 처리 |
| 컴포넌트 props ↔ TarotPage state | useState 4종(currentPage, cardsData, shuffledThree, selectedCardId)이 진실의 원천 |
| useEffect cleanup → setState | unmount 후 setState 시도 차단 (cancelled flag) |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | Information Disclosure | Sentry.captureException 메타에 PII | mitigate | extra에 `{ phase: 'tarot_intro_fetch' }` 정도만 포함 — 사용자 입력 없음 (D-09 fetch는 인증 불필요). PII 컬럼 보존 안 함. acceptance criteria로 grep 검증 |
| T-03-12 | Tampering | unmount 후 setState로 인한 React state 오염 | mitigate | Pitfall 7 cancelled 플래그 패턴 + cleanup. acceptance criteria로 코드 패턴 검증 |
| T-03-13 | Denial of Service | useEffect 무한 루프 (deps 미설정) | mitigate | useEffect deps 빈 배열 `[]` 명시 — mount 1회만. retry는 별도 함수로 분리해 sufficient state로 트리거 |
| T-03-14 | Tampering | 잘못된 selectedCardId로 cards.find 실패 | mitigate | TarotResult가 `if (!card) return null` 가드 (Wave 2 검증) + 본 Wave에서 `cardsData.find(c => c.id === selectedCardId)` 결과를 prop 전달 |
| T-03-15 | Information Disclosure | 22장 카드 메시지에 secret/PII 노출 | accept | 마이그레이션 시드(Wave 1)는 사용자 노출 컨텐츠 — secret 아님 |

**보안 게이트:** T-03-11, T-03-12, T-03-13 mitigation은 acceptance criteria로 자동 검증.
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 3.1: TarotPage.jsx 확장 — 4 useState + 22장 fetch + 3단계 라우팅 + intro/Loading/Error</name>
  <files>src/pages/TarotPage.jsx</files>
  <read_first>
    - src/pages/TarotPage.jsx (현재 Phase 2 placeholder — currentPage useState만 존재. 본 task가 확장 대상)
    - .planning/phases/03-daily-one-card-core/03-CONTEXT.md (D-01~D-11 전체 — 특히 D-03 매 세션 새 3장, D-04 다시 뽑기 = 새 셔플, D-09 fetch 시점, D-10 에러 처리)
    - .planning/phases/03-daily-one-card-core/03-RESEARCH.md (Pattern 1 TarotPage 스켈레톤 + Pattern 4 Fisher-Yates + §5 다시 뽑기 핸들러 + Pitfall 2/5/6/7 모두)
    - .planning/phases/03-daily-one-card-core/03-UI-SPEC.md (Layout intro 단계 + Loading & Error States 섹션 + Copywriting intro 카피 정본)
    - src/pages/HomePage.jsx (라인 88-130, 307-352 — Promise.all + setHasError + Loader + 에러 + 재시도 패턴)
    - src/components/Loading.jsx (Sentry.captureException 사용 패턴 — `extra: { ... }` 메타)
    - src/components/TarotShuffle.jsx (Wave 2 — props 시그니처 정확 import)
    - src/components/TarotResult.jsx (Wave 2 — props 시그니처 정확 import)
    - src/components/TarotCardArt.jsx (Wave 2 — intro 카드 뒷면 preview용 size="lg" 호출)
    - src/lib/supabase.js (Wave 1 — fetchTarotCards 시그니처)
    - src/assets/images/cards/index.js (Wave 1 — prefetchAllCardImages 시그니처)
  </read_first>
  <action>
**기존 `src/pages/TarotPage.jsx`를 다음 정확한 코드로 완전 교체** (Phase 2 placeholder는 본 Wave가 확장한다고 명시되어 있음 — Phase 2 P2-D-04 carry-forward).

```jsx
// /tarot 라우트 — 데일리 원카드 코어 화면 (Phase 3).
// 상태 머신: 'intro' → 'shuffle' → 'result' → '다시 뽑기' → 'shuffle' (D-04 무제한, Phase 4가 광고로 wrap).
//
// CONTEXT D-09: intro 진입 시 22장 fetch 1회 → cardsData state 캐시 → shuffle/result 0 latency.
// CONTEXT D-03: 매 shuffle 진입 시 22장 중 새 랜덤 3장 (중복 없음).
// CONTEXT D-10: fetch 실패 → Loader → 에러 헤드라인 + 재시도 버튼 + Sentry.captureException.
// RESEARCH Pitfall 2: intro 마운트 직후 prefetchAllCardImages() 1회 — 첫 result 깜빡임 회피.
// RESEARCH Pitfall 6: handleRedraw가 startShuffle 호출 → pickThreeRandom 매번 재실행 (같은 3장 재현 회피).
// RESEARCH Pitfall 7: useEffect cleanup cancelled 플래그 — unmount 후 setState 차단.
// RESEARCH Pitfall 5: 본 페이즈는 useState 4종 유지. Phase 4·5 진입 시점에 Context 승격 결정.

import { useEffect, useState } from 'react';
import { Loader } from '@toss/tds-mobile';
import * as Sentry from '@sentry/react';
import { fetchTarotCards } from '../lib/supabase';
import { getCardImageUrl, prefetchAllCardImages } from '../assets/images/cards';
import TarotCardArt from '../components/TarotCardArt';
import TarotShuffle from '../components/TarotShuffle';
import TarotResult from '../components/TarotResult';

// Fisher-Yates 셔플 + 상위 3장 (RESEARCH Pattern 4).
// 22장 중복 없는 랜덤 3장. 원본 cardsData 변경 금지 (slice() shallow copy).
function pickThreeRandom(cards) {
  if (!Array.isArray(cards) || cards.length < 3) return [];
  const arr = cards.slice();
  for (let i = arr.length - 1; i > 0; i--) {
    const j = Math.floor(Math.random() * (i + 1));
    [arr[i], arr[j]] = [arr[j], arr[i]];
  }
  return arr.slice(0, 3);
}

export default function TarotPage() {
  const [currentPage, setCurrentPage] = useState('intro');     // 'intro' | 'shuffle' | 'result'
  const [cardsData, setCardsData] = useState([]);              // 22장 전체 (intro fetch 결과)
  const [shuffledThree, setShuffledThree] = useState([]);      // 매 shuffle 진입 시 3장
  const [selectedCardId, setSelectedCardId] = useState(null);  // result 단계 카드 id
  const [isLoading, setIsLoading] = useState(true);            // intro fetch 진행 상태
  const [errorState, setErrorState] = useState(null);          // 'fetch_failed' | null
  const [retryNonce, setRetryNonce] = useState(0);             // 재시도 트리거 (deps에 포함)

  // intro fetch — D-09 1회 호출. retryNonce 변경 시 재시도. cleanup cancelled flag (Pitfall 7).
  useEffect(() => {
    let cancelled = false;
    async function load() {
      try {
        setIsLoading(true);
        setErrorState(null);
        const data = await fetchTarotCards();
        if (cancelled) return;
        if (!Array.isArray(data) || data.length < 3) {
          // RLS 정책 누락 또는 시드 미적용 케이스 (Pitfall 3) — 에러로 취급
          throw new Error(`tarot_cards 데이터 부족: ${data?.length ?? 0}행`);
        }
        setCardsData(data);
        // Pitfall 2 회피: 22장 webp prefetch (intro 단계 사용자 헤드라인 읽는 시간 동안 다운로드)
        prefetchAllCardImages();
      } catch (err) {
        if (cancelled) return;
        console.error('[TarotPage] tarot_cards fetch 실패:', err);
        Sentry.captureException(err, { extra: { phase: 'tarot_intro_fetch' } });
        setErrorState('fetch_failed');
      } finally {
        if (!cancelled) setIsLoading(false);
      }
    }
    load();
    return () => { cancelled = true; };
  }, [retryNonce]);

  const startShuffle = () => {
    // Pitfall 6 회피: 매번 새 pickThreeRandom 호출
    setShuffledThree(pickThreeRandom(cardsData));
    setSelectedCardId(null);
    setCurrentPage('shuffle');
  };

  const handleSelectCard = (id) => {
    setSelectedCardId(id);
    setCurrentPage('result');
  };

  // D-04: 다시 뽑기 = intro 건너뛰고 즉시 새 셔플. Phase 4가 본 핸들러 안쪽에 광고 wrap.
  const handleRedraw = () => {
    startShuffle();
  };

  const handleRetry = () => {
    setRetryNonce((n) => n + 1);
  };

  const selectedCard = selectedCardId !== null
    ? cardsData.find((c) => c.id === selectedCardId)
    : null;

  // 로딩 상태 (UI-SPEC Loading & Error States)
  if (isLoading) {
    return (
      <div
        role="status"
        aria-live="polite"
        style={{
          minHeight: '100vh',
          background: '#FFFFFF',
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          justifyContent: 'center',
          padding: '60px 20px',
          gap: 16,
        }}
        data-current-page={currentPage}
      >
        <Loader />
        <p style={{ fontSize: 14, fontWeight: 700, lineHeight: 1.4, color: '#6B7684', margin: 0 }}>
          카드를 준비하고 있어요
        </p>
      </div>
    );
  }

  // 에러 상태 (UI-SPEC Loading & Error States + D-10)
  if (errorState === 'fetch_failed') {
    return (
      <div
        role="alert"
        style={{
          minHeight: '100vh',
          background: '#FFFFFF',
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          justifyContent: 'center',
          padding: '60px 20px',
        }}
        data-current-page={currentPage}
      >
        <h2 style={{ fontSize: 20, fontWeight: 700, lineHeight: 1.3, color: '#191F28', margin: 0, textAlign: 'center' }}>
          카드 데이터를<br />불러오지 못했어요
        </h2>
        <p style={{ marginTop: 8, fontSize: 16, fontWeight: 400, lineHeight: 1.6, color: '#6B7684', margin: 0, textAlign: 'center' }}>
          잠시 후 다시 시도해주세요
        </p>
        <button
          type="button"
          onClick={handleRetry}
          className="tap-card"
          style={{
            marginTop: 24,
            padding: '14px 24px',
            fontSize: 16,
            fontWeight: 700,
            color: '#FFFFFF',
            background: '#64119F',
            border: 0,
            borderRadius: 12,
            cursor: 'pointer',
          }}
        >
          다시 시도하기
        </button>
      </div>
    );
  }

  // 정상 흐름 — currentPage 라우팅
  return (
    <div data-current-page={currentPage}>
      {currentPage === 'intro' && (
        <TarotIntro onStart={startShuffle} />
      )}
      {currentPage === 'shuffle' && (
        <TarotShuffle cards={shuffledThree} onSelect={handleSelectCard} />
      )}
      {currentPage === 'result' && selectedCard && (
        <TarotResult card={selectedCard} onRedraw={handleRedraw} />
      )}
    </div>
  );
}

// intro 단계 — TarotPage 내부 인라인 컴포넌트 (단일 책임 + 별도 파일 분리 비용 회피).
// UI-SPEC Layout intro: 카드 뒷면 lg preview + 32px gap + 헤드라인 + 16px gap + 부제 + 32px gap + CTA.
// UI-SPEC Copywriting: "오늘의 한 장을 만나보세요" / "카드를 한 장 뒤집어 오늘의 메시지를 받아보세요" / "오늘의 한 장 뽑기".
function TarotIntro({ onStart }) {
  return (
    <div
      style={{
        minHeight: '100vh',
        background: '#FFFFFF',
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        justifyContent: 'center',
        padding: '24px 16px calc(96px + env(safe-area-inset-bottom))',
        gap: 0,
      }}
    >
      <TarotCardArt faceUp={false} size="lg" framed={false} />

      <h1 style={{ marginTop: 32, fontSize: 24, fontWeight: 700, lineHeight: 1.3, color: '#191F28', margin: 0, textAlign: 'center' }}>
        오늘의 한 장을<br />만나보세요
      </h1>

      <p style={{ marginTop: 16, fontSize: 16, fontWeight: 400, lineHeight: 1.6, color: '#6B7684', margin: 0, textAlign: 'center' }}>
        카드를 한 장 뒤집어<br />오늘의 메시지를 받아보세요
      </p>

      <button
        type="button"
        onClick={onStart}
        className="tap-card"
        style={{
          marginTop: 32,
          width: '100%',
          maxWidth: 320,
          height: 56,
          fontSize: 16,
          fontWeight: 700,
          color: '#FFFFFF',
          background: '#64119F',
          border: 0,
          borderRadius: 12,
          cursor: 'pointer',
        }}
      >
        오늘의 한 장 뽑기
      </button>
    </div>
  );
}
```

**중요:**
- Phase 2 placeholder를 완전 교체 (`useState` import도 `useEffect, useState`로 확장)
- 4 useState만 사용 (currentPage, cardsData, shuffledThree, selectedCardId) + 보조 3종(isLoading, errorState, retryNonce) — Pitfall 5에서 useState 유지 명시 (Phase 4·5에서 Context 승격 결정)
- TarotIntro는 동일 파일 안에 인라인 컴포넌트로 작성 (UI-SPEC Component Inventory "옵션 — TarotPage에서 inline 가능, 200줄 초과 시 분리" 명시 — 본 task에서는 inline 채택)
- pickThreeRandom 함수는 module 레벨 (TarotPage 외부) — DRY + 재테스트 용이
- prefetchAllCardImages는 fetch 성공 직후 호출 — Pitfall 2 회피
- useEffect deps `[retryNonce]` — 재시도 트리거. 빈 배열 `[]` 사용 시 retry 안 동작
- `data?.length ?? 0 < 3` 가드 — RLS 누락(Pitfall 3) + 시드 미적용 케이스를 fetch_failed로 자연 변환
- Sentry.captureException 메타에 PII 미포함 — `{ phase: 'tarot_intro_fetch' }` 정도만 (T-03-11 mitigation)
- v1.0 페이지/컴포넌트 import·수정 금지
- granite.config.ts·package.json·lock 파일 변경 금지 (D-02)
- copywriting 정본:
  - intro 헤드라인 "오늘의 한 장을 만나보세요"
  - intro 부제 "카드를 한 장 뒤집어 오늘의 메시지를 받아보세요"
  - intro CTA "오늘의 한 장 뽑기"
  - 로딩 캡션 "카드를 준비하고 있어요"
  - 에러 헤드라인 "카드 데이터를 불러오지 못했어요"
  - 에러 보조 "잠시 후 다시 시도해주세요"
  - 에러 CTA "다시 시도하기"
- aria 속성: 로딩 `role="status" aria-live="polite"`, 에러 `role="alert"`
- weight 400/700만 사용 (UI-SPEC Dimension 4)
  </action>
  <verify>
    <automated>cd /Users/trinity/Projects/app-in-toss/fortune-cat && grep -c "export default function TarotPage" src/pages/TarotPage.jsx && grep -c "import { fetchTarotCards }" src/pages/TarotPage.jsx && grep -c "import { getCardImageUrl, prefetchAllCardImages }" src/pages/TarotPage.jsx && grep -c "import TarotShuffle" src/pages/TarotPage.jsx && grep -c "import TarotResult" src/pages/TarotPage.jsx && grep -c "import TarotCardArt" src/pages/TarotPage.jsx && grep -c "import \\* as Sentry" src/pages/TarotPage.jsx && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - TarotPage default export: `grep -c "export default function TarotPage" src/pages/TarotPage.jsx` = 1
    - 4 useState 명시: `grep -cE "useState\\((['\"]intro['\"]|\\[\\]|null|true|0)" src/pages/TarotPage.jsx` ≥ 7 (currentPage·cardsData·shuffledThree·selectedCardId·isLoading·errorState·retryNonce)
    - fetchTarotCards import: `grep -c "import { fetchTarotCards } from '../lib/supabase'" src/pages/TarotPage.jsx` = 1
    - prefetchAllCardImages import: `grep -c "import { getCardImageUrl, prefetchAllCardImages } from '../assets/images/cards'" src/pages/TarotPage.jsx` = 1
    - Sentry import: `grep -c "import \\* as Sentry from '@sentry/react'" src/pages/TarotPage.jsx` = 1
    - TDS Loader import: `grep -c "import { Loader } from '@toss/tds-mobile'" src/pages/TarotPage.jsx` = 1
    - TarotShuffle import: `grep -c "import TarotShuffle from '../components/TarotShuffle'" src/pages/TarotPage.jsx` = 1
    - TarotResult import: `grep -c "import TarotResult from '../components/TarotResult'" src/pages/TarotPage.jsx` = 1
    - TarotCardArt import: `grep -c "import TarotCardArt from '../components/TarotCardArt'" src/pages/TarotPage.jsx` = 1
    - useEffect cleanup cancelled flag (Pitfall 7): `grep -c "let cancelled = false" src/pages/TarotPage.jsx` = 1 그리고 `grep -c "if (cancelled) return" src/pages/TarotPage.jsx` ≥ 2 그리고 `grep -c "cancelled = true" src/pages/TarotPage.jsx` ≥ 1
    - useEffect deps [retryNonce]: `grep -cE "\\}, \\[retryNonce\\]\\)" src/pages/TarotPage.jsx` = 1
    - Pitfall 6 회피 (handleRedraw → startShuffle): `grep -c "const handleRedraw = " src/pages/TarotPage.jsx` = 1 그리고 `grep -c "startShuffle()" src/pages/TarotPage.jsx` ≥ 2
    - Pitfall 2 회피 (prefetch 호출): `grep -c "prefetchAllCardImages()" src/pages/TarotPage.jsx` = 1
    - Fisher-Yates 함수 정의: `grep -c "function pickThreeRandom" src/pages/TarotPage.jsx` = 1
    - Sentry.captureException 호출: `grep -c "Sentry.captureException(err" src/pages/TarotPage.jsx` = 1
    - Sentry 메타에 PII 미포함 (T-03-11): `grep -c "extra: { phase: 'tarot_intro_fetch' }" src/pages/TarotPage.jsx` = 1
    - 3단계 라우팅 분기: `grep -cE "currentPage === '(intro|shuffle|result)'" src/pages/TarotPage.jsx` = 3
    - copywriting intro 헤드라인: `grep -c "오늘의 한 장을" src/pages/TarotPage.jsx` ≥ 1
    - copywriting intro CTA: `grep -c "오늘의 한 장 뽑기" src/pages/TarotPage.jsx` = 1
    - copywriting 로딩 캡션: `grep -c "카드를 준비하고 있어요" src/pages/TarotPage.jsx` = 1
    - copywriting 에러 헤드라인: `grep -c "카드 데이터를" src/pages/TarotPage.jsx` ≥ 1 그리고 `grep -c "불러오지 못했어요" src/pages/TarotPage.jsx` = 1
    - copywriting 에러 CTA: `grep -c "다시 시도하기" src/pages/TarotPage.jsx` = 1
    - aria 로딩 status: `grep -c "role=\"status\"" src/pages/TarotPage.jsx` = 1 그리고 `grep -c "aria-live=\"polite\"" src/pages/TarotPage.jsx` = 1
    - aria 에러 alert: `grep -c "role=\"alert\"" src/pages/TarotPage.jsx` = 1
    - 금지 weight 미사용: `grep -cE "fontWeight: (500|600|800|900)" src/pages/TarotPage.jsx` = 0
    - 금지 의존성 미사용: `grep -cE "(framer-motion|zustand|@emotion/react)" src/pages/TarotPage.jsx` = 0
    - dangerouslySetInnerHTML 미사용: `grep -c "dangerouslySetInnerHTML" src/pages/TarotPage.jsx` = 0
    - v1.0 페이지 import 금지: `grep -cE "from '\\./(SajuPage|AmuletPage|HomePage|NewYearPage)'" src/pages/TarotPage.jsx` = 0
    - v1.0 컴포넌트 import 금지: `grep -cE "from '\\.\\./components/(Result|TabBar|Loading|BirthdateInput|UserInfoInput|TossLogin)'" src/pages/TarotPage.jsx` = 0
    - 본 task가 변경한 파일 외 미수정: `git diff --name-only` 출력에 `src/pages/TarotPage.jsx` 외 항목 없음 (단 .planning/* 제외)
    - granite.config·package·lock 미변경: `git diff --stat granite.config.ts package.json package-lock.json pnpm-lock.yaml` 출력 없음
    - ESLint 통과: `npm run lint` exit 0
    - Vite build 통과: `npm run build` exit 0
  </acceptance_criteria>
  <done>
    파일 작성 완료, 위 acceptance_criteria 31개 모두 PASS, lint/build 통과.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3.2: UI-SPEC Checker Sign-Off 6 dimensions 검수</name>
  <files>.planning/phases/03-daily-one-card-core/03-UI-SPEC.md (체크리스트 ☑ 업데이트), src/pages/TarotPage.jsx + src/components/Tarot*.jsx (검수 대상 — 변경 없음)</files>
  <read_first>
    - .planning/phases/03-daily-one-card-core/03-UI-SPEC.md (§Checker Sign-Off 6 dimensions 체크리스트 정본)
    - .planning/phases/03-daily-one-card-core/03-VALIDATION.md (§Per-Task Verification Map)
  </read_first>
  <action>
이 task는 "checkpoint:human-verify" 타입 — Claude는 자동화된 일부(grep/build 검증)는 실행하지만, 시각·인지 영역(폰트 weight 시각 확인, 색상 정확값, 부채꼴 각도 시각 일치, 4050 가독성 평가)은 사람이 브라우저/시뮬레이터에서 직접 검수해야 한다.

사용자가 아래 `<how-to-verify>` 절차에 따라 6 dimensions 모두 검수한 뒤 03-UI-SPEC.md §Checker Sign-Off 6개 체크박스를 ☑로 업데이트하고 resume-signal을 입력하면 다음 task(3.3)로 진행한다.
  </action>
  <what-built>
    Wave 1·2·3 통합 결과로 `/tarot` 라우트가 데일리 원카드 흐름을 일순한다. 본 checkpoint는 03-UI-SPEC.md 마지막 §Checker Sign-Off 6 dimensions 체크리스트를 사용자(또는 디자인 리뷰어)가 직접 검수하는 단계.
  </what-built>
  <how-to-verify>
    1. `npm run dev` 실행 후 브라우저에서 `http://localhost:<port>/tarot` 접속 (또는 AIT 시뮬레이터)
    2. 03-UI-SPEC.md를 열고 §Checker Sign-Off 섹션의 6 dimensions를 한 항목씩 검수:

       **Dimension 1 — Copywriting:**
       - intro 헤드라인 "오늘의 한 장을 만나보세요" 표시 확인
       - intro 부제 "카드를 한 장 뒤집어 오늘의 메시지를 받아보세요" 표시 확인
       - intro CTA "오늘의 한 장 뽑기" 표시 확인
       - shuffle 헤드라인 "세 장 중 한 장을 골라보세요" 표시 확인
       - shuffle 보조 "탭하면 그 자리에서 뒤집혀요" 표시 확인
       - result 메타 "오늘의 한 장" 표시 확인
       - result 헤더 `이모지 한국어명 (영문명)` 형식 확인 (예: `✨ 별 (The Star)`)
       - result CTA "다시 뽑기" 표시 확인
       - 로딩 캡션 "카드를 준비하고 있어요" 표시 확인 (DevTools Network throttling Slow 3G로 재현)
       - 에러 헤드라인/보조/CTA 표시 확인 (DevTools Network throttling Offline → /tarot 진입)

       **Dimension 2 — Visuals:**
       - intro: 카드 뒷면 preview lg(200×300) 중앙 정렬, 32px gap 후 헤드라인, CTA 100% 폭 56px 높이 확인
       - shuffle: 부채꼴 3장 좌(-15° translateX -64) 중(0°) 우(+15° translateX +64), md 사이즈 120×180 확인
       - result: 카드 lg framed(매트 cream→primary-light), 24px gap 후 헤더, 16px gap 후 chip, 24px gap 후 메시지 카드 확인
       - 다시 뽑기 버튼이 탭바 위 8px gap에 fixed로 떠 있는지 확인 (탭바를 가리지 않음)
       - 카드 뒤집기 시 0.5s ease-out 단일 회전, 양면 동시 표시 잔상 없음 (Pitfall 1 검증)

       **Dimension 3 — Color (60/30/10 + Reserved-for):**
       - 모든 단계 배경 #FFFFFF 확인
       - 메시지 카드/chip 배경 #F4E6FF (primary-light) 확인
       - intro CTA, result CTA 배경, chip 텍스트, 영문명 텍스트 모두 #64119F (primary) 확인
       - 그 외 위치에 #64119F 미사용 확인 (테두리, 일반 아이콘 등)
       - WCAG AA contrast: 본문 텍스트가 배경 대비 4.5:1 이상 (Chrome DevTools Accessibility Pane)

       **Dimension 4 — Typography (400/700 통일):**
       - 본 페이즈에 weight 500/600/800/900 일체 사용 안 함 — DevTools Computed font-weight 확인
       - intro 헤드라인 24px / 700 / line-height 1.3
       - intro CTA 라벨 16px / 700
       - shuffle 헤드라인 20px / 700 / 1.3
       - shuffle 보조 16px / 400 / 1.6
       - result 헤더 24px / 700
       - result chip 14px / 700
       - result 메시지 16px / 400 / 1.6 (4050 가독성)
       - result 다시 뽑기 16px / 700
       - 로딩 캡션 14px / 700, 에러 헤드라인 20px / 700, 에러 보조 16px / 400, 에러 CTA 16px / 700

       **Dimension 5 — Spacing (8-point + exceptions):**
       - intro: 카드↔헤드라인 32px, 헤드라인↔부제 16px, 부제↔CTA 32px
       - shuffle: 헤드라인↔보조 16px, 보조↔부채꼴 48px
       - result: 카드↔헤더 24px, 헤더↔chip 16px, chip↔메시지 24px, 메시지↔fixed 96px spacer
       - exception: 다시 뽑기 fixed 버튼 bottom = `calc(72px + env(safe-area-inset-bottom) + 8px)` (DevTools Computed bottom 검증)
       - exception: 부채꼴 좌/우 카드 translateX ±64px (DevTools transform 검증)

       **Dimension 6 — Registry Safety:**
       - `package.json` 비교: Wave 0 commit 대비 신규 의존성 0건 (`git diff package.json package-lock.json pnpm-lock.yaml` 출력 없음)
       - shadcn 미사용 (`components.json` 미존재 — `ls components.json` 결과 없음)
       - 신규 SVG 파일 0건 (`git diff --name-only --diff-filter=A | grep -E '\\.svg$' | wc -l` = 0)

    3. 6 dimensions 모두 PASS면 03-UI-SPEC.md 하단 §Checker Sign-Off 6개 체크박스 ☑ 처리 + Approval: pending → approved.
    4. 실패 항목 발견 시 어느 dimension에서 어떻게 어긋났는지 메모 후 plan을 revision으로 돌려보냄 (Wave 2 또는 Wave 3 task 수정).
  </how-to-verify>
  <verify>
    <automated>grep -cE "fontWeight: (500|600|800|900)" src/pages/TarotPage.jsx src/components/TarotCardArt.jsx src/components/TarotShuffle.jsx src/components/TarotResult.jsx | grep -v ':0' | wc -l && git diff --stat package.json package-lock.json pnpm-lock.yaml granite.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - Dimension 1 (Copywriting) 10개 항목 모두 PASS
    - Dimension 2 (Visuals) 5개 항목 모두 PASS
    - Dimension 3 (Color 60/30/10 + Reserved-for) 5개 항목 모두 PASS
    - Dimension 4 (Typography 400/700 통일) — `grep -cE "fontWeight: (500|600|800|900)" src/pages/TarotPage.jsx src/components/TarotCardArt.jsx src/components/TarotShuffle.jsx src/components/TarotResult.jsx` 모두 0
    - Dimension 5 (Spacing 8-point + exceptions) 5개 항목 모두 PASS (DevTools Computed 검증)
    - Dimension 6 (Registry Safety) — `git diff --stat package.json package-lock.json pnpm-lock.yaml` 출력 없음 + `ls components.json 2>/dev/null` 빈 출력 + 신규 SVG 0건
    - 03-UI-SPEC.md §Checker Sign-Off 6개 체크박스 모두 ☑ 처리 + Approval: pending → approved
  </acceptance_criteria>
  <done>
    UI-SPEC Checker Sign-Off 6 dimensions 모두 PASS, 03-UI-SPEC.md 체크리스트 업데이트 완료, Phase 3 시각 계약 충족.
  </done>
  <resume-signal>
    "approved" 또는 "통과" 입력 + 6 dimensions 체크 완료 캡처. 실패 시 dimension/항목 명시.
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3.3: 수동 디바이스 검증 — TAROT-01/02/03 + Pitfall 시나리오 일순</name>
  <files>.planning/phases/03-daily-one-card-core/03-VALIDATION.md (status 컬럼 ⬜→✅ 업데이트), src/pages/TarotPage.jsx + src/components/Tarot*.jsx (검수 대상 — 변경 없음)</files>
  <read_first>
    - .planning/phases/03-daily-one-card-core/03-VALIDATION.md (§Manual-Only Verifications + §Per-Task Verification Map 정본)
    - .planning/REQUIREMENTS.md (TAROT-01/02/03 + NAV-03 회귀 방지 정의)
    - .planning/ROADMAP.md (Phase 3 4 Success Criteria)
  </read_first>
  <action>
이 task는 "checkpoint:human-verify" 타입 — 디바이스 인터랙션·Sentry dashboard 확인·iOS Safari 호환성·60fps 측정·v1.0 회귀 검증은 사람이 실기기/시뮬레이터에서 직접 수행한다 (네트워크 모킹·Performance 녹화 자동화 인프라 부재).

사용자가 아래 `<how-to-verify>` 5 시나리오를 모두 수행한 뒤 03-VALIDATION.md §Per-Task Verification Map의 status를 ⬜ pending → ✅ green으로 업데이트하고 resume-signal을 입력하면 Wave 3 종료 + Phase 3 종료가 확정된다.
  </action>
  <what-built>
    Wave 1+2+3 통합으로 사용자가 `/tarot` 진입 → intro → shuffle → flip → result → 다시 뽑기 흐름을 완료할 수 있다. 본 checkpoint는 03-VALIDATION.md §Manual-Only Verifications + Per-Task Verification Map의 manual 항목을 실기기 또는 AIT 시뮬레이터에서 1순회 검증하는 단계.
  </what-built>
  <how-to-verify>
    **Setup:**
    1. AIT 시뮬레이터 또는 실기기에서 fortune-cat 빌드 실행 (`pnpm run dev` 또는 토스 인앱 deeplink)
    2. Sentry 프로젝트 dashboard 접속 가능 (production deploy 후 확인)
    3. Chrome DevTools 사용 가능 (Performance·Network·Computed 탭)

    **시나리오 1 — TAROT-01 정상 흐름 (intro → shuffle → flip → result):**
    1. 탭바 "타로" 탭 클릭 → /tarot 진입
    2. intro 단계: 카드 뒷면 preview + 헤드라인 + CTA 표시 확인
    3. "오늘의 한 장 뽑기" CTA 탭 → shuffle 단계로 즉시 전환
    4. shuffle: 부채꼴 3장 (좌·중·우 -15°/0°/+15°) 표시 확인
    5. 가운데 카드 탭 → 그 자리에서 0.5s rotateY 뒤집기 애니메이션 → 약 0.7s 뒤 result 진입 확인
    6. result: 카드 이미지·이모지·한국어명·영문명·키워드 chip(2-4개)·메시지 본문 모두 한 화면에서 보임 확인
    7. 다시 뽑기 fixed 버튼이 탭바 바로 위 8px gap에 표시되며 메시지 본문을 가리지 않음 확인 → **TAROT-01·02·03 PASS**

    **시나리오 2 — TAROT-03 다시 뽑기 5회 reshuffle (Pitfall 6 검증):**
    1. result에서 "다시 뽑기" 탭 → shuffle로 즉시 돌아감 (intro 건너뛰기 D-04 확인)
    2. 새 부채꼴 3장 노출 — 1번 시도와 다른 카드 조합인지 확인 (정확히 같을 확률 = 22C3 분의 1 ≈ 0.06%, 5회 중 ≥3회는 다른 조합 노출되어야 함)
    3. 1장 탭 → 새 result 진입
    4. 다시 뽑기 5회 반복 → 매번 다른 카드 조합 + 다른 result 카드 노출 확인 → **Pitfall 6 PASS**

    **시나리오 3 — D-09/D-10 fetch 에러 + 재시도:**
    1. DevTools Network → Offline 토글
    2. /tarot 새로고침 진입
    3. 로딩 spinner + "카드를 준비하고 있어요" 캡션 표시 확인 (Loader 동작)
    4. fetch 실패 후 에러 헤드라인 "카드 데이터를 불러오지 못했어요" + 보조 "잠시 후 다시 시도해주세요" + "다시 시도하기" 버튼 표시 확인
    5. Sentry dashboard에 `tarot_intro_fetch` extra 메타가 포함된 이벤트 도착 확인 (production만 — dev 환경에서는 console.error만)
    6. Network → Online 토글 → "다시 시도하기" 버튼 탭 → fetch 성공 → intro 단계 표시 확인 → **D-09/D-10 PASS**

    **시나리오 4 — D-02 카드 뒤집기 60fps (Pitfall 1·4 검증):**
    1. Chrome DevTools Performance 탭 → Record 시작
    2. shuffle에서 카드 1장 탭 → 뒤집기 애니메이션 완료까지 녹화
    3. Stop → Frames row에서 16ms/frame 이내 유지 확인 (60fps)
    4. iOS Safari에서 동일 시나리오 — 뒤집기 도중 양면 동시 표시·잔상 없음 확인 (Pitfall 1 backface-visibility prefix)
    5. AIT 시뮬레이터에서 generateHapticFeedback 미통합 시나리오 — 크래시 없음 (햅틱 미도입은 본 페이즈 결정 — Discretion 미채택, RESEARCH §6 참고만) → **D-02·Pitfall 1·4 PASS**

    **시나리오 5 — 회귀 방지 (NAV-03 carry-forward):**
    1. 탭바 "사주" 탭 클릭 → 기존 v1.0 사주 흐름(/) 정상 동작 확인
    2. /saju, /new-year, /amulet 진입 → 각 페이지 v1.0 동일 동작 확인
    3. v1.0 페이지에 시각·인터랙션 회귀 없음 확인 → **NAV-03 PASS**

    **각 시나리오 PASS 시 03-VALIDATION.md §Per-Task Verification Map의 status를 ⬜ pending → ✅ green으로 변경.**
    실패 시 어느 시나리오/단계에서 어떻게 실패했는지 메모 후 plan을 revision으로 돌려보냄.
  </how-to-verify>
  <verify>
    <automated>npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - 시나리오 1 (TAROT-01 정상 흐름 intro→shuffle→flip→result) PASS
    - 시나리오 2 (TAROT-03 다시 뽑기 5회 reshuffle, Pitfall 6 회피) PASS — 5회 중 ≥3회는 다른 카드 조합 노출
    - 시나리오 3 (D-09/D-10 fetch 에러 + 재시도 + Sentry) PASS
    - 시나리오 4 (D-02 카드 뒤집기 60fps + Pitfall 1 backface-visibility iOS Safari) PASS
    - 시나리오 5 (NAV-03 회귀 방지 — v1.0 사주/신년/부적 정상 동작) PASS
    - 03-VALIDATION.md §Per-Task Verification Map status가 모두 ⬜ → ✅로 업데이트됨
    - `npm run lint && npm run build` 통과
  </acceptance_criteria>
  <done>
    수동 디바이스 검증 5 시나리오 모두 PASS, 03-VALIDATION.md status 업데이트 완료, Phase 3 4 Success Criteria 100% 충족, NAV-03 회귀 없음 확인.
  </done>
  <resume-signal>
    "all green" 또는 5개 시나리오 PASS 캡처. 실패 시 시나리오 번호·단계·증상 명시.
  </resume-signal>
</task>

</tasks>

<verification>
**Wave 3 통합 검증 (모든 task 완료 후):**

1. `npm run lint && npm run build` 통과 (exit 0)
2. 변경 파일 1개만 확인 (`git diff --name-only`):
   - `src/pages/TarotPage.jsx` (수정)
3. 변경 금지 파일 미수정 확인:
   - `git diff --stat src/pages/SajuPage.jsx src/pages/AmuletPage.jsx src/pages/HomePage.jsx src/pages/NewYearPage.jsx src/components/Result.jsx src/components/TabBar.jsx src/components/Loading.jsx src/App.jsx src/main.jsx src/lib/firebase.js` → 출력 없음
   - `git diff granite.config.ts package.json package-lock.json pnpm-lock.yaml` → 출력 없음 (D-02 신규 의존성 금지 검증)
4. UI-SPEC Checker Sign-Off 6 dimensions 모두 ☑ (Task 3.2)
5. 수동 디바이스 검증 5개 시나리오 모두 PASS (Task 3.3)
6. ROADMAP Phase 3 4개 Success Criteria 모두 PASS:
   - SC1 (intro 진입 시 카드 뒷면 + 1장 뒤집기로 오늘의 카드 뽑기) — 시나리오 1
   - SC2 (result 화면 카드 이미지·이름·해석 함께 표시) — 시나리오 1
   - SC3 (result에서 "다시 뽑기" 액션 식별·시작) — 시나리오 1·2
   - SC4 (4050 가독성 텍스트 사이즈·여백·콘트라스트) — Task 3.2 Dimension 4·3 + 시나리오 1
7. NAV-03 회귀 없음 — 시나리오 5

**보안 검증 (Threat Model):**
- T-03-11 (PII 미노출): `grep "Sentry.captureException" src/pages/TarotPage.jsx` 메타에 PII 컬럼 미포함 검증
- T-03-12 (cleanup): cancelled flag 패턴 검증
- T-03-13 (무한 루프): useEffect deps `[retryNonce]` 명시 검증
</verification>

<success_criteria>
**ROADMAP Phase 3 완료 조건 (4 Success Criteria 100% 충족):**
- [ ] **SC1:** 사용자가 타로 탭에 진입 → 카드 뒷면 → 1장 뒤집기 → 오늘의 카드 뽑기 — 시나리오 1 PASS
- [ ] **SC2:** 사용자가 결과 화면에서 카드 이미지·이름·해석 텍스트를 한 화면에서 함께 봄 — 시나리오 1 PASS + Dimension 2 PASS
- [ ] **SC3:** 사용자가 결과 화면에서 "다시 뽑기" 액션을 명확히 식별·시작 — 시나리오 1·2 PASS (Phase 4가 광고로 wrap할 준비 완료)
- [ ] **SC4:** 4050 여성 기준 텍스트 사이즈·여백·콘트라스트 편안 — Dimension 3·4·5 PASS

**TAROT-01/02/03 Requirements 충족:**
- [ ] TAROT-01 카드 뒷면 → 1장 뒤집기 (시나리오 1)
- [ ] TAROT-02 결과 화면 카드 이미지·이름·해석 (시나리오 1 + Dimension 2)
- [ ] TAROT-03 다시 뽑기 액션 시작 (시나리오 1·2)

**작업 정합성 (CLAUDE.md + CONTEXT 잠금 결정 준수):**
- [ ] D-02 신규 의존성 0건 (`git diff package.json` 비어 있음)
- [ ] CSS 3D 뒤집기만 사용 (framer-motion 미import — `grep -r framer-motion src/` 빈 결과)
- [ ] D-08 마이그레이션 적용 + RLS 동작 (Wave 1 검증)
- [ ] D-09 intro에서 22장 fetch 1회 (시나리오 1)
- [ ] D-10 fetch 에러 처리 + Sentry (시나리오 3)
- [ ] D-11 22장 webp 정적 import (Wave 1)
- [ ] v1.0 페이지/컴포넌트 일체 미변경 (시나리오 5 + git diff)
- [ ] UI-SPEC Checker Sign-Off 6 dimensions PASS (Task 3.2)
- [ ] 수동 디바이스 검증 5 시나리오 PASS (Task 3.3)
- [ ] `npm run lint && npm run build` 통과
</success_criteria>

<output>
After completion, create `.planning/phases/03-daily-one-card-core/03-03-SUMMARY.md` with:
- Files modified: `src/pages/TarotPage.jsx` (Phase 2 placeholder → 3단계 상태 머신 완성)
- 4 useState 시그니처 잠금 정리 (Phase 4·5에서 Context 승격 결정 필요)
- handleRedraw 핸들러 잠금 위치 (Phase 4 광고 wrap 지점) — 정확한 함수 시그니처 명시
- selectedCard prop 흐름 (Phase 5 공유 시트가 사용할 데이터 위치)
- UI-SPEC Checker Sign-Off 결과 (6 dimensions × PASS/FAIL)
- 수동 디바이스 검증 5 시나리오 결과 (시나리오별 PASS/FAIL + 발견된 이슈)
- Phase 3 종료 후 PROJECT.md/STATE.md 갱신 노트 (현재 cleanup 작업 없음 — CONTEXT deferred 섹션에서 명시)
- Phase 4·5 영향 노트 (다시 뽑기 핸들러 위치, fixed 버튼 시각 잠금, 카드 22장 prefetch 패턴 등)
- 검증된 assumptions (RESEARCH §Assumptions Log A1~A8 중 본 페이즈에서 확인된 항목)
</output>
