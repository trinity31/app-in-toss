---
phase: 03-daily-one-card-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/2026XXXX_create_tarot_cards.sql
  - src/assets/images/cards/00.webp
  - src/assets/images/cards/01.webp
  - src/assets/images/cards/02.webp
  - src/assets/images/cards/03.webp
  - src/assets/images/cards/04.webp
  - src/assets/images/cards/05.webp
  - src/assets/images/cards/06.webp
  - src/assets/images/cards/07.webp
  - src/assets/images/cards/08.webp
  - src/assets/images/cards/09.webp
  - src/assets/images/cards/10.webp
  - src/assets/images/cards/11.webp
  - src/assets/images/cards/12.webp
  - src/assets/images/cards/13.webp
  - src/assets/images/cards/14.webp
  - src/assets/images/cards/15.webp
  - src/assets/images/cards/16.webp
  - src/assets/images/cards/17.webp
  - src/assets/images/cards/18.webp
  - src/assets/images/cards/19.webp
  - src/assets/images/cards/20.webp
  - src/assets/images/cards/21.webp
  - src/assets/images/cards/index.js
  - src/lib/supabase.js
autonomous: false
requirements: [TAROT-01, TAROT-02]
requirements_addressed: [TAROT-01, TAROT-02]
must_haves:
  truths:
    - "사용자가 운영 Supabase 프로젝트의 tarot_cards 테이블에서 22행을 anon role select로 받을 수 있다"
    - "클라이언트가 fetchTarotCards()를 호출해 22장의 카드 데이터(id/name_ko/name_en/emoji/image_path/keywords/message)를 정렬된 배열로 받을 수 있다"
    - "클라이언트가 getCardImageUrl(id)로 0~21 카드 webp 이미지의 fingerprint URL을 즉시 받을 수 있다"
  artifacts:
    - path: "scripts/2026XXXX_create_tarot_cards.sql"
      provides: "tarot_cards 7컬럼 스키마 + RLS select USING(true) + 22 row INSERT"
      contains: "CREATE TABLE IF NOT EXISTS tarot_cards"
    - path: "src/assets/images/cards/00.webp ~ 21.webp"
      provides: "메이저 아르카나 22장 정적 이미지"
      min_count: 22
    - path: "src/assets/images/cards/index.js"
      provides: "getCardImageUrl(id) 헬퍼 + (옵션) prefetchAllCardImages()"
      exports: ["getCardImageUrl", "prefetchAllCardImages"]
    - path: "src/lib/supabase.js"
      provides: "fetchTarotCards() 비동기 함수 (export named)"
      contains: "export async function fetchTarotCards"
  key_links:
    - from: "src/lib/supabase.js fetchTarotCards()"
      to: "Supabase tarot_cards 테이블"
      via: "supabase.from('tarot_cards').select(...).order('id')"
      pattern: "from\\(['\"]tarot_cards['\"]\\)"
    - from: "src/assets/images/cards/index.js"
      to: "src/assets/images/cards/00.webp ~ 21.webp"
      via: "Vite static import x 22"
      pattern: "import card\\d{2} from"
---

<objective>
Phase 3 Wave 1 — 데이터 레이어 구축. 운영 Supabase에 `tarot_cards` 테이블(7컬럼 + RLS + 22 row seed)을 생성·적용하고, 22장 webp 카드 이미지를 fortune-cat에 정적 import 가능한 형태로 복사하고, `fetchTarotCards()` 함수와 `getCardImageUrl(id)` 헬퍼를 export한다.

Purpose: Wave 2 UI 컴포넌트(TarotCardArt/TarotShuffle/TarotResult)와 Wave 3 TarotPage 통합이 즉시 사용할 수 있는 데이터·이미지 인터페이스를 미리 잠금. CONTEXT D-08(스키마)·D-09(intro 진입 시 fetch)·D-10(에러 처리)·D-11(이미지 형식)을 정확하게 코드/SQL로 실체화.

Output:
- `scripts/2026XXXX_create_tarot_cards.sql` — 마이그레이션 + 22 row seed
- `src/assets/images/cards/00.webp ~ 21.webp` — 22장 카드 이미지
- `src/assets/images/cards/index.js` — getCardImageUrl(id) + prefetchAllCardImages()
- `src/lib/supabase.js` — fetchTarotCards() 함수 추가 (기존 export 보존)
- 운영 Supabase에 마이그레이션 적용 완료 (수동 적용 task)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-daily-one-card-core/03-CONTEXT.md
@.planning/phases/03-daily-one-card-core/03-RESEARCH.md
@.planning/phases/03-daily-one-card-core/03-VALIDATION.md
@.planning/phases/01-tarot-prototype-evaluation/01-INVENTORY.md

<interfaces>
<!-- Wave 1이 노출하는 신규 인터페이스 (Wave 2/3가 import해서 사용) -->

From src/lib/supabase.js (확장):
```javascript
// 기존 export 그대로 유지 + 아래 함수 추가
export async function fetchTarotCards(): Promise<Array<{
  id: number,           // 0~21
  name_ko: string,
  name_en: string,
  emoji: string,
  image_path: string,   // '00.webp' ~ '21.webp' (파일명만)
  keywords: string[],   // 2~4개
  message: string       // ~300자
}>>
// 실패 시 throw error (호출 측에서 try/catch + Sentry.captureException)
```

From src/assets/images/cards/index.js (신규):
```javascript
export function getCardImageUrl(id: number): string  // Vite static import URL (id 0~21)
export function prefetchAllCardImages(): void         // 22장 new Image() prefetch (옵션)
```

From scripts/2026XXXX_create_tarot_cards.sql (신규):
- Supabase 운영 프로젝트에 적용 후 22행 select 가능 (anon role)
- 컬럼: id (PK int), name_ko text, name_en text, emoji text, image_path text, keywords text[], message text
</interfaces>

<reference_files>
- src/lib/supabase.js (현재 패턴: getMenuImageUrl, getAmuletConfig, getTodayOrderCount — 동일 모듈에 fetchTarotCards 추가)
- scripts/create_amulet_types_table.sql (CREATE TABLE + RLS 패턴 정본)
- scripts/seed_amulet_types.sql (INSERT 패턴 정본)
- /Users/trinity/Projects/toss-app-generator/workspace/boknyang-tarot/boknyang-tarot/public/cards/00.webp ~ 21.webp (22장 원본)
- /Users/trinity/Projects/toss-app-generator/workspace/boknyang-tarot/boknyang-tarot/src/data/cards.ts (22장 메시지 텍스트 — "~다냥" 톤 그대로 추출)
</reference_files>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 클라이언트 → Supabase REST | anon role JWT로 tarot_cards 테이블 접근. RLS가 유일한 권한 게이트 |
| 빌드 시스템 → 정적 webp | Vite가 22장 webp를 빌드 산출물로 fingerprinting. 외부 입력 없음 |
| 마이그레이션 실행자 → DB | Supabase Studio에서 SQL 실행. 수동 적용이므로 DDL 검토 사람이 함 |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | tarot_cards 테이블 RLS | mitigate | RLS 활성화 + `FOR SELECT USING (true)` 정책만 정의 — INSERT/UPDATE/DELETE 정책 미정의로 anon role 쓰기 거부 (Pitfall 3 회피). 마이그레이션 SQL에 `ALTER TABLE ... ENABLE ROW LEVEL SECURITY` + select policy 명시 검증 (acceptance criteria) |
| T-03-02 | Tampering | 22장 INSERT 무결성 | mitigate | `ON CONFLICT (id) DO NOTHING`로 재실행 안전. 22행 모두 id 0~21 존재 검증을 적용 task acceptance criteria에 명시 (`SELECT count(*) = 22`) |
| T-03-03 | Denial of Service | tarot_cards select | accept | 22행/세션 1회/캐시 가능 — 부하 미미. CDN/edge 캐싱 불필요 (saju 테이블도 동일 패턴 운영 중) |
| T-03-04 | Tampering | image_path 컬럼이 클라이언트 path traversal 가능성 | accept | image_path는 클라이언트가 사용 안 함 (D-11: id 기반 정적 import만 사용). 컬럼은 Phase 4·5 동적 카드 추가 대비 메타데이터로만 보존 — 본 페이즈에서는 무해 |
| T-03-05 | Information Disclosure | 마이그레이션 SQL이 git에 commit | accept | tarot_cards 메시지 본문은 사용자 노출 컨텐츠 — secret 아님. 운영 DB credential 미포함 (Supabase URL은 .env 분리) |

**보안 게이트:** 위 threat 모두 disposition 결정 완료. T-03-01은 적용 후 anon role로 select 22행 가능 + INSERT 시도 거부 검증 필수.
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1.1: tarot_cards 마이그레이션 SQL 작성 (스키마 + RLS + 22 row seed)</name>
  <files>scripts/2026XXXX_create_tarot_cards.sql</files>
  <read_first>
    - .planning/phases/03-daily-one-card-core/03-CONTEXT.md (D-08 스키마 정확값)
    - .planning/phases/03-daily-one-card-core/03-RESEARCH.md (§4 SQL 코드 스니펫, Pitfall 3 RLS 누락 회피)
    - scripts/create_amulet_types_table.sql (CREATE TABLE + RLS 패턴 정본 — `CREATE POLICY ... FOR SELECT USING (true)` 라인 21-25 동일 적용)
    - scripts/seed_amulet_types.sql (INSERT 패턴 정본)
    - /Users/trinity/Projects/toss-app-generator/workspace/boknyang-tarot/boknyang-tarot/src/data/cards.ts (22장 메시지 텍스트 — "~다냥" 톤 그대로 추출 대상)
  </read_first>
  <action>
파일명: `scripts/<YYYYMMDD>_create_tarot_cards.sql` (예: `scripts/20260502_create_tarot_cards.sql`). 날짜는 작성 당일(KST) `date +%Y%m%d` 출력값 사용.

다음 정확한 SQL을 작성한다 (CONTEXT D-08 + RESEARCH §4 그대로):

```sql
-- tarot_cards 테이블 생성 (Phase 3 D-08)
-- 메이저 아르카나 22장. 운영 메타데이터(updated_at·is_active·sort_order) 미포함 (deferred).
-- amulet_types 패턴(create_amulet_types_table.sql) 참고.

CREATE TABLE IF NOT EXISTS tarot_cards (
  id INT PRIMARY KEY,
  name_ko TEXT NOT NULL,
  name_en TEXT NOT NULL,
  emoji TEXT NOT NULL,
  image_path TEXT NOT NULL,
  keywords TEXT[] NOT NULL,
  message TEXT NOT NULL
);

-- RLS 정책: anon role 읽기 가능 (Pitfall 3 빈 배열 반환 회피)
ALTER TABLE tarot_cards ENABLE ROW LEVEL SECURITY;

CREATE POLICY "tarot_cards_select_policy" ON tarot_cards
  FOR SELECT USING (true);

-- 22장 seed (boknyang-tarot/src/data/cards.ts의 메이저 아르카나 22장 메시지 그대로 추출, "~다냥" 톤 유지)
-- image_path는 D-11에 따라 단순 파일명만 저장 (클라이언트는 id 기반 정적 import 사용)
INSERT INTO tarot_cards (id, name_ko, name_en, emoji, image_path, keywords, message) VALUES
  (0,  '바보',      'The Fool',          '🐾', '00.webp', ARRAY['시작','자유','설렘'],     $$<cards.ts에서 The Fool 메시지 그대로 — "~다냥" 톤 유지>$$),
  (1,  '마법사',    'The Magician',      '✨', '01.webp', ARRAY['창조','의지','재능'],     $$<cards.ts에서 The Magician 메시지>$$),
  (2,  '여사제',    'The High Priestess','🌙', '02.webp', ARRAY['직관','신비','내면'],     $$<...>$$),
  (3,  '여황제',    'The Empress',       '🌷', '03.webp', ARRAY['풍요','여성성','사랑'],   $$<...>$$),
  (4,  '황제',      'The Emperor',       '👑', '04.webp', ARRAY['권위','안정','구조'],     $$<...>$$),
  (5,  '교황',      'The Hierophant',    '📜', '05.webp', ARRAY['전통','지혜','지도'],     $$<...>$$),
  (6,  '연인',      'The Lovers',        '💞', '06.webp', ARRAY['선택','관계','조화'],     $$<...>$$),
  (7,  '전차',      'The Chariot',       '🐎', '07.webp', ARRAY['의지','승리','전진'],     $$<...>$$),
  (8,  '힘',        'Strength',          '🦁', '08.webp', ARRAY['용기','인내','내면의힘'], $$<...>$$),
  (9,  '은둔자',    'The Hermit',        '🕯️', '09.webp', ARRAY['성찰','지혜','고독'],     $$<...>$$),
  (10, '운명의수레', 'Wheel of Fortune', '🎡', '10.webp', ARRAY['전환점','운명','순환'],   $$<...>$$),
  (11, '정의',      'Justice',           '⚖️', '11.webp', ARRAY['공정','균형','진실'],     $$<...>$$),
  (12, '매달린남자', 'The Hanged Man',   '🙃', '12.webp', ARRAY['관점전환','희생','수용'], $$<...>$$),
  (13, '죽음',      'Death',             '🦋', '13.webp', ARRAY['변화','종결','재생'],     $$<...>$$),
  (14, '절제',      'Temperance',        '🌊', '14.webp', ARRAY['조화','절제','치유'],     $$<...>$$),
  (15, '악마',      'The Devil',         '🔥', '15.webp', ARRAY['집착','유혹','속박'],     $$<...>$$),
  (16, '탑',        'The Tower',         '⚡', '16.webp', ARRAY['붕괴','각성','해방'],     $$<...>$$),
  (17, '별',        'The Star',          '✨', '17.webp', ARRAY['희망','치유','영감'],     $$<...>$$),
  (18, '달',        'The Moon',          '🌙', '18.webp', ARRAY['직감','꿈','불확실'],     $$<...>$$),
  (19, '태양',      'The Sun',           '☀️', '19.webp', ARRAY['기쁨','성공','활력'],     $$<...>$$),
  (20, '심판',      'Judgement',         '🎺', '20.webp', ARRAY['부활','각성','용서'],     $$<...>$$),
  (21, '세계',      'The World',         '🌏', '21.webp', ARRAY['완성','성취','조화'],     $$<...>$$)
ON CONFLICT (id) DO NOTHING;
```

**중요:**
1. **메시지 본문은 boknyang-tarot/src/data/cards.ts에서 22장 정확히 추출** — 단순 placeholder 문자열 금지. `$$ ... $$` dollar-quoted string 사용 (RESEARCH Open Question 4 권고 — 한국어 따옴표/특수문자 escape 회피).
2. **카드 한국어명·영문명·이모지·키워드는 위 표 그대로 사용** (boknyang-tarot 표준 메이저 아르카나 22종). cards.ts와 다른 부분이 있으면 cards.ts를 정본으로 따른다 (메시지 톤 일관성).
3. **`ON CONFLICT (id) DO NOTHING`** 필수 — 재실행 안전 (T-03-02 mitigation).
4. **RLS 정책 누락 금지** — `ALTER TABLE ... ENABLE ROW LEVEL SECURITY` + `CREATE POLICY ... FOR SELECT USING (true)` 두 줄 필수 (Pitfall 3 + T-03-01).
5. INSERT/UPDATE/DELETE 정책은 정의 안 함 — anon role 쓰기 자동 차단.
6. 파일은 작성만 하고 실제 적용은 다음 task(1.2)에서 사용자 수동 적용.
  </action>
  <verify>
    <automated>test -f scripts/[0-9]*_create_tarot_cards.sql && grep -c "INSERT INTO tarot_cards" scripts/[0-9]*_create_tarot_cards.sql && grep -c "ENABLE ROW LEVEL SECURITY" scripts/[0-9]*_create_tarot_cards.sql && grep -c "FOR SELECT USING (true)" scripts/[0-9]*_create_tarot_cards.sql</automated>
  </verify>
  <acceptance_criteria>
    - 파일 존재: `ls scripts/*_create_tarot_cards.sql` 출력 1줄
    - 스키마 7컬럼 모두 정의: `grep -E "(id INT PRIMARY KEY|name_ko TEXT NOT NULL|name_en TEXT NOT NULL|emoji TEXT NOT NULL|image_path TEXT NOT NULL|keywords TEXT\[\] NOT NULL|message TEXT NOT NULL)" scripts/*_create_tarot_cards.sql | wc -l` = 7
    - RLS 활성화: `grep -c "ALTER TABLE tarot_cards ENABLE ROW LEVEL SECURITY" scripts/*_create_tarot_cards.sql` ≥ 1
    - SELECT 정책: `grep -c "FOR SELECT USING (true)" scripts/*_create_tarot_cards.sql` ≥ 1
    - 22행 INSERT: `grep -E "^\s*\([0-9]+," scripts/*_create_tarot_cards.sql | wc -l` = 22 (id 0~21 모두 등장)
    - id 0~21 누락 없음: `grep -oE "^\s*\(([0-9]+)," scripts/*_create_tarot_cards.sql | grep -oE "[0-9]+" | sort -n | uniq | wc -l` = 22
    - ON CONFLICT 절: `grep -c "ON CONFLICT (id) DO NOTHING" scripts/*_create_tarot_cards.sql` ≥ 1
    - 메시지에 "다냥" 톤 보존: `grep -c "다냥" scripts/*_create_tarot_cards.sql` ≥ 10 (22장 중 최소 10장 이상에 마스코트 종결어미 — cards.ts 톤 추출 검증)
    - 단순 placeholder 금지: `grep -c "<.*>" scripts/*_create_tarot_cards.sql` = 0 (`<...>` 같은 미완성 마커 금지)
  </acceptance_criteria>
  <done>
    파일 작성 완료, 위 acceptance_criteria 9개 모두 PASS, git status에 신규 SQL 파일 표시.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1.2: 마이그레이션 운영 Supabase에 수동 적용 (사용자 작업)</name>
  <files>scripts/[0-9]*_create_tarot_cards.sql (Task 1.1 산출 — 본 task가 운영 DB에 적용)</files>
  <read_first>
    - scripts/[0-9]*_create_tarot_cards.sql (Task 1.1 산출물 — Supabase Studio에 그대로 복사·실행)
    - scripts/create_amulet_types_table.sql (이전 마이그레이션 적용 패턴 — 본 페이즈 동일 절차)
  </read_first>
  <action>
이 task는 "checkpoint:human-action" 타입 — Claude가 자동으로 실행할 수 없는 사용자 수동 작업이다. fortune-cat은 표준 `supabase/migrations/` 패턴이 아닌 `scripts/*.sql`을 Supabase Studio에서 수동 적용하는 관행이며, Supabase 운영 프로젝트 credential이 사용자 브라우저 세션에만 존재하므로 Claude는 마이그레이션 적용을 자동화할 수 없다.

사용자가 아래 `<how-to-verify>` 절차를 따라 수동으로 마이그레이션을 적용한 뒤 resume-signal을 입력하면 다음 task(1.3)으로 진행한다.
  </action>
  <what-built>
    Task 1.1에서 작성한 `scripts/<YYYYMMDD>_create_tarot_cards.sql` (tarot_cards 7컬럼 스키마 + RLS + 22 row INSERT). fortune-cat은 표준 `supabase/migrations/` 패턴이 아닌 `scripts/*.sql`을 Supabase Studio에서 수동 적용하는 관행 — 따라서 자동 적용 없음.
  </what-built>
  <how-to-verify>
    1. Supabase Studio에 로그인 (`saju.trinity-apps.net` 백엔드 = 운영 프로젝트)
    2. 좌측 사이드바 **SQL Editor** 클릭 → **New query**
    3. 생성된 `scripts/<YYYYMMDD>_create_tarot_cards.sql` 파일 내용 전체 복사·붙여넣기
    4. **Run** 버튼 클릭 → 에러 없이 완료 확인
    5. 좌측 **Table Editor** → `tarot_cards` 테이블 선택 → 22행 표시 확인 (id 0~21 모두 존재)
    6. SQL Editor에서 검증 쿼리 실행:
       ```sql
       SELECT count(*) FROM tarot_cards;                       -- 결과: 22
       SELECT id FROM tarot_cards ORDER BY id LIMIT 3;          -- 결과: 0, 1, 2
       SELECT id FROM tarot_cards ORDER BY id DESC LIMIT 1;     -- 결과: 21
       ```
    7. anon role select 검증 (RLS 정책 적용 확인):
       - 새 익명 브라우저 또는 Supabase Studio의 SQL Editor "Impersonate role" → anon 선택
       - `SELECT count(*) FROM tarot_cards;` → 결과 22 (RLS USING(true) 통과 검증)
    8. anon role 쓰기 거부 검증 (T-03-01):
       - anon role로 `INSERT INTO tarot_cards (id, name_ko, name_en, emoji, image_path, keywords, message) VALUES (99, 'x','y','z','99.webp',ARRAY['t'],'m');` 실행
       - 결과: `new row violates row-level security policy` 또는 동일 의미 에러 (성공하면 RLS 정책 누락 — 마이그레이션 재검토)

    실패 시:
    - 마이그레이션 파일을 수정하고 Task 1.1을 다시 실행한 뒤 본 task 재진행
    - `tarot_cards` 테이블이 이미 존재하면 `IF NOT EXISTS`로 안전하게 skip되며, INSERT는 `ON CONFLICT DO NOTHING`으로 중복 회피됨
  </how-to-verify>
  <verify>
    <automated>echo "manual: human applies migration via Supabase Studio — verified by select count(*) = 22 + RLS anon role test"</automated>
  </verify>
  <acceptance_criteria>
    - 사용자가 SQL Editor에서 `SELECT count(*) FROM tarot_cards;` 실행 → 결과 22 캡처
    - 사용자가 anon role impersonation으로 SELECT 22행 통과 캡처
    - 사용자가 anon role INSERT 시도 → "new row violates row-level security policy" 에러 캡처
    - 위 3개 캡처를 resume-signal로 공유
  </acceptance_criteria>
  <done>
    운영 Supabase에 tarot_cards 22행 적용 완료, RLS 정책으로 anon select 가능 + INSERT 거부 확인. Wave 1 후속 task(1.3, 1.4)가 즉시 진행 가능.
  </done>
  <resume-signal>
    "applied" 또는 "통과" 입력 + 22행 count 결과 캡처. 실패 시 에러 메시지 공유.
  </resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 1.3: 22장 webp 카드 이미지 복사 + getCardImageUrl 헬퍼 작성</name>
  <files>
    src/assets/images/cards/00.webp, src/assets/images/cards/01.webp, ..., src/assets/images/cards/21.webp,
    src/assets/images/cards/index.js
  </files>
  <read_first>
    - .planning/phases/03-daily-one-card-core/03-CONTEXT.md (D-11 webp 형식 + 정적 import 결정)
    - .planning/phases/03-daily-one-card-core/03-RESEARCH.md (Pattern 6 코드 스니펫, Pitfall 2 깜빡임 회피, A1 webp 합계 가정)
    - 디렉토리 확인: `ls /Users/trinity/Projects/toss-app-generator/workspace/boknyang-tarot/boknyang-tarot/public/cards/` (22장 원본 위치)
  </read_first>
  <action>
**Step 1 — 디렉토리 생성 + 22장 webp 복사:**

```bash
mkdir -p /Users/trinity/Projects/app-in-toss/fortune-cat/src/assets/images/cards
cp /Users/trinity/Projects/toss-app-generator/workspace/boknyang-tarot/boknyang-tarot/public/cards/*.webp /Users/trinity/Projects/app-in-toss/fortune-cat/src/assets/images/cards/
ls /Users/trinity/Projects/app-in-toss/fortune-cat/src/assets/images/cards/*.webp | wc -l   # 22 확인
du -sh /Users/trinity/Projects/app-in-toss/fortune-cat/src/assets/images/cards/             # RESEARCH A1 실측 (1MB 미만 가정 검증)
```

원본 파일명이 `00.webp ~ 21.webp`가 아닌 경우 (예: `the_fool.webp`) → 0~21 패딩된 파일명으로 rename. boknyang-tarot 원본은 `00.webp ~ 21.webp` 형식임이 INVENTORY 라인 138에서 검증됨.

**Step 2 — `src/assets/images/cards/index.js` 작성:**

다음 정확한 코드를 작성한다 (RESEARCH Pattern 6 그대로):

```javascript
// 22장 메이저 아르카나 카드 webp 정적 import + id 기반 lookup 헬퍼.
// CONTEXT D-11: webp 형식 그대로, Vite 정적 import로 빌드 타임 fingerprint URL 생성.
// RESEARCH Pattern 6 + Pitfall 2 (intro 진입 직후 prefetch로 첫 result 깜빡임 회피).

import card00 from './00.webp';
import card01 from './01.webp';
import card02 from './02.webp';
import card03 from './03.webp';
import card04 from './04.webp';
import card05 from './05.webp';
import card06 from './06.webp';
import card07 from './07.webp';
import card08 from './08.webp';
import card09 from './09.webp';
import card10 from './10.webp';
import card11 from './11.webp';
import card12 from './12.webp';
import card13 from './13.webp';
import card14 from './14.webp';
import card15 from './15.webp';
import card16 from './16.webp';
import card17 from './17.webp';
import card18 from './18.webp';
import card19 from './19.webp';
import card20 from './20.webp';
import card21 from './21.webp';

const CARD_IMAGES = [
  card00, card01, card02, card03, card04, card05, card06, card07,
  card08, card09, card10, card11, card12, card13, card14, card15,
  card16, card17, card18, card19, card20, card21,
];

/**
 * 카드 id(0~21)로 정적 import된 webp URL을 반환.
 * 잘못된 id는 0번(바보) URL로 fallback + console.warn.
 */
export function getCardImageUrl(id) {
  if (!Number.isInteger(id) || id < 0 || id > 21) {
    console.warn(`[cards] invalid card id: ${id}`);
    return CARD_IMAGES[0];
  }
  return CARD_IMAGES[id];
}

/**
 * 22장 webp를 브라우저 이미지 캐시에 prefetch.
 * intro 단계 mount 직후 호출 → 사용자가 헤드라인 읽는 동안 다운로드 → 첫 result 표시 시 깜빡임 회피 (Pitfall 2).
 * SSR/non-browser 환경에서는 silent skip.
 */
export function prefetchAllCardImages() {
  if (typeof window === 'undefined') return;
  CARD_IMAGES.forEach((url) => {
    const img = new Image();
    img.src = url;
  });
}
```

**중요:**
- `CARD_IMAGES` 배열 22장 전부 명시 (loop·dynamic require 금지 — Vite 빌드 타임 분석 위해)
- export named 2종: `getCardImageUrl`, `prefetchAllCardImages`
- default export 금지 (RESEARCH Pattern 6 + fortune-cat 유틸 패턴 일관)
- 다른 파일 변경 금지 (CLAUDE.md 글로벌 #2 — 명시 작업만)
  </action>
  <verify>
    <automated>ls /Users/trinity/Projects/app-in-toss/fortune-cat/src/assets/images/cards/*.webp | wc -l && grep -c "^import card" /Users/trinity/Projects/app-in-toss/fortune-cat/src/assets/images/cards/index.js && grep -c "export function getCardImageUrl" /Users/trinity/Projects/app-in-toss/fortune-cat/src/assets/images/cards/index.js && grep -c "export function prefetchAllCardImages" /Users/trinity/Projects/app-in-toss/fortune-cat/src/assets/images/cards/index.js && cd /Users/trinity/Projects/app-in-toss/fortune-cat && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - 22장 webp 존재: `ls src/assets/images/cards/*.webp | wc -l` = 22
    - 파일명이 정확히 0~21 패딩: `ls src/assets/images/cards/ | grep -E "^[0-9]{2}\.webp$" | wc -l` = 22
    - 22 import 라인: `grep -c "^import card" src/assets/images/cards/index.js` = 22
    - getCardImageUrl export: `grep -c "export function getCardImageUrl" src/assets/images/cards/index.js` = 1
    - prefetchAllCardImages export: `grep -c "export function prefetchAllCardImages" src/assets/images/cards/index.js` = 1
    - default export 금지: `grep -c "export default" src/assets/images/cards/index.js` = 0
    - 22장 합계 1MB 미만: `du -sb src/assets/images/cards/ | awk '{print $1}'`가 1048576 미만 (RESEARCH A1 가정 검증 — 초과 시 본 task SUMMARY에 기록 + Phase 4·5 prefetch 정책 재검토 필요)
    - ESLint 통과: `npm run lint` exit 0
    - 본 task가 변경한 파일 외 어떤 파일도 수정 안 됨: `git diff --name-only` 출력에 `src/assets/images/cards/*` 외 항목 없음
  </acceptance_criteria>
  <done>
    22장 webp 복사 완료, index.js export 2종 작성 완료, 위 acceptance criteria 9개 모두 PASS.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 1.4: src/lib/supabase.js에 fetchTarotCards() 함수 추가</name>
  <files>src/lib/supabase.js</files>
  <read_first>
    - .planning/phases/03-daily-one-card-core/03-CONTEXT.md (D-08 컬럼 7종, D-09 fetch 시점, D-10 에러 처리)
    - .planning/phases/03-daily-one-card-core/03-RESEARCH.md (§1 fetchTarotCards 코드 스니펫, Pitfall 3 빈 배열)
    - src/lib/supabase.js (현재 export 목록 — 기존 함수 보존 필수, getMenuImageUrl/getAmuletConfig/getTodayOrderCount 등)
    - src/pages/HomePage.jsx (라인 88-130 — Promise.all + setHasError 패턴 호출 측 사용 흐름 참고)
  </read_first>
  <action>
**기존 `src/lib/supabase.js`의 마지막에 다음 정확한 코드를 추가한다** (RESEARCH §1 그대로). 기존 함수·import·export는 일체 변경 금지.

```javascript

/**
 * tarot_cards 테이블에서 22장 메이저 아르카나 카드 데이터를 id 오름차순으로 select.
 *
 * Phase 3 D-08: 7컬럼 (id/name_ko/name_en/emoji/image_path/keywords/message) 스키마.
 * Phase 3 D-09: TarotPage intro 단계 진입 시 1회 호출 → cardsData state에 캐시 → shuffle/result 0 latency.
 *
 * @returns {Promise<Array<{id: number, name_ko: string, name_en: string, emoji: string, image_path: string, keywords: string[], message: string}>>}
 *   22장 카드 객체 배열 (id 오름차순). 0행이면 빈 배열 반환 (RLS 정책 누락 시 — Pitfall 3 회피용 호출 측 검증 필요).
 * @throws {Error} Supabase 네트워크/권한 에러. 호출 측에서 try/catch + Sentry.captureException 처리.
 */
export async function fetchTarotCards() {
  const { data, error } = await supabase
    .from('tarot_cards')
    .select('id, name_ko, name_en, emoji, image_path, keywords, message')
    .order('id', { ascending: true });
  if (error) {
    console.error('[supabase] fetchTarotCards 실패:', error);
    throw error;
  }
  return data || [];
}
```

**중요:**
- 기존 파일의 import/createClient/기존 export 라인 일체 변경 금지 (CLAUDE.md 글로벌 #2 + DRY)
- `select(...)` 컬럼 명시(SELECT *) 사용 금지 → 7컬럼만 명시 (DRY + 신규 컬럼 추가 시 명시적 영향 파악)
- `.order('id', { ascending: true })` 필수 (호출 측 인덱싱 일관성)
- error throw 후 호출 측이 Sentry.captureException 책임 (D-10 — Pattern 1 TarotPage useEffect에서 처리)
- JSDoc 한국어 작성 (CLAUDE.md 글로벌 #1)
  </action>
  <verify>
    <automated>grep -c "export async function fetchTarotCards" /Users/trinity/Projects/app-in-toss/fortune-cat/src/lib/supabase.js && grep -c "from('tarot_cards')" /Users/trinity/Projects/app-in-toss/fortune-cat/src/lib/supabase.js && grep -c "id, name_ko, name_en, emoji, image_path, keywords, message" /Users/trinity/Projects/app-in-toss/fortune-cat/src/lib/supabase.js && grep -c "order('id'" /Users/trinity/Projects/app-in-toss/fortune-cat/src/lib/supabase.js && cd /Users/trinity/Projects/app-in-toss/fortune-cat && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - fetchTarotCards 함수 정의: `grep -c "export async function fetchTarotCards" src/lib/supabase.js` = 1
    - tarot_cards 테이블 참조: `grep -c "from('tarot_cards')" src/lib/supabase.js` = 1
    - 7컬럼 명시: `grep -c "id, name_ko, name_en, emoji, image_path, keywords, message" src/lib/supabase.js` = 1
    - id 오름차순 정렬: `grep -c "order('id', { ascending: true })" src/lib/supabase.js` = 1
    - SELECT * 사용 금지: `grep -c "select('\*')" src/lib/supabase.js` = 0
    - 기존 export 보존: `grep -E "^export (function|async function|const|class) (getMenuImageUrl|getAmuletStyleImageUrl|getAmuletIntroImageUrl|getOgImageUrl|getAmuletConfig)" src/lib/supabase.js` 출력이 변경 전과 동일 (기존 함수 5개 이상 보존)
    - `supabase` 클라이언트 재생성 금지: `grep -c "createClient" src/lib/supabase.js` = 1 (기존 1회만 — 신규 createClient 호출 금지)
    - ESLint 통과: `npm run lint` exit 0
    - Vite build 통과: `npm run build` exit 0
    - 본 task가 변경한 파일 외 수정 없음: `git diff --name-only` 출력에 `src/lib/supabase.js` 외 항목 없음
  </acceptance_criteria>
  <done>
    fetchTarotCards 함수 추가 완료, 기존 export 보존, lint/build 모두 PASS, 위 acceptance criteria 10개 모두 PASS.
  </done>
</task>

</tasks>

<verification>
**Wave 1 통합 검증 (모든 task 완료 후):**

1. `npm run lint && npm run build` 통과 (exit 0)
2. 마이그레이션 적용 확인: Supabase Studio에서 `SELECT count(*) FROM tarot_cards;` = 22
3. 신규/변경 파일 확인 (`git status`):
   - `scripts/<YYYYMMDD>_create_tarot_cards.sql` (신규)
   - `src/assets/images/cards/00.webp ~ 21.webp` (신규 22장)
   - `src/assets/images/cards/index.js` (신규)
   - `src/lib/supabase.js` (수정 — 함수 1개 추가만)
4. 변경 금지 파일 미수정 확인:
   - `git diff --stat src/pages/SajuPage.jsx src/pages/AmuletPage.jsx src/pages/HomePage.jsx src/pages/NewYearPage.jsx src/components/Result.jsx src/components/TabBar.jsx src/App.jsx` → 출력 없음
   - `git diff granite.config.ts package.json package-lock.json pnpm-lock.yaml` → 출력 없음 (D-02 신규 의존성 금지 검증)
5. 22장 webp 합계가 RESEARCH A1 가정(<1MB) 충족 — 초과 시 SUMMARY에 명시 + Wave 2/3에서 prefetch 정책 재검토.
6. Threat T-03-01·T-03-02 mitigation 검증 (Task 1.2 수동 단계 7·8에서 anon select 22행 + INSERT 거부 확인).
</verification>

<success_criteria>
**ROADMAP Phase 3 Success Criteria 매핑:**

본 plan은 SC1 (intro에서 카드 fetch) 데이터 기반 + SC2 (결과 화면 카드 데이터 공급원)의 데이터 레이어 부분만 충족. UI 인터랙션은 Wave 2/3 plan이 책임.

**완료 조건:**
- [ ] `scripts/<YYYYMMDD>_create_tarot_cards.sql` 작성 + 운영 Supabase 적용 완료
- [ ] anon role로 `tarot_cards` 22행 select 가능 (RLS USING(true))
- [ ] anon role로 INSERT/UPDATE/DELETE 거부 (RLS 누락 정책 검증)
- [ ] `src/assets/images/cards/00.webp ~ 21.webp` 22장 존재
- [ ] `src/assets/images/cards/index.js`에 `getCardImageUrl` + `prefetchAllCardImages` export
- [ ] `src/lib/supabase.js`에 `fetchTarotCards` export (기존 함수 보존)
- [ ] `npm run lint && npm run build` 통과
- [ ] v1.0 페이지/컴포넌트(SajuPage·AmuletPage·HomePage·Result·TabBar·App) 일체 미변경
- [ ] `granite.config.ts`·`package.json`·lock 파일 미변경 (D-02 신규 의존성 금지 검증)
- [ ] 위 모든 task acceptance_criteria 통과
</success_criteria>

<output>
After completion, create `.planning/phases/03-daily-one-card-core/03-01-SUMMARY.md` with:
- Files created/modified (마이그레이션 SQL 파일명에 적용 날짜 포함)
- Tarot_cards 22행 적용 확인 (Supabase Studio 캡처 또는 count 쿼리 결과)
- 22장 webp 합계 실측 결과 (RESEARCH A1 가정 검증 결과)
- RLS 정책 적용 확인 (anon select 22행 + INSERT 거부 검증)
- Phase 4·5 영향 노트 (fetchTarotCards 함수 시그니처 잠금, getCardImageUrl 시그니처 잠금)
- Wave 2 PLAN으로 넘어가는 인터페이스 명세 재확인
</output>
