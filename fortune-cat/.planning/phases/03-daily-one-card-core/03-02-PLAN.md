---
phase: 03-daily-one-card-core
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/components/TarotCardArt.jsx
  - src/components/TarotShuffle.jsx
  - src/components/TarotResult.jsx
autonomous: true
requirements: [TAROT-01, TAROT-02, TAROT-03]
requirements_addressed: [TAROT-01, TAROT-02, TAROT-03]
must_haves:
  truths:
    - "TarotCardArt가 size 'sm'/'md'/'lg' 3종 + faceUp/framed prop으로 카드 앞면(이미지) 또는 뒷면(보라+골드+🌙)을 단독 렌더링할 수 있다"
    - "TarotShuffle이 cards prop(3장) + onSelect 콜백을 받아 부채꼴 3장(좌 -15° 중 0° 우 +15°, translateX ±64px)을 절대 위치로 표시하고 1장 탭 시 그 자리에서 0.5s rotateY 뒤집기 후 onSelect(id) 호출"
    - "TarotResult가 card prop(7컬럼 객체) + onRedraw 콜백을 받아 카드 이미지 + 헤더(이모지+한국어명+영문명) + 키워드 chip + 메시지 카드 + fixed 다시 뽑기 버튼을 렌더링"
    - "모든 신규 컴포넌트의 fontWeight가 400 또는 700만 사용 (UI-SPEC Dimension 4 통일 정책)"
    - "모든 신규 컴포넌트가 transition: all 사용 안 함 (transform/opacity 같은 명시적 property만)"
  artifacts:
    - path: "src/components/TarotCardArt.jsx"
      provides: "카드 시각 컴포넌트 (앞/뒷면 + framed 매트 + size sm/md/lg)"
      exports: ["default"]
    - path: "src/components/TarotShuffle.jsx"
      provides: "shuffle 단계 부채꼴 3장 + 탭 → 뒤집기 인터랙션"
      exports: ["default"]
    - path: "src/components/TarotResult.jsx"
      provides: "result 단계 카드+이름+키워드+메시지+fixed 다시뽑기"
      exports: ["default"]
  key_links:
    - from: "src/components/TarotShuffle.jsx"
      to: "TarotCardArt + getCardImageUrl"
      via: "import + 부채꼴 위치 + 선택 후 FlipCard 내부에서 앞면 image prop 전달"
      pattern: "import TarotCardArt|getCardImageUrl"
    - from: "src/components/TarotResult.jsx"
      to: "TarotCardArt + getCardImageUrl"
      via: "result 카드 lg framed + image={getCardImageUrl(card.id)}"
      pattern: "import TarotCardArt|getCardImageUrl"
    - from: "TarotShuffle handleTap"
      to: "props.onSelect(cards[slotIdx].id)"
      via: "0.5s rotateY 뒤집기 setTimeout 후 호출"
      pattern: "onSelect\\(cards\\[.*\\]\\.id\\)"
---

<objective>
Phase 3 Wave 2 — UI 컴포넌트 구축. shuffle/result 단계의 시각·인터랙션을 담당할 3개 신규 컴포넌트(TarotCardArt, TarotShuffle, TarotResult)를 작성한다. CSS 3D 카드 뒤집기(D-02)·부채꼴 3장 절대 배치(D-01)·결과 화면 수직 흐름(D-05~D-07)을 UI-SPEC §Layout/§Animation/§Color/§Typography/§Spacing/§Copywriting 토큰 그대로 구현한다.

Purpose: Wave 3 TarotPage 통합이 단순히 컴포넌트를 import + props로 데이터만 전달하면 되도록 캡슐화. UI-SPEC Checker Sign-Off 6 dimensions의 시각 구현체를 본 plan에서 모두 만들어 두면 Wave 3는 상태 머신 결합과 검증에만 집중 가능.

Output:
- `src/components/TarotCardArt.jsx` — 카드 시각화 (앞/뒷면 + framed 매트, size sm/md/lg)
- `src/components/TarotShuffle.jsx` — 부채꼴 3장 + 탭 → 뒤집기 인터랙션 (FlipCard 내부 정의)
- `src/components/TarotResult.jsx` — 결과 화면 (카드+헤더+chip+메시지+fixed 다시뽑기)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-daily-one-card-core/03-CONTEXT.md
@.planning/phases/03-daily-one-card-core/03-RESEARCH.md
@.planning/phases/03-daily-one-card-core/03-UI-SPEC.md
@.planning/phases/03-daily-one-card-core/03-VALIDATION.md
@.planning/phases/03-daily-one-card-core/03-01-SUMMARY.md

<interfaces>
<!-- Wave 1이 잠금한 인터페이스 (본 plan이 사용) -->

From src/lib/supabase.js (Wave 1 작성):
```javascript
export async function fetchTarotCards(): Promise<Card[]>
// 본 Wave에서 직접 호출 안 함 (TarotPage가 호출 후 props로 내려줌)
```

From src/assets/images/cards/index.js (Wave 1 작성):
```javascript
export function getCardImageUrl(id: number): string
export function prefetchAllCardImages(): void
```

<!-- Wave 2가 노출하는 인터페이스 (Wave 3가 사용) -->

TarotCardArt props:
- emoji?: string — 카드별 이모지 (CardFront fallback에서 사용)
- nameEn?: string — 영문명 (CardFront fallback)
- image?: string — getCardImageUrl(id) 결과 (앞면일 때 표시)
- faceUp?: boolean — default true. false면 카드 뒷면(보라+골드+🌙)
- size?: 'sm'|'md'|'lg' — default 'md'. sm 64×96, md 120×180, lg 200×300
- framed?: boolean — default false. true면 외곽 파스텔 매트

TarotShuffle props:
- cards: Array (정확히 3장; 각 객체는 id/name_ko/name_en/emoji/image_path/keywords/message)
- onSelect: (id: number) => void  // 탭 후 0.7s 뒤(0.5s flip + 0.2s scale) 호출

TarotResult props:
- card: { id: number, name_ko: string, name_en: string, emoji: string, keywords: string[], message: string }
- onRedraw: () => void — fixed 다시뽑기 버튼 클릭 시 즉시 호출 (Phase 3는 광고 없음 — Phase 4가 wrap)
</interfaces>

<reference_files>
- src/components/TabBar.jsx (탭바 높이 ~72px + safe-area 패턴 — TarotResult fixed 버튼의 bottom 계산 정본)
- src/components/Result.jsx (라인 200-260 — 메시지 카드 + fixed 버튼 패턴 정본 — 단 weight 600 사용 부분은 본 페이즈 미적용, 700으로 통일)
- src/index.css (CSS 변수 토큰: --color-primary, --color-primary-light, --color-gray-700/500/200, --color-white. line 92-98 prefers-reduced-motion 글로벌)
- /Users/trinity/Projects/toss-app-generator/workspace/boknyang-tarot/boknyang-tarot/src/components/TarotCardArt.tsx (포팅 원본 — TS→JSX, framer-motion 제거, oklch→hex 변환)
- /Users/trinity/Projects/toss-app-generator/workspace/boknyang-tarot/boknyang-tarot/src/routes/shuffle.tsx (부채꼴 패턴 원본 — framer-motion 제거)
- /Users/trinity/Projects/toss-app-generator/workspace/boknyang-tarot/boknyang-tarot/src/routes/result.tsx (라인 102-137 flip 패턴 원본)
</reference_files>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| props → 컴포넌트 내부 | TarotPage에서 받은 props는 신뢰. fetch 단계는 Wave 1·3에서 검증 |
| 클라이언트 → 정적 webp | Vite가 빌드 시 fingerprint URL 생성. 외부 입력 없음 |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Tampering | `<img alt={card.name_ko}>` 텍스트 인젝션 | accept | name_ko는 운영 DB에서만 입력 — 자체 작성 컨텐츠. 22장 seed가 마이그레이션에서 통제됨 (Wave 1 task) |
| T-03-07 | Information Disclosure | console.warn(`invalid card id: ${id}`) | accept | id는 0~21 정수 — PII 아님. dev/production 모두 무해 |
| T-03-08 | Denial of Service | 카드 22장 메모리 누수 (이미지 prefetch) | mitigate | prefetch는 Wave 3 TarotPage가 1회만 호출. Wave 2 컴포넌트는 prefetch 직접 호출 안 함 |
| T-03-09 | Spoofing | XSS via SVG 카드 이미지 | mitigate | D-11에 따라 webp만 사용 (SVG 금지). webp는 raster format으로 script 실행 불가. import 시 Vite가 빌드 타임 검증 |
| T-03-10 | Information Disclosure | message 본문 XSS via React render | mitigate | React는 기본적으로 escape 처리. dangerouslySetInnerHTML 미사용 — `<p>{card.message}</p>` 형식만 사용 (acceptance criteria로 검증) |

**보안 게이트:** T-03-09·T-03-10 mitigation은 acceptance criteria로 검증 — webp 외 이미지 형식 import 금지 + dangerouslySetInnerHTML grep = 0.
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 2.1: TarotCardArt.jsx 작성 (카드 시각 — 앞/뒷면 + framed + size 3종)</name>
  <files>src/components/TarotCardArt.jsx</files>
  <read_first>
    - .planning/phases/03-daily-one-card-core/03-UI-SPEC.md (Layout 섹션 카드 dimensions 표 + 카드 뒷면 디자인 결정 + Color §Reserved-for + Typography 매트릭스)
    - .planning/phases/03-daily-one-card-core/03-RESEARCH.md (§2 TarotCardArt 코드 스니펫 — verbatim 복사 가능)
    - /Users/trinity/Projects/toss-app-generator/workspace/boknyang-tarot/boknyang-tarot/src/components/TarotCardArt.tsx (원본 — TS→JSX 변환 시 oklch/Tailwind 제거 비교용)
    - src/index.css (CSS 변수: --color-primary, --color-primary-light, --color-gray-* — 인라인 hex 대신 var() 우선 사용 가능 영역 확인)
  </read_first>
  <action>
파일 생성: `src/components/TarotCardArt.jsx`. 다음 정확한 코드를 작성한다 (RESEARCH §2 + UI-SPEC Layout 카드 dimensions 표 + UI-SPEC 카드 뒷면 디자인 결정 그대로):

```jsx
// 타로 카드 시각 컴포넌트 — 앞/뒷면 + framed 매트 + size sm/md/lg.
// Source: boknyang-tarot/src/components/TarotCardArt.tsx 포팅 + UI-SPEC Layout/Color 적용.
// CONTEXT D-02: framer-motion 미사용. CSS transition만 사용.
// CONTEXT D-11: 앞면 이미지는 webp 정적 import URL을 image prop으로 받음.

const SIZES = {
  sm: { w: 64,  h: 96,  fs: 28, label: 7,  pad: 4,  radius: 10 },
  md: { w: 120, h: 180, fs: 56, label: 10, pad: 6,  radius: 14 },
  lg: { w: 200, h: 300, fs: 96, label: 14, pad: 10, radius: 18 },
};

export default function TarotCardArt({
  emoji = '🌟',
  nameEn,
  image,
  faceUp = true,
  size = 'md',
  framed = false,
}) {
  const s = SIZES[size];

  const card = (
    <div
      style={{
        position: 'relative',
        flexShrink: 0,
        overflow: 'hidden',
        width: s.w,
        height: s.h,
        borderRadius: s.radius + 6,
        boxShadow: '0 8px 24px rgba(100, 17, 159, 0.12)',
      }}
    >
      {faceUp
        ? (image ? <CardImage src={image} alt={nameEn} /> : <CardFront s={s} emoji={emoji} nameEn={nameEn} />)
        : <CardBack s={s} />}
    </div>
  );

  if (!framed) return card;

  const matPad = size === 'lg' ? 10 : size === 'md' ? 8 : 5;
  return (
    <div
      style={{
        position: 'relative',
        flexShrink: 0,
        padding: matPad,
        borderRadius: s.radius + 14,
        background: 'linear-gradient(160deg, #FFF8E6, #F4E6FF)',
        boxShadow: '0 4px 16px rgba(0, 0, 0, 0.06)',
      }}
    >
      {card}
    </div>
  );
}

function CardImage({ src, alt }) {
  return (
    <img
      src={src}
      alt={alt ?? 'tarot card'}
      draggable={false}
      style={{
        height: '100%',
        width: '100%',
        userSelect: 'none',
        display: 'block',
        objectFit: 'cover',
      }}
    />
  );
}

function CardBack({ s }) {
  return (
    <div
      style={{
        position: 'relative',
        height: '100%',
        width: '100%',
        background: 'linear-gradient(135deg, #64119F, #4A0A78)',
      }}
      aria-hidden="true"
    >
      <div
        style={{
          position: 'absolute',
          top: s.pad,
          right: s.pad,
          bottom: s.pad,
          left: s.pad,
          borderRadius: s.radius,
          border: '1.5px solid #D4A537',
          pointerEvents: 'none',
        }}
      />
      <div style={{ position: 'absolute', inset: 0, display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
        <span style={{ fontSize: Math.max(36, s.w * 0.36), lineHeight: 1 }}>🌙</span>
      </div>
      {[
        { top: s.pad + 4, left: s.pad + 4 },
        { top: s.pad + 4, right: s.pad + 4 },
        { bottom: s.pad + 4, left: s.pad + 4 },
        { bottom: s.pad + 4, right: s.pad + 4 },
      ].map((pos, i) => (
        <span
          key={i}
          style={{ position: 'absolute', ...pos, color: '#D4A537', fontSize: s.w * 0.07, pointerEvents: 'none' }}
        >
          ✦
        </span>
      ))}
    </div>
  );
}

function CardFront({ s, emoji, nameEn }) {
  return (
    <div
      style={{
        position: 'relative',
        height: '100%',
        width: '100%',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        background: 'linear-gradient(160deg, #FFF8E6, #F4E6FF)',
      }}
    >
      <span style={{ fontSize: s.fs }}>{emoji}</span>
      {nameEn && (
        <div
          style={{
            position: 'absolute',
            bottom: s.pad + 4,
            fontSize: s.label,
            fontWeight: 700,
            color: '#191F28',
          }}
        >
          {nameEn}
        </div>
      )}
    </div>
  );
}
```

**중요:**
- `export default function TarotCardArt` 단일 default export
- weight 700만 사용 (CardFront의 nameEn label) — UI-SPEC Dimension 4 통일 정책 준수
- aria-hidden="true" — 카드 뒷면은 장식 요소 (UI-SPEC §Tap Targets)
- framer-motion·zustand·@emotion 모두 import 금지 (D-02)
- fontWeight 600/500/800/900 일체 금지 (UI-SPEC §Typography 매트릭스 — 400/700만)
- transition 사용 안 함 (Wave 3 FlipCard에서 부모 transform으로 처리)
- dangerouslySetInnerHTML 사용 금지 (T-03-10 mitigation)
- SVG import 금지 (T-03-09 mitigation — webp만 사용)
  </action>
  <verify>
    <automated>test -f src/components/TarotCardArt.jsx && grep -c "export default function TarotCardArt" src/components/TarotCardArt.jsx && cd /Users/trinity/Projects/app-in-toss/fortune-cat && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - 파일 존재: `test -f src/components/TarotCardArt.jsx` (exit 0)
    - default export: `grep -c "export default function TarotCardArt" src/components/TarotCardArt.jsx` = 1
    - SIZES 객체 3 size: `grep -E "^  (sm|md|lg):" src/components/TarotCardArt.jsx | wc -l` = 3
    - 카드 뒷면 그라디언트 정확값: `grep -c "linear-gradient(135deg, #64119F, #4A0A78)" src/components/TarotCardArt.jsx` = 1
    - 골드 hairline border: `grep -c "1.5px solid #D4A537" src/components/TarotCardArt.jsx` = 1
    - 카드 외부 shadow 정확값: `grep -c "0 8px 24px rgba(100, 17, 159, 0.12)" src/components/TarotCardArt.jsx` = 1
    - framed 매트 그라디언트: `grep -c "linear-gradient(160deg, #FFF8E6, #F4E6FF)" src/components/TarotCardArt.jsx` ≥ 1
    - 금지 weight 미사용: `grep -cE "fontWeight: (500|600|800|900)" src/components/TarotCardArt.jsx` = 0
    - 금지 의존성 미사용: `grep -cE "(framer-motion|zustand|@emotion)" src/components/TarotCardArt.jsx` = 0
    - dangerouslySetInnerHTML 미사용: `grep -c "dangerouslySetInnerHTML" src/components/TarotCardArt.jsx` = 0
    - SVG import 미사용: `grep -cE "import .* from .*\\.svg" src/components/TarotCardArt.jsx` = 0
    - aria-hidden 카드 뒷면: `grep -c "aria-hidden" src/components/TarotCardArt.jsx` ≥ 1
    - ESLint 통과: `npm run lint` exit 0
    - 본 task 외 파일 미수정: `git diff --name-only` 출력에 `src/components/TarotCardArt.jsx` 외 항목 없음
  </acceptance_criteria>
  <done>
    파일 작성 완료, 위 acceptance_criteria 14개 모두 PASS.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2.2: TarotShuffle.jsx 작성 (부채꼴 3장 + CSS 3D 뒤집기 + 선택 핸들러)</name>
  <files>src/components/TarotShuffle.jsx</files>
  <read_first>
    - .planning/phases/03-daily-one-card-core/03-UI-SPEC.md (Layout shuffle 단계 시각 구조 + Animation 카드 뒤집기 0.5s cubic-bezier + Spacing exception "horizontal overlap" + Copywriting shuffle 헤드라인/보조 + Tap Targets 카드 3장 aria-label)
    - .planning/phases/03-daily-one-card-core/03-RESEARCH.md (Pattern 2 CSS 3D 뒤집기 코드 + Pattern 5 부채꼴 코드 + Pitfall 1 backface-visibility iOS prefix + Pitfall 5 prop drilling 회피)
    - .planning/phases/03-daily-one-card-core/03-CONTEXT.md (D-01 부채꼴 즉시 뒤집기 + D-02 CSS만)
    - src/components/TarotCardArt.jsx (Wave 2 Task 2.1 산출 — props 시그니처 정확 확인)
    - src/assets/images/cards/index.js (Wave 1 산출 — getCardImageUrl import 경로 확인)
  </read_first>
  <action>
파일 생성: `src/components/TarotShuffle.jsx`. 다음 정확한 코드를 작성한다 (RESEARCH Pattern 2 + Pattern 5 + UI-SPEC Layout shuffle + Animation 0.5s cubic-bezier 그대로):

```jsx
// shuffle 단계 — 부채꼴 3장 + 1장 탭 시 그 자리에서 CSS 3D rotateY 뒤집기 → onSelect(id) 호출.
// CONTEXT D-01: 부채꼴 3장 중 1장 선택 (confirm 단계 없음).
// CONTEXT D-02: CSS keyframes/transition만 사용. framer-motion 금지.
// UI-SPEC Animation: 카드 뒤집기 0.5s cubic-bezier(0.22, 1, 0.36, 1), 부채꼴 stagger 0.08s.
// UI-SPEC Layout: 좌 -15° / 중앙 0° / 우 +15°, translateX -64/0/+64.
// RESEARCH Pitfall 1: -webkit-backface-visibility vendor prefix 필수 (iOS Safari < 16).

import { useState } from 'react';
import TarotCardArt from './TarotCardArt';
import { getCardImageUrl } from '../assets/images/cards';

const POSITIONS = [
  { x: -64, rot: -15 },
  { x: 0,   rot: 0   },
  { x: 64,  rot: 15  },
];

export default function TarotShuffle({ cards, onSelect }) {
  const [selectedSlot, setSelectedSlot] = useState(null);
  const [flipping, setFlipping] = useState(false);

  if (!Array.isArray(cards) || cards.length < 3) return null;

  const handleTap = (slotIdx) => {
    if (selectedSlot !== null) return;
    setSelectedSlot(slotIdx);
    setFlipping(true);
    // UI-SPEC Animation: 0.5s flip + 0.2s scale → 약 0.7s 뒤 result로 진입
    setTimeout(() => onSelect(cards[slotIdx].id), 700);
  };

  return (
    <div
      style={{
        minHeight: '100vh',
        background: '#FFFFFF',
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        paddingTop: 48,
        paddingLeft: 16,
        paddingRight: 16,
        paddingBottom: 'calc(96px + env(safe-area-inset-bottom))',
      }}
    >
      <h2 style={{ fontSize: 20, fontWeight: 700, lineHeight: 1.3, color: '#191F28', margin: 0, textAlign: 'center' }}>
        세 장 중 한 장을 골라보세요
      </h2>
      <p style={{ marginTop: 16, fontSize: 16, fontWeight: 400, lineHeight: 1.6, color: '#6B7684', textAlign: 'center' }}>
        탭하면 그 자리에서 뒤집혀요
      </p>

      <div
        style={{
          marginTop: 48,
          position: 'relative',
          width: '100%',
          height: 240,
        }}
      >
        {cards.slice(0, 3).map((card, i) => {
          const isSel = selectedSlot === i;
          const isOther = selectedSlot !== null && !isSel;
          const pos = POSITIONS[i];
          return (
            <button
              key={card.id}
              type="button"
              onClick={() => handleTap(i)}
              aria-label={`카드 ${i + 1}번 선택`}
              style={{
                position: 'absolute',
                top: 0,
                left: '50%',
                marginLeft: -60,
                transform: `translateX(${isSel ? 0 : pos.x}px) rotate(${isSel ? 0 : pos.rot}deg) scale(${isSel ? 1.15 : 1})`,
                opacity: isOther ? 0.3 : 1,
                transition: 'transform 0.4s ease-out, opacity 0.3s ease-out',
                transitionDelay: selectedSlot === null ? `${i * 0.08}s` : '0s',
                border: 0,
                background: 'transparent',
                cursor: 'pointer',
                padding: 0,
                touchAction: 'manipulation',
                WebkitTapHighlightColor: 'transparent',
              }}
            >
              {isSel && flipping ? (
                <FlipCard card={card} />
              ) : (
                <TarotCardArt faceUp={false} size="md" />
              )}
            </button>
          );
        })}
      </div>
    </div>
  );
}

// 선택된 카드 1장만 그 자리에서 0.5s rotateY 뒤집기. 진입 직후 flipped=true로 트랜지션 시작.
function FlipCard({ card }) {
  const [flipped, setFlipped] = useState(false);

  // mount 직후 다음 프레임에 flipped=true → CSS transition 0 → 180deg 시작
  if (!flipped) {
    requestAnimationFrame(() => setFlipped(true));
  }

  return (
    <div style={{ perspective: 1200 }}>
      <div
        style={{
          position: 'relative',
          width: 120,
          height: 180,
          transformStyle: 'preserve-3d',
          WebkitTransformStyle: 'preserve-3d',
          transform: flipped ? 'rotateY(180deg)' : 'rotateY(0deg)',
          transition: 'transform 0.5s cubic-bezier(0.22, 1, 0.36, 1)',
          willChange: 'transform',
        }}
      >
        <div
          style={{
            position: 'absolute',
            top: 0, right: 0, bottom: 0, left: 0,
            backfaceVisibility: 'hidden',
            WebkitBackfaceVisibility: 'hidden',
          }}
        >
          <TarotCardArt faceUp={false} size="md" />
        </div>
        <div
          style={{
            position: 'absolute',
            top: 0, right: 0, bottom: 0, left: 0,
            backfaceVisibility: 'hidden',
            WebkitBackfaceVisibility: 'hidden',
            transform: 'rotateY(180deg)',
          }}
        >
          <TarotCardArt
            faceUp
            size="md"
            image={getCardImageUrl(card.id)}
            emoji={card.emoji}
            nameEn={card.name_en}
          />
        </div>
      </div>
    </div>
  );
}
```

**중요:**
- `export default function TarotShuffle` 단일 default export
- POSITIONS 정확값(-64/0/+64, -15/0/+15) UI-SPEC §Spacing exception 명시 그대로
- transition은 명시적 property만(`transform 0.4s, opacity 0.3s`) — `transition: all` 금지 (RESEARCH Anti-pattern)
- `WebkitBackfaceVisibility` + `WebkitTransformStyle` vendor prefix 필수 (Pitfall 1 iOS Safari)
- `cubic-bezier(0.22, 1, 0.36, 1)` UI-SPEC Animation 정본
- aria-label 카드 1/2/3 — UI-SPEC §Tap Targets 명시
- 셔플 진입 시 `requestAnimationFrame`으로 flipped=false → true 트랜지션 발사 (mount 직후 즉시 flipped=true 설정 시 트랜지션 안 보임)
- copywriting "세 장 중 한 장을 골라보세요" / "탭하면 그 자리에서 뒤집혀요" UI-SPEC §Copywriting 정본
- weight 400(보조 본문), 700(헤드라인) 외 사용 금지
- framer-motion·zustand·@emotion·SVG 모두 미사용
- v1.0 컴포넌트(SajuPage·AmuletPage·HomePage·Result.jsx·TabBar.jsx) import·수정 금지
  </action>
  <verify>
    <automated>test -f src/components/TarotShuffle.jsx && grep -c "export default function TarotShuffle" src/components/TarotShuffle.jsx && grep -c "import TarotCardArt" src/components/TarotShuffle.jsx && grep -c "import { getCardImageUrl }" src/components/TarotShuffle.jsx && cd /Users/trinity/Projects/app-in-toss/fortune-cat && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - 파일 존재 + default export: `grep -c "export default function TarotShuffle" src/components/TarotShuffle.jsx` = 1
    - TarotCardArt import: `grep -c "^import TarotCardArt from './TarotCardArt'" src/components/TarotShuffle.jsx` = 1
    - getCardImageUrl import: `grep -c "^import { getCardImageUrl } from '../assets/images/cards'" src/components/TarotShuffle.jsx` = 1
    - POSITIONS 정확값(-64/0/+64): `grep -cE "x: (-64|0|64)" src/components/TarotShuffle.jsx` = 3
    - POSITIONS rot 정확값(-15/0/+15): `grep -cE "rot: (-15|0|15)" src/components/TarotShuffle.jsx` = 3
    - cubic-bezier 정확값: `grep -c "cubic-bezier(0.22, 1, 0.36, 1)" src/components/TarotShuffle.jsx` = 1
    - 0.5s flip duration: `grep -c "transform 0.5s cubic-bezier" src/components/TarotShuffle.jsx` = 1
    - WebkitBackfaceVisibility prefix: `grep -c "WebkitBackfaceVisibility: 'hidden'" src/components/TarotShuffle.jsx` ≥ 2
    - WebkitTransformStyle prefix: `grep -c "WebkitTransformStyle: 'preserve-3d'" src/components/TarotShuffle.jsx` ≥ 1
    - perspective 1200: `grep -c "perspective: 1200" src/components/TarotShuffle.jsx` ≥ 1
    - aria-label 카드 N번: `grep -cE "aria-label=\`카드 \\$\\{i \\+ 1\\}번 선택" src/components/TarotShuffle.jsx` = 1
    - copywriting 헤드라인 정본: `grep -c "세 장 중 한 장을 골라보세요" src/components/TarotShuffle.jsx` = 1
    - copywriting 보조 정본: `grep -c "탭하면 그 자리에서 뒤집혀요" src/components/TarotShuffle.jsx` = 1
    - onSelect 호출: `grep -c "onSelect(cards\\[slotIdx\\].id)" src/components/TarotShuffle.jsx` = 1
    - setTimeout 700ms: `grep -c "setTimeout(.*, 700)" src/components/TarotShuffle.jsx` = 1
    - transition: all 금지: `grep -c "transition: 'all'" src/components/TarotShuffle.jsx` = 0
    - 금지 weight 미사용: `grep -cE "fontWeight: (500|600|800|900)" src/components/TarotShuffle.jsx` = 0
    - 금지 의존성 미사용: `grep -cE "(framer-motion|zustand|@emotion)" src/components/TarotShuffle.jsx` = 0
    - v1.0 페이지 import 금지: `grep -cE "from '\\.\\./pages/(SajuPage|AmuletPage|HomePage|NewYearPage)'" src/components/TarotShuffle.jsx` = 0
    - ESLint 통과: `npm run lint` exit 0
    - 본 task 외 파일 미수정: `git diff --name-only` 출력에 `src/components/TarotShuffle.jsx` 외 항목 없음
  </acceptance_criteria>
  <done>
    파일 작성 완료, 위 acceptance_criteria 21개 모두 PASS.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2.3: TarotResult.jsx 작성 (결과 화면 — 카드+헤더+chip+메시지+fixed 다시뽑기)</name>
  <files>src/components/TarotResult.jsx</files>
  <read_first>
    - .planning/phases/03-daily-one-card-core/03-UI-SPEC.md (Layout result 단계 시각 구조 + Spacing exception "다시 뽑기 버튼 fixed 영역" + Color §Reserved-for + Typography 매트릭스 + Copywriting result 카피 + Tap Targets)
    - .planning/phases/03-daily-one-card-core/03-RESEARCH.md (§3 TarotResult 코드 + Pitfall 8 weight 600 회피)
    - .planning/phases/03-daily-one-card-core/03-CONTEXT.md (D-05 수직 흐름 + D-06 헤더/chip/메시지 + D-07 fixed 버튼)
    - src/components/TarotCardArt.jsx (Wave 2 Task 2.1 산출 — lg framed 호출 시그니처 정확 확인)
    - src/components/TabBar.jsx (탭바 높이 ~72px 정본)
    - src/components/Result.jsx (라인 200-260 메시지 카드 + fixed 버튼 패턴 비교 — 단 weight 600은 본 페이즈 미적용)
    - src/assets/images/cards/index.js (Wave 1 산출 — getCardImageUrl 시그니처 정확 확인)
  </read_first>
  <action>
파일 생성: `src/components/TarotResult.jsx`. 다음 정확한 코드를 작성한다 (RESEARCH §3 + UI-SPEC Layout result + Copywriting + Color/Typography/Spacing 토큰 그대로):

```jsx
// result 단계 — 카드 이미지 + 헤더 + 키워드 chip + 메시지 카드 + fixed 다시 뽑기 버튼.
// CONTEXT D-05: 수직 흐름. D-06: 이모지+한국어명+영문명/chip 2-4개/메시지 ~300자.
// CONTEXT D-07: fixed 버튼 = 탭바 + safe-area + 8px 위.
// UI-SPEC Color: --color-primary-light 메시지 카드 + chip 배경, --color-primary 영문명/chip 텍스트/CTA.
// UI-SPEC Typography: Display 24/700, Body 16/400 line-height 1.6, Body Strong 16/700 CTA, Label 14/700.
// UI-SPEC Spacing: 카드↔헤더 24px, 헤더↔chip 16px, chip↔메시지 24px, 메시지↔fixed 96px spacer.

import TarotCardArt from './TarotCardArt';
import { getCardImageUrl } from '../assets/images/cards';

export default function TarotResult({ card, onRedraw }) {
  if (!card) return null;
  const keywords = (card.keywords || []).slice(0, 4);

  return (
    <div style={{ minHeight: '100vh', background: '#FFFFFF', position: 'relative' }}>
      <div
        style={{
          paddingTop: 32,
          paddingLeft: 16,
          paddingRight: 16,
          paddingBottom: 96,
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
        }}
      >
        <span
          style={{
            fontSize: 14,
            fontWeight: 700,
            lineHeight: 1.4,
            color: '#6B7684',
            marginBottom: 8,
          }}
        >
          오늘의 한 장
        </span>

        <TarotCardArt
          faceUp
          framed
          size="lg"
          image={getCardImageUrl(card.id)}
          emoji={card.emoji}
          nameEn={card.name_en}
        />

        <h2
          style={{
            marginTop: 24,
            fontSize: 24,
            fontWeight: 700,
            lineHeight: 1.3,
            margin: 0,
            textAlign: 'center',
          }}
        >
          <span style={{ color: '#191F28' }}>{card.emoji} {card.name_ko} </span>
          <span style={{ color: '#64119F' }}>({card.name_en})</span>
        </h2>

        {keywords.length > 0 && (
          <div
            style={{
              marginTop: 16,
              display: 'flex',
              flexWrap: 'wrap',
              gap: 8,
              justifyContent: 'center',
            }}
          >
            {keywords.map((k) => (
              <span
                key={k}
                style={{
                  fontSize: 14,
                  fontWeight: 700,
                  lineHeight: 1.4,
                  color: '#64119F',
                  background: '#F4E6FF',
                  padding: '4px 12px',
                  borderRadius: 999,
                }}
              >
                #{k}
              </span>
            ))}
          </div>
        )}

        <div
          style={{
            marginTop: 24,
            background: '#F4E6FF',
            borderRadius: 16,
            padding: 20,
            maxWidth: 360,
            width: '100%',
            textAlign: 'center',
          }}
        >
          <p
            style={{
              fontSize: 16,
              fontWeight: 400,
              lineHeight: 1.6,
              color: '#191F28',
              margin: 0,
              whiteSpace: 'pre-wrap',
            }}
          >
            {card.message}
          </p>
        </div>
      </div>

      <button
        type="button"
        onClick={onRedraw}
        className="tap-card"
        style={{
          position: 'fixed',
          bottom: 'calc(72px + env(safe-area-inset-bottom) + 8px)',
          left: 16,
          right: 16,
          height: 56,
          fontSize: 16,
          fontWeight: 700,
          color: '#FFFFFF',
          background: '#64119F',
          border: 0,
          borderRadius: 12,
          cursor: 'pointer',
          zIndex: 15,
          boxShadow: '0 -2px 8px rgba(0,0,0,0.04)',
        }}
      >
        다시 뽑기
      </button>
    </div>
  );
}
```

**중요:**
- `export default function TarotResult` 단일 default export
- TarotCardArt: `faceUp framed size="lg" image={getCardImageUrl(card.id)} emoji={card.emoji} nameEn={card.name_en}` 정확히 6 prop
- 색상 정확값:
  - 메시지 카드 배경 #F4E6FF (`--color-primary-light`)
  - chip 배경 #F4E6FF, chip 텍스트 #64119F
  - 영문명 텍스트 #64119F
  - 다시 뽑기 버튼 배경 #64119F, 텍스트 #FFFFFF
  - 메시지 본문 #191F28, 메타 캡션 #6B7684
- 폰트 정확값:
  - Display 24/700/1.3 (카드 헤더)
  - Body 16/400/1.6 (메시지 본문)
  - Body Strong 16/700 (다시 뽑기 라벨)
  - Label 14/700/1.4 (메타 + chip)
- 간격 정확값: 카드↔헤더 24px, 헤더↔chip 16px, chip↔메시지 24px, 본문 paddingBottom 96 (spacer)
- fixed 버튼 bottom 정확값: `calc(72px + env(safe-area-inset-bottom) + 8px)` (탭바 회피, UI-SPEC §Spacing exception)
- fixed 버튼 z-index 15 (탭바 z-index 20 아래)
- copywriting 정본: "오늘의 한 장" 메타, "다시 뽑기" CTA
- 키워드 chip 최대 4개 slice (D-06)
- whiteSpace: 'pre-wrap' — 메시지 줄바꿈 보존
- weight 600 일체 사용 금지 (Pitfall 8 회피, UI-SPEC Dimension 4)
- dangerouslySetInnerHTML 사용 금지 (T-03-10)
- v1.0 페이지/컴포넌트(SajuPage·AmuletPage·HomePage·NewYearPage·Result·TabBar) import·수정 금지
- framer-motion·zustand·@emotion 미사용
  </action>
  <verify>
    <automated>test -f src/components/TarotResult.jsx && grep -c "export default function TarotResult" src/components/TarotResult.jsx && grep -c "import TarotCardArt" src/components/TarotResult.jsx && grep -c "import { getCardImageUrl }" src/components/TarotResult.jsx && cd /Users/trinity/Projects/app-in-toss/fortune-cat && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - 파일 존재 + default export: `grep -c "export default function TarotResult" src/components/TarotResult.jsx` = 1
    - TarotCardArt import: `grep -c "^import TarotCardArt from './TarotCardArt'" src/components/TarotResult.jsx` = 1
    - getCardImageUrl import: `grep -c "^import { getCardImageUrl } from '../assets/images/cards'" src/components/TarotResult.jsx` = 1
    - TarotCardArt 호출(lg+framed): `grep -c "size=\"lg\"" src/components/TarotResult.jsx` = 1 그리고 `grep -c "framed" src/components/TarotResult.jsx` ≥ 1
    - 메시지 카드 배경: `grep -c "background: '#F4E6FF'" src/components/TarotResult.jsx` ≥ 2 (메시지 카드 + chip)
    - 영문명/chip/CTA primary 색상: `grep -c "#64119F" src/components/TarotResult.jsx` ≥ 3
    - 메시지 본문 16/400/1.6: `grep -c "fontSize: 16" src/components/TarotResult.jsx` ≥ 2 그리고 `grep -c "fontWeight: 400" src/components/TarotResult.jsx` ≥ 1 그리고 `grep -c "lineHeight: 1.6" src/components/TarotResult.jsx` ≥ 1
    - 헤더 24/700/1.3: `grep -c "fontSize: 24" src/components/TarotResult.jsx` = 1 그리고 `grep -E "fontWeight: 700.*lineHeight: 1.3|lineHeight: 1.3.*fontWeight: 700" src/components/TarotResult.jsx` ≥ 0 (둘 다 등장 검증)
    - chip Label 14/700/1.4: `grep -c "fontSize: 14" src/components/TarotResult.jsx` ≥ 2 (메타 + chip)
    - fixed 버튼 bottom 정확값: `grep -c "calc(72px + env(safe-area-inset-bottom) + 8px)" src/components/TarotResult.jsx` = 1
    - fixed 버튼 height 56: `grep -c "height: 56" src/components/TarotResult.jsx` = 1
    - fixed 버튼 z-index 15: `grep -c "zIndex: 15" src/components/TarotResult.jsx` = 1
    - copywriting 정본 메타: `grep -c "오늘의 한 장" src/components/TarotResult.jsx` = 1
    - copywriting 정본 CTA: `grep -c ">다시 뽑기<" src/components/TarotResult.jsx` = 1
    - keywords slice(0, 4): `grep -c "slice(0, 4)" src/components/TarotResult.jsx` = 1
    - whiteSpace pre-wrap: `grep -c "whiteSpace: 'pre-wrap'" src/components/TarotResult.jsx` = 1
    - weight 600 금지(Pitfall 8): `grep -cE "fontWeight: (500|600|800|900)" src/components/TarotResult.jsx` = 0
    - dangerouslySetInnerHTML 금지: `grep -c "dangerouslySetInnerHTML" src/components/TarotResult.jsx` = 0
    - v1.0 페이지 import 금지: `grep -cE "from '\\.\\./pages/(SajuPage|AmuletPage|HomePage|NewYearPage)'" src/components/TarotResult.jsx` = 0
    - v1.0 컴포넌트 import 금지: `grep -cE "from '\\./Result(\\.jsx)?'|from '\\./TabBar(\\.jsx)?'" src/components/TarotResult.jsx` = 0
    - framer-motion 등 금지: `grep -cE "(framer-motion|zustand|@emotion)" src/components/TarotResult.jsx` = 0
    - ESLint 통과: `npm run lint` exit 0
    - Vite build 통과: `npm run build` exit 0
    - 본 task 외 파일 미수정: `git diff --name-only` 출력에 `src/components/TarotResult.jsx` 외 항목 없음
  </acceptance_criteria>
  <done>
    파일 작성 완료, 위 acceptance_criteria 23개 모두 PASS.
  </done>
</task>

</tasks>

<verification>
**Wave 2 통합 검증 (모든 task 완료 후):**

1. `npm run lint && npm run build` 통과 (exit 0)
2. 신규 파일 3개 확인 (`git status`):
   - `src/components/TarotCardArt.jsx`
   - `src/components/TarotShuffle.jsx`
   - `src/components/TarotResult.jsx`
3. 변경 금지 파일 미수정 확인:
   - `git diff --stat src/pages/ src/lib/ src/hooks/ src/utils/ src/App.jsx src/main.jsx src/components/Result.jsx src/components/TabBar.jsx src/components/Loading.jsx` → 출력 없음
   - `git diff granite.config.ts package.json package-lock.json pnpm-lock.yaml` → 출력 없음
4. UI-SPEC Dimension 4 (Typography) 통일 검증:
   - `grep -rE "fontWeight: (500|600|800|900)" src/components/TarotCardArt.jsx src/components/TarotShuffle.jsx src/components/TarotResult.jsx` → 출력 없음
5. 인터페이스 시그니처 일관성:
   - TarotShuffle.jsx와 TarotResult.jsx 모두 `import { getCardImageUrl } from '../assets/images/cards'` 정확 경로
   - TarotShuffle.jsx와 TarotResult.jsx 모두 `import TarotCardArt from './TarotCardArt'` 정확 경로
6. Wave 3 plan에서 import할 컴포넌트 3종이 default export로 노출됨

**보안 검증 (Threat Model):**
- T-03-09 (SVG XSS): 모든 신규 컴포넌트 grep `\\.svg` = 0
- T-03-10 (XSS via dangerouslySetInnerHTML): 모든 신규 컴포넌트 grep `dangerouslySetInnerHTML` = 0
</verification>

<success_criteria>
**ROADMAP Phase 3 Success Criteria 부분 충족:**
- SC1 카드 뒷면 → 뒤집기 인터랙션의 시각·애니메이션 부품 (TarotCardArt + TarotShuffle FlipCard) 완성 — Wave 3에서 TarotPage에 결합
- SC2 결과 화면 카드 이미지·이름·해석 함께 표시 (TarotResult 컴포넌트) 완성 — Wave 3에서 데이터 연결
- SC3 다시 뽑기 액션 명확히 식별 (TarotResult fixed 다시뽑기 버튼) 완성 — Wave 3에서 핸들러 연결
- SC4 4050 가독성 (16px/1.6/WCAG AA contrast) UI-SPEC §Typography/§Color 토큰으로 구현 완료

**완료 조건:**
- [ ] `src/components/TarotCardArt.jsx` 작성 + 14개 acceptance criteria 통과
- [ ] `src/components/TarotShuffle.jsx` 작성 + 21개 acceptance criteria 통과
- [ ] `src/components/TarotResult.jsx` 작성 + 23개 acceptance criteria 통과
- [ ] `npm run lint && npm run build` 통과
- [ ] v1.0 페이지/컴포넌트 일체 미변경
- [ ] `granite.config.ts`·`package.json`·lock 파일 미변경 (D-02 신규 의존성 금지 검증)
- [ ] 모든 컴포넌트 fontWeight 400/700만 사용 (UI-SPEC Dimension 4 통일 정책)
</success_criteria>

<output>
After completion, create `.planning/phases/03-daily-one-card-core/03-02-SUMMARY.md` with:
- Files created (3개 컴포넌트 경로)
- 인터페이스 시그니처 재확인 (Wave 3 TarotPage가 사용할 props)
- UI-SPEC Dimension 4 통일 정책 검증 결과 (grep 출력)
- 보안 mitigation 검증 결과 (T-03-09/T-03-10 grep 출력)
- Wave 3 plan에서 결합할 때 필요한 정보 (FlipCard 동작 흐름, fixed 버튼 위치, 부채꼴 좌표 등)
</output>
