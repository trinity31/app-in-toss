---
milestone: v1.1
milestone_name: 복냥타로 통합 (별도 탭)
created: 2026-05-04
status: pending
total_items: 22
sources:
  - .planning/phases/02-tab-bar-navigation-shell/02-VERIFICATION.md
  - .planning/phases/04-daily-lock-and-storage/04-VERIFICATION.md
  - .planning/phases/05-share-and-analytics/05-VERIFICATION.md
  - .planning/phases/05-share-and-analytics/05-HUMAN-UAT.md (진행 추적용 거울)
phase_03_gap: implicit_pass_recommended
---

# v1.1 인간 검증 체크리스트 — 복냥타로 통합

**Audit 보고서:** `.planning/v1.1-MILESTONE-AUDIT.md`
**진행 방식:** 각 항목 디바이스/브라우저에서 통과 후 `[ ]` → `[x]` 체크 + 결과 칸에 메모. 최종 22/22 통과 시 milestone archive 가능.

## 환경별 셋업

| 환경 | 용도 | 항목 수 |
|------|------|---------|
| **AIT WebView 디바이스** (실 토스 앱 또는 미니앱 미리보기) | Toss Storage 영속, 공유 시트, Firebase 실수신, displayName 노출 | 13 |
| **iPhone 디바이스** (notch / Dynamic Island) | safe-area, 시각 가독성 | 1 |
| **일반 브라우저** (`npm run dev`) | graceful degradation | 2 |
| **DevTools (Storage 변조 시나리오)** | 손상 데이터 자동 정리 | 1 |
| **디바이스 시각 변경** | KST 자정 lock 해제 | 1 |
| **v1.0 회귀** (디바이스 또는 dev) | 사주/딥리딩/부적 흐름 무회귀 | 4 |

> 실 검증은 같은 디바이스에서 시나리오 묶음으로 한 번에 수행하면 효율적. 권장 순서: § 2 (탭바 시각) → § 3 (DAILY lock) → § 4 (Analytics 3종 + 공유) → § 5 (회귀 + graceful).

---

## § 1. 사전 빌드/배포

```bash
# AIT 빌드 (토스 미니앱 콘솔 미리보기용)
cd fortune-cat && npm run build  # vite build → dist/
# AIT 패키징
# (granite CLI 또는 토스 미니앱 콘솔 가이드에 따라 fortune-cat.ait 업로드)
```

- [ ] AIT 빌드 성공 (`dist/` + `fortune-cat.ait`) 산출물 확인
- [ ] 토스 미니앱 콘솔 또는 디바이스 sandbox 배포 완료
- [ ] Firebase Analytics DebugView 활성화 (디바이스 식별자 등록)

---

## § 2. Phase 02 — 하단 탭바 네비게이션 셸 (5건)

> 출처: [02-VERIFICATION.md](.planning/phases/02-tab-bar-navigation-shell/02-VERIFICATION.md) `human_verification:` 섹션

| # | REQ | Test | 환경 | 결과 |
|---|-----|------|------|------|
| **UAT-02-1** | NAV-03 / D-05 | `/saju`, `/newyear`, `/amulet` 진입 시 탭바가 화면에 노출되지 **않는다** (풀스크린 흐름 가림 없음) | AIT/dev | [ ] |
| **UAT-02-2** | NAV-01 (SC1) | `/` ↔ `/tarot` 탭 한 번으로 즉각 전환 (transition 없이 화면 갱신, 4050 사용자 인지 가능) | AIT | [ ] |
| **UAT-02-3** | NAV-02 (SC2) | active = `colors.blue500` + 아이콘 fill, inactive = `colors.grey500` + 아이콘 stroke. 별=사주, 카드=타로 의미 가독성 + hit area 충분 | AIT | [ ] |
| **UAT-02-4** | NAV-03 (SC3) | HomePage 메뉴 → 사주/신년운세/부적 4개 흐름이 v1.0과 동일하게 결과 화면까지 진행 (회귀 0) | AIT/dev | [ ] |
| **UAT-02-5** | D-06 | iPhone 홈 인디케이터(notch / Dynamic Island)가 탭 버튼을 가리지 않음 (`padding-bottom: env(safe-area-inset-bottom)` 적용) | iPhone | [ ] |

---

## § 3. Phase 04 — 데일리 lock + 영속 저장 + 자정 리셋 (8건)

> 출처: [04-VERIFICATION.md](.planning/phases/04-daily-lock-and-storage/04-VERIFICATION.md) `human_verification:` 섹션

| # | REQ | Test | 환경 | 결과 |
|---|-----|------|------|------|
| **UAT-04-1** | DAILY-01 | 카드 1장 뽑은 후 앱 종료 → 같은 날 타로 탭 재진입 → intro/shuffle 건너뛰고 즉시 result (같은 카드) | AIT | [ ] |
| **UAT-04-2** | DAILY-02 | 디바이스 날짜를 다음날로 변경 → 타로 탭 재진입 → intro 화면 + CTA 라벨 `오늘의 카드 뽑기 ✨` | AIT (시각 변경) | [ ] |
| **UAT-04-3** | DAILY-03 | 카드 1장 뽑은 후 새로고침 / 앱 재시작 → 즉시 result 직진 (같은 카드, 같은 해석) | AIT | [ ] |
| **UAT-04-4** | (graceful) | `npm run dev` 일반 브라우저 진입 → throw 없이 intro 정상 노출, 카드 뽑기 → result 동작 (메모리 기반, lock 미발동, 새로고침 시 다시 intro) | dev browser | [ ] |
| **UAT-04-5** | D-13 | result에서 `처음으로` → intro 진입 시 CTA 라벨이 `오늘의 카드 다시 보기 ✨` 로 자동 변경 | AIT | [ ] |
| **UAT-04-6** | D-14 | UAT-04-5 상태에서 CTA 탭 → shuffle 단계 건너뛰고 result 직진 (저장된 카드 표시) | AIT | [ ] |
| **UAT-04-7** | NAV-03 회귀 | 사주 / 신년운세 / 부적 / 딥리딩 4개 흐름 v1.0 동작 그대로 (라우팅·결제·광고 무영향) | AIT/dev | [ ] |
| **UAT-04-8** | (자동 복구) | DevTools로 Toss Storage(또는 localStorage) `FORTUNE_CAT_TAROT_TODAY_DRAW` 를 `'invalid_json'` 또는 `{date:'bad', card_id:99}` 로 변조 → 새로고침 → 손상 데이터 자동 정리 + intro 정상 노출 | DevTools | [ ] |

---

## § 4. Phase 05 — 공유 + Analytics 마무리 (9건)

> 출처: [05-VERIFICATION.md](.planning/phases/05-share-and-analytics/05-VERIFICATION.md) `human_verification:` 섹션
> 진행 추적 거울: [05-HUMAN-UAT.md](.planning/phases/05-share-and-analytics/05-HUMAN-UAT.md)

| # | REQ | Test | 환경 | 결과 |
|---|-----|------|------|------|
| **UAT-05-1** | ANL-01 (cold) | AIT 디바이스 타로 탭 진입 → Firebase DebugView/Realtime에 `tarot_view` + `already_drawn=false` 수신 (1건) | AIT + Firebase 콘솔 | [ ] |
| **UAT-05-2** | ANL-02 | 카드 1장 뽑기 → Firebase 콘솔에 `card_drawn` + `card_id`(1~22) + `slot`(0/1/2) 수신 | AIT + Firebase 콘솔 | [ ] |
| **UAT-05-3** | WR-01 fix | 같은 날 재진입 → `tarot_view` + `already_drawn=true` 가 **1회만** 발화 (useRef 가드 동작 — 중복 차단) | AIT + Firebase DebugView (이벤트 카운트) | [ ] |
| **UAT-05-4** | SHARE-01 | result `공유하기 ✨` 탭 → 토스 공유 시트 노출 + 메시지에 헤드라인 `[복냥타로] 오늘의 카드 — {name_ko} · {name_en}` + 80자 본문 + getTossShareLink URL 모두 포함 | AIT (디바이스) | [ ] |
| **UAT-05-5** | ANL-03 | 공유 완료 → Firebase 콘솔에 `card_shared` + `card_id` + `with_link=true` 수신 | AIT + Firebase 콘솔 | [ ] |
| **UAT-05-6** | (silent) | 공유 시트에서 사용자가 취소 → `card_shared` 이벤트 발화 안 됨 + 사용자 알림 없음 + `console.error` 만 출력 (HomePage silent 패턴) | AIT + Firebase 콘솔 (미수신 확인) | [ ] |
| **UAT-05-7** | (graceful) | dev 일반 브라우저에서 result `공유하기 ✨` 탭 → `getTossShareLink`/`share` throw 해도 페이지 정상 표시 + 사용자 알림 없음 + `console.error` 만 (REVIEW WR-02 인지 trade-off 동작 확인) | dev browser | [ ] |
| **UAT-05-8** | D-04 | AIT 빌드 산출물의 `displayName` 이 토스 미니앱 카드/리스트/공유 미리보기에 `복냥사주&타로` 로 노출 (구 `복냥사주` 흔적 없음) | 토스 미니앱 콘솔/디바이스 | [ ] |
| **UAT-05-9** | NAV-03 회귀 | HomePage 공유 / SajuPage 사주 / NewYearPage 딥리딩 / AmuletPage 인앱결제 — v1.0과 동일 동작 (Phase 05 변경 후 회귀 0) | AIT/dev | [ ] |

---

## § 5. Phase 03 — 데일리 원카드 코어 (갭, **선택적**)

> 03-VERIFICATION.md 미생성. RESEARCH/CONTEXT 에서 거론된 manual 항목이지만 트래킹 정의 없음.
> Audit 권고: **implicit pass** — Phase 04/05 검증에서 후속 동작 wiring 매치됨. 아래는 강하게 검증하고 싶을 때만 추가 수행.

| # | 항목 | 환경 | 결과 |
|---|------|------|------|
| (선택) **03-A** | 카드 뒤집기 60fps 유지 (Pitfall 1) — Chrome DevTools Performance 30s 캡처에서 main thread > 16ms long task 없음 | dev browser + DevTools | [ ] |
| (선택) **03-B** | 4050 여성 가독성 — TarotResult name_ko (24px) · 해석 본문 (16px) 디바이스에서 편한지 시각 검증 | AIT 디바이스 | [ ] |
| (선택) **03-C** | 22장 fetch 실패 동선 (Sentry capture + 에러 화면 + 재시도) — DevTools Offline + 타로 탭 진입 → 에러 화면 → 재시도 → 정상 fetch 복구 | dev + DevTools Network Offline | [ ] |
| (선택) **03-D** | Sentry 대시보드에 `phase: tarot_intro_fetch` 태그로 캡처 도착 (PII 미포함 확인) | Sentry 대시보드 | [ ] |

> 4건 모두 통과 후에는 [03-VALIDATION.md](.planning/phases/03-daily-one-card-core/03-VALIDATION.md) frontmatter 의 `nyquist_compliant: false` → `true`, status `draft` → `passed (via human UAT)` 로 갱신 권장.

---

## § 6. 진행 요약

```
┌──────────────────────────────────────────┐
│ v1.1 Human UAT — 22 / 22 trackable      │
├──────────────────────────────────────────┤
│ § 2  Phase 02 (탭바 셸)         5 / 5 ☐  │
│ § 3  Phase 04 (DAILY lock)      8 / 8 ☐  │
│ § 4  Phase 05 (공유 + Analytics) 9 / 9 ☐  │
├──────────────────────────────────────────┤
│ § 5  Phase 03 (선택, 4 추가)    0 / 4 ☐  │
└──────────────────────────────────────────┘
```

**완료 기준:** § 2 + § 3 + § 4 의 22건 통과 시 v1.1 출시 게이트 충족 → `/gsd-complete-milestone v1.1` 가능.

§ 5 (Phase 03) 은 milestone archive 후에도 보완 가능 — implicit pass 채택 시 skip.

---

## § 7. 발견된 이슈 / 메모 영역

(각 UAT 진행 중 발견된 이슈는 여기에 기록 — 차단 시 GitHub Issue / `/gsd-debug` 로 라우팅)

```
[ID] [발견일] [환경] 이슈 요약 → 후속 조치
```

예시 (실제 입력 시 삭제):
```
WR-XX  2026-05-04  AIT iOS  공유 시트가 한 번에 두 번 노출됨 → /gsd-debug 시작
```

---

*Generated by gsd-audit-milestone — 2026-05-04*
*Source aggregated from 02/04/05 VERIFICATION.md `human_verification:` frontmatter sections.*
