---
phase: quick-260914-hb2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/HomeHeroCarousel.jsx
  - src/pages/HomePage.jsx
autonomous: true
requirements: [QUICK-260914-hb2]

must_haves:
  truths:
    - "홈 최상단 Hero 영역이 배경이미지+문구 대신 애정운/궁합 2장짜리 가로 슬라이드 배너로 보인다"
    - "애정운 배너 탭 시 /newyear 로 이동하고 2026년 애정운(new_year_2026_love) 풀이 입력 화면이 열린다"
    - "궁합 배너 탭 시 /newyear 로 이동하고 기존 퀵메뉴 '궁합풀이'와 완전히 동일한 selectedType 으로 궁합 입력 화면(파트너 입력 포함)이 열린다"
    - "배너는 4.5초마다 자동으로 넘어가고, 터치/포인터/포커스 중이거나 prefers-reduced-motion 이면 멈춘다"
    - "하단 점 인디케이터가 현재 슬라이드를 표시하고(활성 점이 더 넓음), 점을 탭하면 해당 슬라이드로 스크롤된다"
    - "공유 버튼이 배너 우측 상단에 그대로 남아 있고 탭 시 기존 handleShare 가 동작한다"
    - "배너 탭은 trackClick('hero_banner_click', { menu: <fortuneType code> }) 를 발생시킨다"
  artifacts:
    - path: "src/components/HomeHeroCarousel.jsx"
      provides: "스크롤스냅 기반 2슬라이드 Hero 캐러셀 (프레젠테이션 컴포넌트)"
      exports: ["default"]
      min_lines: 80
    - path: "src/pages/HomePage.jsx"
      provides: "HERO_SLIDES 정의 + 공용 selectedType 상수/네비게이트 헬퍼 + 캐러셀 렌더"
      contains: "HomeHeroCarousel"
  key_links:
    - from: "src/pages/HomePage.jsx"
      to: "src/components/HomeHeroCarousel.jsx"
      via: "import HomeHeroCarousel + <HomeHeroCarousel slides onSlideClick onShare />"
      pattern: "<HomeHeroCarousel"
    - from: "src/pages/HomePage.jsx (궁합 배너 + 퀵메뉴 궁합풀이)"
      to: "COMPATIBILITY_SELECTED_TYPE 단일 상수"
      via: "공용 goToNewYear(selectedType) 헬퍼"
      pattern: "COMPATIBILITY_SELECTED_TYPE"
    - from: "src/pages/HomePage.jsx"
      to: "src/pages/NewYearPage.jsx"
      via: "navigate('/newyear', { state: { selectedType } }) — fortuneType === 'ai_saju_compatibility' 로 궁합 판정"
      pattern: "navigate\\(\"/newyear\""
---

<objective>
토스 미니앱 홈(HomePage.jsx)의 Hero 배너(배경이미지 + "AI가 알려주는 당신의 운명")를 애정운·궁합 바로가기 2장 슬라이딩 배너로 교체한다. 웹앱(/Users/trinity/Projects/catbot/fortunecat/app/core/screens/home.tsx 의 HERO_SLIDES + Hero())에 이미 구현된 기능을 미니앱 관용구(.jsx, inline style, 상대경로 import, trackClick)로 동일하게 포팅한다.

Purpose: 애정운/궁합 풀이 뒤에 "연애상담 모드" 후속채팅이 붙는다는 것을 홈 첫 화면에서 알리고 바로 진입시킨다.
Output: 신규 src/components/HomeHeroCarousel.jsx, 수정된 src/pages/HomePage.jsx, 커밋 1개.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/trinity/Projects/app-in-toss/fortune-cat/CLAUDE.md
@/Users/trinity/Projects/app-in-toss/fortune-cat/src/pages/HomePage.jsx
@/Users/trinity/Projects/catbot/fortunecat/app/core/screens/home.tsx

작업 디렉토리 주의: 미니앱 루트는 /Users/trinity/Projects/app-in-toss/fortune-cat, git 루트는 부모 /Users/trinity/Projects/app-in-toss (모노레포). 모든 경로는 절대경로로 다룬다.

<interfaces>
<!-- 실행자가 코드베이스를 탐색하지 않아도 되도록 추출한 계약 -->

From src/lib/analytics.js:
  export function trackClick(eventName, params = {}, buttonName)

HomePage.jsx 현재 구조 (수정 대상):
  - L18: import heroBackground from "../assets/images/hero.png";  // 이 파일에서만 사용 (grep 확인됨) → 제거
  - L19: import { trackClick } from "../lib/analytics";
  - L73-89: quickMenuItems 의 "궁합풀이" 항목 — navigate("/newyear", { state: { selectedType: { fortuneType: "ai_saju_compatibility", themeType: "ai_saju", readingType: "ai_saju", fortuneTypeTitle: "궁합풀이" } } })
  - L143-163: handleNewYearTypeClick(type, section) — DB row 를 { fortuneType: type.code, themeType: type.theme_type, readingType: type.reading_type, fortuneTypeTitle: type.title_ko } 로 매핑해 /newyear 이동
  - L207-220: handleShare (async, 내부에서 trackClick("share_click", {}, "home_share") 호출) — 그대로 유지
  - L231-317: Hero 블록 (교체 대상). 바깥 div 는 position relative, width "calc(100% + 40px)", margin "0 -20px" (container 의 좌우 padding 20px 을 상쇄하는 풀블리드)
  - L468: 기존 문구 "운세 보고 질문도 무제한으로 하기" — 이번 작업 범위 밖, 건드리지 않는다

src/pages/NewYearPage.jsx:
  const selectedType = location.state?.selectedType  // 없으면 '/' 로 replace
  const isCompatibility = selectedType?.fortuneType === 'ai_saju_compatibility'

Supabase 검증값 (애정운, new_year_fortune_types):
  code "new_year_2026_love", title_ko "2026년 애정운", theme_type "new_year_2026_love", reading_type "new_year_2026_love"

eslint.config.js: js recommended + react-hooks recommended (exhaustive-deps 경고) + no-unused-vars(error, varsIgnorePattern '^[A-Z_]') + react-refresh/only-export-components(warn).
src/components/Intro.jsx 의 자동 회전 관용구: useEffect 안에서 setInterval → cleanup 에서 clearInterval.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HomeHeroCarousel 프레젠테이션 컴포넌트 생성</name>
  <files>/Users/trinity/Projects/app-in-toss/fortune-cat/src/components/HomeHeroCarousel.jsx</files>
  <action>
신규 파일 src/components/HomeHeroCarousel.jsx 를 만든다. 웹앱 home.tsx 의 Hero() 를 JSX + inline style 로 포팅하되, 네비게이션/데이터는 모르는 순수 프레젠테이션 컴포넌트로 둔다 (슬라이드 데이터·이동은 HomePage 가 소유 — Task 2).

Props: `slides` (배열, 각 원소 { key, icon, eyebrow, title, description, bg } + 그 외 필드는 그대로 통과), `onSlideClick(slide)`, `onShare()`. `export default function HomeHeroCarousel({ slides, onSlideClick, onShare })`. 모듈 상수 `HERO_AUTOPLAY_MS = 4500`. 이 파일은 컴포넌트 하나만 export (react-refresh 경고 방지).

구조와 동작 (웹앱과 동일하게):
1. 루트 `<section>`: style { position "relative", width "calc(100% + 40px)", margin "0 -20px" } (기존 Hero 풀블리드 유지). 속성 aria-roledescription="carousel", aria-label="연애상담 풀이 바로가기". 일시정지 핸들러: onPointerEnter→paused true, onPointerLeave→false, onTouchStart→true, onTouchEnd→false, onFocusCapture→true, onBlurCapture→false.
2. 첫 자식: `<h1>복냥사주·타로</h1>` 을 시각적으로 숨김 처리 (미니앱엔 Tailwind 없음 → 모듈 상수 `visuallyHidden` inline style: position absolute, width 1px, height 1px, padding 0, margin -1px, overflow hidden, clip "rect(0, 0, 0, 0)", whiteSpace "nowrap", border 0).
3. 스크롤러 `<div ref={scrollerRef} onScroll={onScroll} className="home-hero-scroller">`: style { display flex, overflowX "auto", scrollSnapType "x mandatory", WebkitOverflowScrolling "touch", scrollbarWidth "none" }. WebKit 스크롤바 숨김은 inline style 로 불가하므로 컴포넌트 안에 `<style>` 요소 하나로 `.home-hero-scroller::-webkit-scrollbar { display: none; }` 만 주입한다 (새 의존성/전역 CSS 수정 금지 — index.css 는 건드리지 않는다).
4. 각 슬라이드: `<button type="button">` (미니앱 관용구상 탭 요소는 button; key={slide.key}), onClick → onSlideClick(slide). 속성 role="group", aria-roledescription="slide", aria-label={`${i + 1} / ${slides.length}: ${slide.title}`}. style: position relative, display flex, alignItems center, flex "0 0 100%", width "100%", minHeight "176px", scrollSnapAlign "center", padding "36px 20px 40px", boxSizing border-box, border none, cursor pointer, textAlign left, color "#2a1f36", background slide.bg, fontFamily "inherit".
   - 텍스트 래퍼 div: flex 1, minWidth 0, paddingRight "64px" (이모지/공유버튼과 겹치지 않도록).
     - eyebrow `<p>`: fontSize 13px, fontWeight 700, color "#b33a6a", margin 0.
     - title `<p>`: marginTop 4px, margin 좌우하 0, fontSize 26px, fontWeight 800, lineHeight 1.25.
     - description `<p>`: marginTop 8px, fontSize 14px, lineHeight 1.6, color "rgba(42,31,54,0.8)".
     - CTA pill `<span>`: marginTop 12px, display inline-flex, alignItems center, gap 4px, borderRadius 999px, background "#2a1f36", padding "6px 12px", fontSize 13px, fontWeight 500, color "#fff". 문구 정확히 "연애상담 모드 · 바로 보기 →".
   - 아이콘 `<span aria-hidden="true">`: position absolute, right 16px, bottom 32px, fontSize 56px, lineHeight 1, filter "drop-shadow(0 4px 10px rgba(0,0,0,0.15))".
5. 점 인디케이터: position absolute, bottom 12px, left 0, right 0, display flex, justifyContent center, gap 6px. 각 점 `<button type="button">` onClick → goTo(i), aria-label={`${i + 1}번째 배너 보기`}, aria-current={active === i}. style: height 6px, width 활성 20px / 비활성 6px, borderRadius 999px, border none, padding 0, cursor pointer, background 활성 "#2a1f36" / 비활성 "rgba(42,31,54,0.3)", transition "all 0.2s ease".
6. 공유 버튼: 래퍼 div position absolute, top 16px, right 20px. 버튼은 기존 HomePage L282-315 의 공유 버튼 style·aria-label="공유"·svg 를 그대로 옮기고 onClick={onShare}.

상태/로직:
- `const scrollerRef = useRef(null)`, `const [active, setActive] = useState(0)`, `const [paused, setPaused] = useState(false)`.
- `goTo(index)`: el 없으면 return; el.scrollTo({ left: index * el.clientWidth, behavior: "smooth" }).
- `onScroll`: el 없거나 clientWidth 0 이면 return; setActive(Math.round(el.scrollLeft / el.clientWidth)).
- 자동재생 useEffect(deps [active, paused, slides.length]): paused 이거나 slides.length <= 1 이면 return; window.matchMedia 가 있고 "(prefers-reduced-motion: reduce)" matches 면 return; setInterval 로 HERO_AUTOPLAY_MS 마다 다음 인덱스((active + 1) % slides.length)로 스크롤 → cleanup clearInterval. exhaustive-deps 경고를 피하려고 goTo 를 effect 의존성에 넣지 말고, effect 안에서 scrollerRef.current 로 직접 scrollTo 를 수행한다 (ref 는 deps 불필요).
- 신규 문구에 "무제한" 단어 사용 금지.
  </action>
  <verify>
    <automated>cd /Users/trinity/Projects/app-in-toss/fortune-cat && npx eslint src/components/HomeHeroCarousel.jsx && test "$(grep -c '무제한' src/components/HomeHeroCarousel.jsx)" = "0" && grep -q '연애상담 모드 · 바로 보기 →' src/components/HomeHeroCarousel.jsx && grep -q 'scrollSnapType' src/components/HomeHeroCarousel.jsx && grep -q 'prefers-reduced-motion' src/components/HomeHeroCarousel.jsx</automated>
  </verify>
  <done>HomeHeroCarousel.jsx 가 존재하고 eslint 에러/경고 0, "무제한" 0회, 스크롤스냅·자동재생(4500ms, 일시정지·reduced-motion)·점 인디케이터·공유 버튼·시각적 숨김 h1 이 모두 포함됨.</done>
</task>

<task type="auto">
  <name>Task 2: HomePage 에 슬라이드 데이터·공용 네비게이트 헬퍼 연결, 기존 Hero 교체 후 빌드·커밋</name>
  <files>/Users/trinity/Projects/app-in-toss/fortune-cat/src/pages/HomePage.jsx</files>
  <action>
src/pages/HomePage.jsx 만 수정한다.

1. import 정리: L18 `import heroBackground from "../assets/images/hero.png";` 삭제 (이 파일에서만 쓰임. hero.png 에셋 파일 자체는 삭제하지 않는다). `import HomeHeroCarousel from "../components/HomeHeroCarousel";` 추가.

2. 모듈 상수 (컴포넌트 밖, SHOW_AMULET 아래) — DRY:
   - `COMPATIBILITY_SELECTED_TYPE` = { fortuneType: "ai_saju_compatibility", themeType: "ai_saju", readingType: "ai_saju", fortuneTypeTitle: "궁합풀이" } — 기존 퀵메뉴 궁합풀이 값과 완전히 동일(브리프 지시: 기존 퀵메뉴와 다르게 가지 않는다).
   - `LOVE_SELECTED_TYPE` = { fortuneType: "new_year_2026_love", themeType: "new_year_2026_love", readingType: "new_year_2026_love", fortuneTypeTitle: "2026년 애정운" } — Supabase 검증값, handleNewYearTypeClick 이 DB row 로 만드는 모양과 동일.
   - `HERO_SLIDES` 배열 2개, 각 { key, icon, eyebrow, title, description, bg, selectedType }:
     a) key "new_year_2026_love", icon "❤️", eyebrow "2026 애정운", title "올해 내 연애운은?", description "풀이를 보고 나면 복냥이와 연애상담까지 이어져요", bg "linear-gradient(135deg, #fde4ec 0%, #f7b6cd 100%)", selectedType LOVE_SELECTED_TYPE
     b) key "ai_saju_compatibility", icon "💕", eyebrow "궁합 풀이", title "우리, 찰떡일까 상극일까?", description "두 사람 궁합을 보고 실제 고민을 연애상담으로 물어보세요", bg "linear-gradient(135deg, #ece3f8 0%, #c4ade8 100%)", selectedType COMPATIBILITY_SELECTED_TYPE
   (key 는 fortuneType 코드와 동일한 문자열이지만 추적에는 selectedType.fortuneType 을 쓴다.)

3. 컴포넌트 내부 헬퍼:
   - `const goToNewYear = (selectedType) => navigate("/newyear", { state: { selectedType } });`
   - handleNewYearTypeClick 의 navigate 호출도 goToNewYear({ fortuneType: type.code, ... }) 로 바꿔 동일 호출을 한 곳으로 모은다 (trackClick 은 그대로).
   - quickMenuItems 궁합풀이 onTap: trackClick 은 그대로 두고 인라인 navigate 객체를 `goToNewYear(COMPATIBILITY_SELECTED_TYPE)` 로 교체.
   - `const handleHeroSlideClick = (slide) => { trackClick("hero_banner_click", { menu: slide.selectedType.fortuneType }, slide.eyebrow); goToNewYear(slide.selectedType); };` (trackClick 3번째 인자 buttonName 은 기존 관용구대로 사람이 읽는 라벨).
   - goToNewYear 는 quickMenuItems 선언보다 위에 정의한다 (onTap 은 지연 호출이라 문제는 없지만 가독성).

4. JSX: L231-317 의 `{/* 히어로 영역 */}` div 블록 전체(배경이미지·오버레이·h1·부제·공유버튼)를 `<HomeHeroCarousel slides={HERO_SLIDES} onSlideClick={handleHeroSlideClick} onShare={handleShare} />` 한 줄(주석 `{/* 히어로: 애정운·궁합 연애상담 바로가기 배너 */}` 포함)로 교체. Quick Menu 이하 및 styles 객체는 변경하지 않는다. L468 기존 문구 등 무관한 코드는 건드리지 않는다.

5. 검증 후 커밋 (push 금지):
   - `cd /Users/trinity/Projects/app-in-toss/fortune-cat && npx eslint src/pages/HomePage.jsx src/components/HomeHeroCarousel.jsx` — 이번 변경으로 인한 새 에러 0 (HomePage 에 기존부터 있던 경고가 있으면 SUMMARY 에 기록만).
   - `npx vite build` 성공 확인 (ait build 는 네트워크 필요할 수 있어 vite build 로 컴파일 검증). dist 산출물은 커밋하지 않는다.
   - git 루트는 /Users/trinity/Projects/app-in-toss. 반드시 변경한 두 파일만 명시적으로 add: `git -C /Users/trinity/Projects/app-in-toss add fortune-cat/src/pages/HomePage.jsx fortune-cat/src/components/HomeHeroCarousel.jsx` (절대 `git add -A` / `git add .` 금지 — .gitignore, granite.config.ts, 형제 앱 등에 무관한 미커밋 변경이 있음). 커밋 전 `git -C /Users/trinity/Projects/app-in-toss diff --cached --name-only` 결과가 정확히 이 두 파일인지 확인.
   - 커밋 메시지:
     feat(home): Hero 배너를 애정운·궁합 연애상담 바로가기 슬라이드 배너로 교체

     Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
     Claude-Session: https://claude.ai/code/session_01WgR8Yu3oFWWPENiBC2Mw1u
  </action>
  <verify>
    <automated>cd /Users/trinity/Projects/app-in-toss/fortune-cat && npx eslint src/pages/HomePage.jsx src/components/HomeHeroCarousel.jsx && test "$(grep -c 'heroBackground' src/pages/HomePage.jsx)" = "0" && grep -q '<HomeHeroCarousel' src/pages/HomePage.jsx && test "$(grep -c 'COMPATIBILITY_SELECTED_TYPE' src/pages/HomePage.jsx)" -ge 3 && grep -q 'hero_banner_click' src/pages/HomePage.jsx && grep -q 'new_year_2026_love' src/pages/HomePage.jsx && npx vite build >/dev/null && echo BUILD_OK</automated>
  </verify>
  <done>HomePage 가 HomeHeroCarousel 을 렌더하고 heroBackground 참조 0, 궁합 selectedType 은 COMPATIBILITY_SELECTED_TYPE 단일 상수(정의 1 + 퀵메뉴 1 + HERO_SLIDES 1)로 공유, /newyear 이동은 goToNewYear 한 곳, 배너 탭 시 hero_banner_click 추적, eslint 새 에러 0, vite build 성공, 두 파일만 포함된 커밋 1개 생성(push 안 함).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 없음 (신규) | 정적 상수 기반 UI + 기존 라우터 state 전달만 추가. 사용자 입력·네트워크·결제 경로 변경 없음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-hb2-01 | Tampering | HomePage → NewYearPage selectedType | accept | 하드코딩 상수이며 기존 퀵메뉴/카드 탭과 동일한 경로. 결제/페이월 판정은 기존 로직 그대로(이번 작업에서 미변경) |
| T-hb2-02 | Information Disclosure | trackClick('hero_banner_click') | accept | 전송값은 fortuneType 코드뿐, PII 없음 |
| T-hb2-03 | Tampering | 모노레포 커밋 범위 | mitigate | 변경 두 파일만 명시 add + `git diff --cached --name-only` 확인, `git add -A`/`.` 금지 |
</threat_model>

<verification>
- `npx eslint src/pages/HomePage.jsx src/components/HomeHeroCarousel.jsx` 새 에러 0
- `npx vite build` 성공
- 커밋에 포함된 파일이 정확히 fortune-cat/src/pages/HomePage.jsx, fortune-cat/src/components/HomeHeroCarousel.jsx
- (선택, 수동) `npm run dev` 후 홈에서 배너 2장 스와이프·자동넘김·점 탭·애정운/궁합 진입·공유 버튼 확인
</verification>

<success_criteria>
- 홈 Hero 가 애정운/궁합 2슬라이드 캐러셀(웹앱과 동일 문구·색·CTA)로 교체됨
- 애정운 → /newyear (new_year_2026_love), 궁합 → /newyear (기존 퀵메뉴 궁합풀이와 동일 selectedType)
- 자동재생 4500ms + 상호작용/reduced-motion 시 정지, 점 인디케이터 동작, 공유 버튼 유지, h1 시각적 숨김 유지
- 결제/페이월·DeepReadingResult·granite.config.ts·.gitignore·형제 앱 무변경, push 없음
</success_criteria>

<output>
Create `/Users/trinity/Projects/app-in-toss/fortune-cat/.planning/quick/260914-hb2-homepage-hero-2/260914-hb2-SUMMARY.md` when done
</output>
