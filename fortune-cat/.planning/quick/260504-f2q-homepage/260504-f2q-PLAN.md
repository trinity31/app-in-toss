---
phase: quick/260504-f2q-homepage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/HomePage.jsx
autonomous: true
requirements:
  - QUICK-260504-F2Q
must_haves:
  truths:
    - "토스 공유 링크를 통해 진입한 사용자가 보는 HomePage H1 헤더가 '복냥사주&타로'로 표시된다"
    - "변경 외의 헤더 스타일/레이아웃/타이포그래피는 그대로 유지된다"
  artifacts:
    - path: "src/pages/HomePage.jsx"
      provides: "HomePage H1 헤더 텍스트"
      contains: "복냥사주&타로"
  key_links:
    - from: "src/pages/HomePage.jsx:233"
      to: "rendered H1 텍스트 노드"
      via: "JSX text child of <h1>"
      pattern: "복냥사주&타로"
---

<objective>
HomePage 최상단 H1 헤더 텍스트를 `복냥사주`에서 `복냥사주&타로`로 변경한다.

Purpose: v1.1 마일스톤에서 타로 탭이 통합되어, 토스 공유 링크를 따라 진입한 사용자가 처음 보는 HomePage 헤더가 결합된 제품 정체성(사주 + 타로)을 반영해야 한다.
Output: HomePage.jsx의 H1 한 줄 텍스트가 갱신된 상태.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@src/pages/HomePage.jsx

<interfaces>
<!-- 변경 대상 라인의 현재 상태 (HomePage.jsx:225-234) -->
<!-- 단일 텍스트 노드만 변경. 주변 스타일/레이아웃은 건드리지 않는다. -->

```jsx
            position: "relative",
            fontSize: "36px",
            fontWeight: "800",
            color: "#fff",
            margin: "0 0 0px 0",
            textShadow: "0 1px 4px rgba(0,0,0,0.3)",
          }}
        >
          복냥사주
        </h1>
```

변경 후:
```jsx
          복냥사주&타로
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HomePage H1 헤더 텍스트 갱신</name>
  <files>src/pages/HomePage.jsx</files>
  <action>
src/pages/HomePage.jsx 233번 라인의 H1 자식 텍스트 `복냥사주`를 `복냥사주&타로`로 정확히 한 글자 단위로 교체한다. 다른 라인, 다른 텍스트, 스타일, 들여쓰기는 변경하지 않는다.

제약:
- Edit 툴로 정확히 233번 라인의 `복냥사주` 텍스트 노드만 교체. 주변 `<h1>` 스타일 객체, `</h1>` 닫는 태그, 형제 노드는 손대지 않는다.
- `&`는 JSX 텍스트 노드 안에서 그대로 사용 가능 — `&amp;` 등 엔티티 변환 금지(현재 코드베이스 컨벤션상 한글 텍스트 + `&` 직접 사용이 자연스럽다).
- 본 파일에 다른 `복냥사주` 문자열이 있을 수 있으나(타이틀 외 본문/카드 텍스트 등) **이 작업의 스코프는 233번 라인 H1 한 곳뿐**이다. 다른 위치는 사용자가 명시적으로 지시하지 않은 한 절대 변경하지 않는다.
  </action>
  <verify>
    <automated>grep -n "복냥사주&타로" src/pages/HomePage.jsx | grep -E ":233:" && ! grep -nE "^\s*복냥사주\s*$" src/pages/HomePage.jsx | grep -E ":233:"</automated>
  </verify>
  <done>
- src/pages/HomePage.jsx 233번 라인이 `          복냥사주&타로` (들여쓰기 보존)로 변경됨
- 그 외 라인은 git diff 기준 변경 없음 (단일 라인 diff)
- 빌드/린트 깨짐 없음 (JSX 텍스트 노드 단순 변경이므로 회귀 위험 없음)
  </done>
</task>

</tasks>

<verification>
1. `git diff src/pages/HomePage.jsx` — 단일 라인만 변경되었는지 확인
2. `grep -n "복냥사주" src/pages/HomePage.jsx` — 233번 라인에 `복냥사주&타로`가 존재하는지, 다른 의도치 않은 변경이 없는지 확인
3. (선택) 사용자가 dev 서버에서 HomePage 진입 시 H1이 `복냥사주&타로`로 렌더링되는지 육안 확인 — 본 quick 작업의 done 조건에는 포함하지 않음
</verification>

<success_criteria>
- HomePage.jsx 233번 라인의 H1 텍스트가 `복냥사주&타로`
- 같은 파일 내 다른 라인은 git diff 기준 0개 변경
- v1.1 마일스톤의 결합된 제품 정체성이 첫 화면 헤더에 반영됨
</success_criteria>

<output>
완료 후 `.planning/quick/260504-f2q-homepage/260504-f2q-SUMMARY.md` 작성:
- 변경 라인, 변경 전/후 텍스트
- 스코프 외 사항(공유시트 displayName, OG 이미지 캐시)은 본 작업에서 다루지 않음을 명시
</output>
